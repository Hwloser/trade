data:
  data_root: "data"
  raw_dir: "raw"
  silver_dir: "silver"
  models_dir: "models"
  market_daily_subpath: "cn_a/daily"

ingestion:
  default_history_days: 30
  incremental_lookback_days: 5
  min_start_date: "2026-01-01"
  daily_bar_dataset: "cn_a_daily_bar"
  write_raw_layer: true
  write_silver_layer: false
  request_dedup_hours: 24
  stream_poll_interval_sec: 60
