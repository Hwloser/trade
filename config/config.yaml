# Minimal entry config. Keep this file small.
# Add new modules under config/modules and include them here.
includes:
  - modules/data_ingestion.yaml
  - modules/provider_eastmoney.yaml
  - modules/storage_google_drive.yaml
  - modules/security.yaml
  - modules/sentiment.yaml

# Optional local overrides can be added below, for example:
# storage:
#   enabled: true
#   backend: "google_drive"
#   write_mode: "hybrid"
#   hot_days: 30
