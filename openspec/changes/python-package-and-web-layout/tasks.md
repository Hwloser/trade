## 1. Governed Design Approval

- [ ] 1.1 Validate the source-backed package/layout design. Objective: make the child internally complete and traceable before implementation. Inputs: parent architecture, current `pyproject.toml`, root facade, package/import scan, ASGI/runtime, Vite, CMake, notebook/script/test inventory, child proposal/design/specs and policy v1. Outputs: governed artifacts and zero deterministic findings. Affected contracts: distribution/import, CLI, ASGI/HTTP/Web, SDK/notebook, native, deployment control and developer/test paths. Validation: `openspec validate python-package-and-web-layout --strict --no-interactive`, `./trade dev design-check python-package-and-web-layout` and `git diff --check` return zero; every requirement is mapped to an obligation and test/review task. Rollback: revise OpenSpec only; no runtime state or source path changed. Completion evidence: command outputs, selected profiles and artifact digest are recorded. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 1.2 Run the required six-role review in a dedicated review worktree. Objective: challenge package authority, selector fencing/recovery, rollback, wheel/reload/process behavior, lifecycle ownership, route/assets, native isolation, notebook/scripts, capacity and future integration. Inputs: frozen artifact generation plus actual code references. Outputs: six file/line-citing judge reports and one consensus with P0/P1/P2 dispositions. Affected contracts: all child capabilities. Validation: reliability, performance, architecture, data-quality, observability and news/future judges complete; all P0 and implementation-blocking P1 findings are resolved or assigned to a named prerequisite child. Rollback: edit design, invalidate the digest and repeat affected review. Completion evidence: consensus report and finding evidence map. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.control] [validates:layout.governance] [validation:review]

- [ ] 1.3 Record digest-bound review evidence and obtain current strict approval. Objective: bind implementation to the reviewed package/layout generation. Inputs: unchanged governed artifacts, current commit/date and resolved consensus. Outputs: approved `design-review.toml`. Affected contracts: all child requirements. Validation: `./trade dev design-check python-package-and-web-layout --strict` exits zero. Rollback: do not edit production/config/test code; correct artifacts and repeat review. Completion evidence: current policy/artifact digests, full commit, current date, all six approved roles and zero unresolved P0. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.control] [validates:layout.governance] [validation:review]

## 2. Package Authority Foundation

- [ ] 2.1 Reconcile prerequisites and create the implementation worktree. Objective: start code only where the strict design and prior semantic children permit it. Inputs: strict child approval, merged architecture guard, current `kernel-and-public-contracts` package proof, owner/interface/Bootstrap child statuses and refreshed `git status -sb`. Outputs: dedicated implementation worktree/branch and a slice ledger marking eligible and blocked work. Affected contracts: package discovery and migration sequencing. Validation: no production movement is selected without an implemented owner; unrelated dirty/untracked files are absent from the worktree; strict digest still matches. Rollback: remove the unused worktree/branch. Completion evidence: worktree path, branch, prerequisite SHAs and selected first slice. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.2 Prove additive dual-root package discovery in source, editable and clean-wheel environments. Objective: install an empty/contract-only `trade` foundation from `src/trade` while preserving distribution `trade-py`, legacy `trade_py`, root `./trade` and `trade-py` console behavior without moving production ownership. Inputs: approved packaging ADR, current build backend and narrowly reconciled `kernel-and-public-contracts` package proof SHA. Outputs: smallest explicit package configuration, wheel-member allowlist and temporary-environment smoke harness. Affected contracts: distribution, imports and console. Validation: build one wheel; install outside the repository; record digest/member/origin/version/entrypoints; import only approved foundation plus legacy modules with optional native absent; run root/console help parity; fail if checkout path appears; run the committed cold/warm CLI/import RSS/module baseline matrix. Rollback: revert package discovery and harness; legacy distribution remains. Completion evidence: source/editable/wheel matrix passes, no production module changes authority and no tests/data/notebooks/frontend/build/vendor/history paths are installed. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.3 Implement a deterministic shared tree index, module-authority manifest and architecture guard. Objective: prevent duplicate implementation, reverse target-to-legacy imports, unclassified moves, optional-interface leakage and repeated whole-tree work. Inputs: refreshed consumer inventory and authority record schema. Outputs: one immutable index per tree/scanner/rules digest, deterministic owner/module partitions, bounded source-only manifest validator, migration-state validation and focused fixtures. Affected contracts: module authority, compatibility direction and future optional-interface boundaries. Validation: tests cover one-scan cache identity/invalidation, import order/concurrency, one-hop bridges, duplicate registry/handler/resource, broad `sys.modules` alias, target `__path__` extension, reverse dependency, plugin/MCP/remote-worker imports from domain/use-case/forwarder modules, absent/incompatible/duplicate optional registrations in a clean wheel, stale consumer ref, scanner/rules/tree mismatch, incomplete/tool-failed inventory, missing owner, over-50-module/500-consumer refusal, 24-hour age boundary, deterministic partitions and current/synthetic-10x wall/RSS bounds; validator reads no real data and does not invent optional modules. Rollback: remove unused index/guard/manifest; no module authority changes. Completion evidence: architecture check fails each invalid fixture, passes current inventory without authorizing a business move and records one shared scan per digest. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.4 Implement immutable read-only evidence vocabulary and layout diagnostics. Objective: make activation, recovery and rollback inspectable without conflating lifecycle, execution, startup health, failure, reconciliation or rollback state and without implementing or semantically owning selector mutation in this parent. Inputs: `ConsumerInventoryRef`, read-only `LayoutSelectorSnapshotV1` and `OperationStatusSnapshotV1`, `PreparedEvidenceRef`, `MigrationEvidenceRef`, `LayoutStatusConstraintsV1`, closed report axes and current `./trade dev` routing. Outputs: dependency-free bounded source/control/evidence snapshot parsers, a pure versioned legality/action/exit oracle, immutable evidence validation and additive `./trade dev layout-status [--json]`; no CAS API, mutable journal transition, start-intent/service-manager protocol, reconciliation transition, slice-specific plan, activation, repair or retention command. Affected contracts: developer diagnostic only; existing user CLI remains unchanged. Validation: a pure property test exhaustively enumerates the finite migration/execution/failure/rollback/startup/reconciliation/action product and proves each product has exactly one healthy/attention/invalid class, derived action and order-independent exit; temporary explicit-root fixtures cover every state value, receipt predicate and representative allowed/forbidden product including `not_run`, `running`, `started_degraded`, rollback `ready/requested/running/unknown`, operation-ID conflict, generation/revision/fence mismatch, phase ordering, stale process, passed activation, timeout with successful forward-revision rollback, non-LIFO compensation, shutdown TERM/KILL/residue, tool failure and partial evidence. Human/JSON goldens prove identical enum/action/exit semantics, including `resume_reconciliation`, `retry_identical_invocation` and `execute_reviewed_rollback`; fixtures accept historical `process_started` only with a later exact-identity terminal absence receipt and reject live/unsuperseded/mismatched starts, adopted-without-process, teardown-with-residue, required/failed-with-passed-authoritative-retireable state, non-derived actions and a historical Python rollback target that removes later slices. The explicit reader accepts at most 32 records/eight reference levels/256 KiB per record/4 MiB aggregate/five seconds and rejects directory walks, Git-history scans, symlinks, `/proc`/service-manager enumeration, live process probes, oversized/corrupt records and unknown enums with exit 2; valid attention states return 1 and only all-healthy consistent reports return 0. Unset/default real roots fail before access and the command performs zero provider/DB/parquet/build/activation/adoption/retry/teardown/repair calls. Rollback: remove the unused diagnostic and read models; current runtime remains authoritative and no selector was written. Completion evidence: the exhaustive oracle report plus paired human/JSON goldens name active/prior generation and revision, observed activation phase, startup/reconciliation states, process residue, prepared/final evidence refs, failure, derived action and exact exit without secrets or raw payloads. [validates:layout.package] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 2.5 Run focused package/authority/evidence checks and commit the foundation units. Objective: establish additive migration inspection mechanics before moving or activating any behavior. Inputs: Tasks 2.2-2.4 and 2.6 plus the reconciled kernel package proof. Outputs: separately validated package, guard/index and read-only evidence/status commits that do not duplicate prerequisite code. Affected contracts: build/install/import/authority/evidence developer contract. Validation: focused pytest, source/editable/wheel smoke, layout-status goldens, performance/capacity fixtures, `python -m compileall src/trade trade_py tests`, architecture guard, `./trade dev check --show-plan`, `./trade dev check` and `git diff --check`; inspect generated files and stage no selector state, wheel/cache/venv artifacts. Rollback: revert each additive commit while retaining the legacy distribution/runtime authority. Completion evidence: exact commands/outcomes, prerequisite SHA mapping, no selector writes and no production runtime behavior change. [validates:layout.package] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 2.6 Enforce package/layout performance and validation-process-tree budgets. Objective: detect compatibility and CI-cost regressions before authority movement without claiming unimplemented host-global admission. Inputs: committed runner/toolchain/dependency identity, current package/import/index/Web baselines and reviewed thresholds. Outputs: deterministic benchmark harness and machine-readable baseline/candidate evidence. Affected contracts: developer validation capacity, startup/import and Web build performance. Validation: 15 cold processes and 30 warm samples after five warmups cover root/console/domain help, `trade`, `trade_web` and factory construction; p50/p95, peak RSS and imported-module thresholds fail closed; current/10x one-scan indexes, unique-root Web no-change/cold/incremental cache behavior, worker/heavy-job/RSS/disk/queue contention and cleanup are exercised within one owned process tree without real-data access; any future cross-invocation/host-global claim requires a reviewed shared lease and a two-invocation contention fixture. Rollback: remove the unused harness/baseline; no authority or runtime state changes. Completion evidence: same-runner baseline/candidate reports, explicit process-tree refusal states and zero residual validation PIDs. [validates:layout.package] [validates:layout.web] [validates:layout.governance] [validation:test]

- [ ] 2.7 Create and strictly approve `layout-selector-control-plane` before any mutable activation implementation. Objective: bind a policy-free finite-scope store, concrete POSIX/deployment adapter, typed owner adapters, operator commands and service-manager integration to the durable selector contract. Inputs: parent read-only vocabulary, real deployment topology and filesystem semantics, exact controller/service-manager/owner-adapter paths, permission/capacity/retention policy and failure injection plan. Outputs: child proposal/design/spec/tasks with concrete writer/readers, root configuration, closed scope enum, CAS/journal/start-intent/process/recovery protocols, acyclic ordering `LayoutActivationPlanV1 -> PreparedEvidenceRef -> selector/phase receipts -> terminal MigrationEvidenceRef -> authority record`, direct selector-to-operation/process indexes, finite service-manager probes, capacity/high-water/disk-pressure/collection behavior, content-addressed single-flight compensation and compensating rollback topology; `tools/layout_control` contains no Python-module, ASGI, Web, native or business policy. Affected contracts: deployment selector, operation journal, stable deployment-unit identity/invocation token, evidence lineage, process fencing, process-safe activation and layout status. Validation: the child freezes concrete files and passes six-role review plus current strict design approval before writer code; its design covers concurrent activation/rollback, lost CAS response, crash before spawn, spawn success with response loss, crash after receipt persistence, no cyclic plan/prepared ref, placeholder or final-evidence overwrite, adoption/absence proof/fenced teardown, exact-identity historical start supersession, O(1) or O(log n) recovery indexes without historical-tree or host-wide process scans, finite probe/backoff/deadline, recovery storms, corrupt predecessor, stale process, backup/hash/retention, bounded collection/disk refusal and a content-addressed single-flight non-LIFO compensation fixture preserving later slices. Rollback: discard the child before implementation; the read-only parent foundation remains usable and no generation changes. Completion evidence: child approved digest and prerequisite SHA. [validates:layout.control] [validates:layout.web] [validates:layout.governance] [validation:review]

## 3. Native Boundary

- [ ] 3.1 Reconcile native binding source/build identity and create `native-module-isolation` before editing CMake. Objective: prove what code defines the extension and digest-bind exact native files, owner Ports, consumers, selector and rollback instead of assuming referenced paths exist. Inputs: current CMake target, Git inventory, generated-source policy, supported build presets and owner prerequisites. Outputs: binding source/initializer/output/wheel inventory or explicit blocker plus a concrete child proposal/design/spec/tasks. Affected contracts: optional native module identity. Validation: source paths are tracked or reproducibly generated by a reviewed command; initializer and expected output can be inspected; child passes six-role review and strict approval; no network fetch is required by validation without approval. Rollback: no code edit when proof or child approval fails. Completion evidence: exact source/build identity and approved child digest, or the native slice remains blocked. [validates:layout.native] [validates:layout.governance] [validation:review]

- [ ] 3.2 Rename and isolate the optional extension as `_trade_native` behind owner adapters. Objective: remove the `trade_py` namespace collision without changing algorithms. Inputs: Task 3.1 proof and implemented owner Port(s). Outputs: aligned CMake/initializer/output name, explicit capability probe and adapter-only imports. Affected contracts: optional native import and owner Port. Validation: clean build/install with native absent/present, architecture guard, no implicit import from `trade`/`trade_py`, CMake build/ctest and owner C++/Python differential fixtures; unsupported state is explicit. Rollback: disable the target native adapter and restore the prior optional build target without moving business code. Completion evidence: `_trade_native.__spec__` and artifact identity match; zero direct domain/use-case imports. [validates:layout.native] [validates:layout.package] [validation:test]

- [ ] 3.3 Commit the independently reversible native unit. Objective: keep native naming separate from Python owner moves. Inputs: passing Task 3.2. Outputs: one validated commit. Affected contracts: native capability only. Validation: repeat clean build/install, differential tests, relevant `./trade test <preset>`, Python import smoke and `git diff --check`. Rollback: revert the commit and leave native unavailable/legacy without changing Python semantics. Completion evidence: commands and supported/unsupported platform state. [validates:layout.native] [validation:test]

## 4. Owner-Ready Python Migration Slices

- [ ] 4.1 Select one bounded owner-ready module slice and obtain strict child approval. Objective: move semantic ownership, not a cosmetic directory or post-approval selection. Inputs: implemented owner child, complete fresh shared-index `ConsumerInventoryRef`, public contract/golden/performance baseline and no more than 50 production modules/500 consumers. Outputs: `owner-<name>-python-layout-slice` with frozen old/target modules, exports, dynamic/patch consumers, state/DB/artifact/side effects, prerequisite/source SHAs and immutable `LayoutActivationPlanV1` naming the shared `python_deployment` expected generation/revision, current composition digest, desired slice delta, target/compensation generation, operation/owner-adapter argv digests and tests. Scheduler, Capture, Dataset, event or news paths additionally emit a typed per-path temporal manifest and canonical ordered registration manifest; optional-interface paths remain blocked for Task 5.4. Affected contracts: selected owner and legacy imports. Validation: every file has one owner/cell or is split-required; mixed files stay legacy; temporal fixtures cover missing/ambiguous timezone, DST fold/gap, restart misfire, bounded catch-up, semantic registration-order drift, late correction/tombstone, replay boundaries and duplicate event identity; every ambiguity leaves authority blocked; parent dependency/DB-owner rules pass; child six-role review and strict design-check pass; stale generation/revision CAS is specified to refuse activation. Rollback: discard the child before edits. Completion evidence: approved child digest, slice and temporal manifests where applicable, activation-plan digest, baselines and prerequisite SHAs. [validates:layout.package] [validates:layout.control] [validates:layout.governance] [validation:review]

- [ ] 4.2 Implement only the strictly approved owner child and add explicit one-hop legacy forwarders. Objective: establish one target authority while retaining supported imports. Inputs: Task 4.1 approved digest, manifests, baselines and activation plan plus approved selector-control implementation. Outputs: exact target owner modules, target-internal imports and narrow old-path forwarders with no side effects. Affected contracts: owner public imports and existing consumers. Validation: old/new import-order/concurrency and object-identity tests, cumulative bridge latency/RSS/module count, owner unit/integration/contract tests, applicable temporal negative fixtures, no reverse legacy or forbidden optional-interface import, no duplicate registry/worker/repository/provider call, source/editable/wheel smoke, fenced start-intent/phase/process `MigrationEvidenceRef` and forward-revision compensation fixture; after accepted slices A and B, reverting A produces a new immutable generation preserving B. Rollback: issue only the plan's new compensation operation from the current composition without changing business data or selecting a historical generation that removes later slices. Completion evidence: authority advances only to `legacy_forwarding` or `target_authoritative` after `verified` observes the exact target selector, deployment unit and process fence. [validates:layout.package] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 4.3 Repeat Tasks 4.1-4.2 only through independently strict-approved owner children. Objective: incrementally populate `src/trade` without a bulk rewrite or umbrella approval. Inputs: remaining implemented Context/Platform/Process/Interface owners. Outputs: one concrete child and reviewable PR per bounded slice. Affected contracts: only that frozen slice. Validation: each child reruns package/architecture/owner/compatibility/performance/recovery/rollback evidence and six-role design and implementation review. Rollback: per-slice forward-revision selector operation and commit revert. Completion evidence: no batch is justified by a directory name, no manifest is selected after approval and no future unimplemented child is required for rollback. [validates:layout.package] [validates:layout.control] [validates:layout.governance] [validation:review]

## 5. SDK, Notebook and Root Tools

- [ ] 5.1 Create and implement `sdk-notebook-layout-compatibility` only after the installed SDK exists. Objective: make notebook analysis reproducible outside the checkout without mixing a moving generation. Inputs: approved `cli-http-sdk-compatibility` SDK, immutable temporary fixtures, current paired notebook and semantic identity baseline. Outputs: strict-approved child, paired notebook under `examples/notebooks`, public SDK imports, one-generation `SnapshotHandle` and no repository discovery. Affected contracts: SDK/notebook snapshot, lineage and error semantics. Validation: child six-role/strict approval; execute from a clean working directory with an explicitly injected temporary root and no `sys.path` mutation/network/direct parquet/repository/adapter import; unset or outside-root DB/parquet/socket/provider access fails before open; a release-race fixture changes formal/current between context/bars/findings/research calls and proves one pinned snapshot/run/release/artifact identity or explicit stale/conflict; semantic assertions compare snapshot ID, lineage, hashes, knowledge mode/cut, revision policy, quality/lifecycle and research metadata in addition to shape; verify cleared outputs/pairing. Rollback: retain the current notebook path and installed SDK; no data changes. Completion evidence: approved child digest, clean-environment run, race result, semantic parity and prohibited-access guard pass. [validates:layout.governance] [validates:layout.package] [validation:test]

- [ ] 5.2 Classify each root script without bulk movement. Objective: separate public CLI, Platform backup, owner migration, deployment and historical responsibilities. Inputs: current script source and owner child contracts. Outputs: per-script disposition and, only when ready, narrow adapter/tool moves. Affected contracts: backup CLI/import and migration/deployment commands. Validation: wheel member guard, existing CLI snapshots, no migration import as library, temporary-root dry-run/backup/rollback evidence for any owner migration; `scripts/migrate_paths.sh` is never run during package validation. Rollback: retain old script and command path. Completion evidence: every moved script has an owner and every blocked/historical script remains intact. [validates:layout.governance] [validation:test]

- [ ] 5.3 Extract governed historical decisions before any `_bmad-output` retirement. Objective: remove obsolete planning output only when no valid rationale or reference is lost. Inputs: six tracked files, Git/source/docs link inventory and retention policy. Outputs: provenance ledger and governed doc/OpenSpec references; deletion candidates for cleanup child only. Affected contracts: developer documentation provenance. Validation: link/reference scan, decision comparison against actual code and parent OpenSpec, retention approval and Git restoration drill. Rollback: restore the individual tracked file. Completion evidence: no `_bmad-output` deletion occurs in this child implementation unless the cleanup child separately approves it. [validates:layout.governance] [validation:review]

- [ ] 5.4 Create and strictly approve `optional-interface-extension-boundary` before adding plugin, MCP or remote-worker dependencies. Objective: keep future transports and discovery at Interface/Platform adapters without leaking into domain, use-case or compatibility modules. Inputs: actual proposed integration consumers and dependency graph. Outputs: child freezing entry-point group/name/version, MCP transport owner, remote-worker DTO/protocol negotiation and idempotency identity, capability states and canonical registration order. Affected contracts: optional extension discovery and remote command compatibility only. Validation: child six-role/strict approval; architecture guard rejects lower-layer/forwarder imports; clean-wheel tests cover optional dependencies absent, incompatible and duplicate; import is side-effect-free and no handler is selected by filesystem/discovery order. Rollback: omit or disable the optional adapter; existing CLI/HTTP/SDK/runtime remains unchanged. Completion evidence: approved child digest, exact dependency allowlist and deterministic capability/registration matrix. [validates:layout.package] [validates:layout.governance] [validation:review]

## 6. ASGI and Web Layout

- [ ] 6.1 Create and implement `http-bootstrap-layout-compatibility` while retaining `trade_web`. Objective: relocate interfaces only after Context queries, Process handles and one Bootstrap owner exist, with the exact ASGI/child topology digest-bound before code. Inputs: interface baseline, Platform/Bootstrap and `cli-http-sdk-compatibility` implementation, current runtime lifecycle/process tests and selector-control prerequisite. Outputs: strict-approved child, target factory/handlers plus thin `trade_web:create_app` and child-module bridges; the external ASGI string remains stable and selection is by the shared immutable fenced Python deployment generation. The child freezes supervisor/worker/command-child parentage, process-group or cgroup ownership, fence propagation, signal routing, PID enumeration and `startup_state` mapping. Affected contracts: ASGI imports, routes, OpenAPI, payload/error/SSE/capability, `started_degraded` and shutdown semantics. Validation: child six-role/strict approval; source/editable/wheel factory/reload/child smoke; crash before spawn, spawn-success/response-loss adoption, parent/supervisor death before/after worker spawn, TERM-ignoring grandchild, KILL escalation, reload teardown, cleanup-failure classification and zero residual PIDs; complete route/OpenAPI baseline including known failure status; payload/error/SSE/capability semantic goldens; automation failure stays running with `startup_state=started_degraded`; runtime resource/command/shutdown suites; architecture/DB-owner guards; revision-fenced selector/start-intent recovery and non-LIFO compensation preserving later owner slices. Rollback: create a new Python deployment selector revision for a verified compensation generation while keeping `trade_web:create_app`; keep target files unused. Completion evidence: one resource/shutdown owner, exact observed selector/deployment-unit/process fence, startup/shutdown receipts and zero interface regression. [validates:layout.web] [validates:layout.package] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 6.2 Move the Vite workspace only through `web-workspace-physical-layout`; activate its default only through `web-workspace-default-activation`. Objective: establish the final frontend root without changing product behavior or coupling physical movement to deployment selection. Inputs: current lockfile/package scripts/Vite/TS/ESLint/Prettier/Vitest/Playwright configuration, Web asset/performance baseline and selector-control prerequisite. Outputs: two strict-approved children, target workspace, unique-root tooling/cache locator and independent legacy-root rollback selector. Affected contracts: npm commands, dev proxy, manifest/build output, static resources and SPA routes. Validation: each child passes six-role/strict approval; install reproducibly; lint/format/typecheck/unit/build/bundle; manifest/member/digest comparison; exactly one root scanned; cache key/invalidation/LRU; no-change/cold/incremental budgets; `/api` and `/predict` proxy plus FastAPI static/SPA smoke; desktop/mobile overlap and referenced resources where required. Rollback: physical child retains the old workspace; activation child creates a forward selector revision to `trade_web/frontend` while retaining explicit `--web-dist`/`TRADE_WEB_DIST`. Completion evidence: backend generation unchanged, both roots independently selectable and physical/default commits/PRs mapped to separate rollback commands. [validates:layout.web] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 6.3 Commit and review ASGI, frontend physical layout and frontend default activation independently. Objective: avoid coupling server, source topology and asset-selection rollback. Inputs: Tasks 6.1-6.2 as separately approved/passing children. Outputs: separate child PRs and commits. Affected contracts: server, physical frontend or default selector only per unit. Validation: repeat each unit's focused performance/process/contract gates, implementation six-role review, current strict child digest and `git diff --check`; review evidence records findings and selector generation/revision for each independent rollback. Rollback: revert or forward-select only the failed child generation. Completion evidence: child/commit/PR mapping names independent rollback operation IDs and commands. [validates:layout.web] [validates:layout.control] [validates:layout.governance] [validation:review]

## 7. Test Layout and Retirement Handoff

- [ ] 7.1 Reclassify tests only through bounded `tool-example-test-layout-<scope>` children after corresponding production paths stabilize. Objective: reach unit/integration/contract/architecture/golden/e2e/fixtures structure without silent coverage loss or post-approval topology selection. Inputs: current pytest/Vitest/Playwright/ctest inventories and stable production authority. Outputs: strict-approved concrete child, frozen old/new test-node ledger and category moves. Affected contracts: test discovery and CI commands. Validation: child six-role/strict approval; before/after collection count and exact mapping, marker/fixture resolution, focused results, no duplicate/deselected tests, performance budget and component runner ownership. Rollback: restore that child's old paths/config without changing production code. Completion evidence: approved digest and every frozen old node maps exactly once. [validates:layout.governance] [validation:test]

- [ ] 7.2 Create `layout-bridge-observability` before dynamic bridge-use coverage. Objective: ground truthful retirement evidence in actual deployment modules without import/request-path I/O. Inputs: bridge registry, supported-consumer/deployment-population sets and actual candidate producer/exporter/collector/retention/query modules. Outputs: strict-approved child naming those modules and, only then, bounded facade observation, collector-health evidence and `BridgeUseCoverageRef`; until implementation coverage remains `unavailable`. Affected contracts: compatibility observability only. Validation: child six-role/strict approval; imports perform zero external/durable I/O; observations contain no user data/arguments/credentials/paths/stacks; closed classes/outcomes, 512 bridges/eight generations/20,000 series/1,000 increments-per-second, one-minute aggregation and 90-day retention bounds; overflow/drop makes coverage partial; complete/partial/unavailable/stale, population mismatch, collector failure, missing interval and last-use are exact; request latency has no blocking exporter. Rollback: disable the optional facade adapter, classify coverage unavailable and retain every bridge. Completion evidence: concrete owners plus population digest, interval, freshness, cardinality/overflow and collector health are queryable through layout-status. [validates:layout.package] [validates:layout.web] [validates:layout.governance] [validation:test]

- [ ] 7.3 Produce the bridge-retirement handoff for `tests-and-legacy-cleanup`. Objective: prevent premature removal after the layout appears complete. Inputs: complete `BridgeUseCoverageRef`, fresh `ConsumerInventoryRef`, release/deprecation evidence, clean installs and rollback drills. Outputs: only individually eligible bridge/script/notebook/history/test cleanup candidates. Affected contracts: legacy imports, ASGI/root paths, tools and docs. Validation: minimum 30-day window, zero observed supported use for the full window, complete deployment-population coverage with no missing/stale interval, clean source/editable/wheel run without candidate and successful restoration. Rollback: retain or restore the bridge. Completion evidence: non-eligible paths remain explicitly owned and tracked. [validates:layout.package] [validates:layout.web] [validates:layout.governance] [validation:review]

## 8. Final Validation and Delivery

- [ ] 8.1 Run the complete applicable implementation validation matrix. Objective: prove the delivered slices without real-data access or hidden checkout dependencies. Inputs: all implemented units. Outputs: package, control-store, architecture, CLI, HTTP/Web, SDK/notebook, native and test evidence. Affected contracts: all implemented child capabilities. Validation: focused pytest; existing CLI/import/Web/runtime/Observatory suites; source/editable/wheel smoke; selector/recovery/process-fence fixtures when applicable; compileall; frontend checks for Web changes; CMake/ctest/differential tests for native changes; `./trade dev check --show-plan`, `./trade dev check`, strict design check and `git diff --check`. Rollback: do not commit or activate a failing unit. Completion evidence: all required commands pass or an unavailable optional slice remains unactivated with an explicit blocker. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.control] [validates:layout.governance] [validation:test]

- [ ] 8.2 Run the six-role implementation review in a separate review worktree and resolve every P0. Objective: audit the actual diff for duplicate authority, selector/recovery failure, shutdown/process leakage, capacity, contract, temporal/data-access and future plugin/worker integration risk. Inputs: frozen implementation commits and validation reports. Outputs: six source-citing reports and consensus. Affected contracts: implemented package/layout slices. Validation: all roles complete, every P0 resolved, material P1 assigned, design artifacts/digest refreshed and reapproved if implementation changed assumptions. Rollback: correct/revert affected slice and repeat review. Completion evidence: consensus and zero P0. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.control] [validates:layout.governance] [validation:review]

- [ ] 8.3 Stage only intentional files, commit each unit, push and open PR(s). Objective: deliver independently reversible implementation. Inputs: passing tests/review and clean worktree audit. Outputs: scoped commits, remote branches and PRs against `master`. Affected contracts: only each committed slice. Validation: `git status -sb`, staged diff and artifact scan show no real control state, data/cache/wheel/venv/frontend build/local config; commit body records validation, compatibility, rollback and exact co-author trailer; push after 3-5 commits and project-local official GitHub CLI creates PR. Rollback: close/revert the isolated PR or issue the reviewed prior-generation selector operation; no business-data restoration. Completion evidence: commit SHAs, selector refs when applicable and PR URLs. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.control] [validates:layout.governance] [validation:review]
