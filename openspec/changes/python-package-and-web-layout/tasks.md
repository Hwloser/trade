## 1. Governed Design Approval

- [ ] 1.1 Validate the source-backed package/layout design. Objective: make the child internally complete and traceable before implementation. Inputs: parent architecture, current `pyproject.toml`, root facade, package/import scan, ASGI/runtime, Vite, CMake, notebook/script/test inventory, child proposal/design/specs and policy v1. Outputs: governed artifacts and zero deterministic findings. Affected contracts: distribution/import, CLI, ASGI/HTTP/Web, SDK/notebook, native and developer/test paths. Validation: `openspec validate python-package-and-web-layout --strict --no-interactive`, `./trade dev design-check python-package-and-web-layout` and `git diff --check` return zero; every requirement is mapped to an obligation and test/review task. Rollback: revise OpenSpec only; no runtime state or source path changed. Completion evidence: command outputs, selected profiles and artifact digest are recorded. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.governance] [validation:test]

- [ ] 1.2 Run the required six-role review in a dedicated review worktree. Objective: challenge package authority, rollback, wheel/reload/process behavior, lifecycle ownership, route/assets, native isolation, notebook/scripts, capacity and future integration. Inputs: frozen artifact generation plus actual code references. Outputs: six file/line-citing judge reports and one consensus with P0/P1/P2 dispositions. Affected contracts: all child capabilities. Validation: reliability, performance, architecture, data-quality, observability and news/future judges complete; all P0 and implementation-blocking P1 findings are resolved or assigned to a named prerequisite child. Rollback: edit design, invalidate the digest and repeat affected review. Completion evidence: consensus report and finding evidence map. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.governance] [validation:review]

- [ ] 1.3 Record digest-bound review evidence and obtain current strict approval. Objective: bind implementation to the reviewed package/layout generation. Inputs: unchanged governed artifacts, current commit/date and resolved consensus. Outputs: approved `design-review.toml`. Affected contracts: all child requirements. Validation: `./trade dev design-check python-package-and-web-layout --strict` exits zero. Rollback: do not edit production/config/test code; correct artifacts and repeat review. Completion evidence: current policy/artifact digests, full commit, current date, all six approved roles and zero unresolved P0. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.governance] [validation:review]

## 2. Package Authority Foundation

- [ ] 2.1 Reconcile prerequisites and create the implementation worktree. Objective: start code only where the strict design and prior semantic children permit it. Inputs: strict child approval, merged architecture guard, current `kernel-and-public-contracts` package proof, owner/interface/Bootstrap child statuses and refreshed `git status -sb`. Outputs: dedicated implementation worktree/branch and a slice ledger marking eligible and blocked work. Affected contracts: package discovery and migration sequencing. Validation: no production movement is selected without an implemented owner; unrelated dirty/untracked files are absent from the worktree; strict digest still matches. Rollback: remove the unused worktree/branch. Completion evidence: worktree path, branch, prerequisite SHAs and selected first slice. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.2 Prove additive dual-root package discovery in source, editable and clean-wheel environments. Objective: install `trade` from `src/trade` while preserving distribution `trade-py`, legacy `trade_py`, root `./trade` and `trade-py` console behavior. Inputs: approved packaging ADR and current build backend. Outputs: smallest explicit package configuration, wheel-member allowlist and temporary-environment smoke harness. Affected contracts: distribution, imports and console. Validation: build one wheel; install outside the repository; record digest/member/origin/version/entrypoints; import approved legacy/target modules with optional native absent; run root/console help parity; fail if checkout path appears. Rollback: revert package discovery and harness; legacy distribution remains. Completion evidence: source/editable/wheel matrix passes and no tests/data/notebooks/frontend/build/vendor/history paths are installed. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.3 Implement a deterministic module-authority manifest and architecture guard. Objective: prevent duplicate implementation, reverse target-to-legacy imports and unclassified moves. Inputs: refreshed consumer inventory and authority record schema. Outputs: bounded source-only manifest validator, migration-state validation and focused fixtures. Affected contracts: module authority and compatibility direction. Validation: tests cover import order/concurrency, duplicate registry/handler/resource, broad `sys.modules` alias, target `__path__` extension, reverse dependency, stale consumer ref, scanner/rules/tree mismatch, incomplete/tool-failed inventory, missing owner, over-50-module/500-consumer refusal, 24-hour age boundary and deterministic ordering; validator reads no real data. Rollback: remove unused guard/manifest; no module authority changes. Completion evidence: architecture check fails each invalid fixture and passes current inventory without authorizing a business move. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.4 Implement typed evidence/status and read-only layout diagnostics. Objective: make activation and rollback inspectable without conflating lifecycle, execution, failure or rollback state. Inputs: `ConsumerInventoryRef`, `LayoutActivationPlanV1`, `MigrationEvidenceRef`, closed status products and current `./trade dev` routing. Outputs: bounded source/evidence parsers, immutable evidence validation and additive `./trade dev layout-status [--json]`; no mutation command. Affected contracts: developer diagnostic only; existing user CLI remains unchanged. Validation: fixtures cover every allowed/forbidden state product, digest/selector/tree/scanner/rules tamper, passed activation, timeout with successful rollback, tool failure, partial evidence, malformed report and exact exits 0/1/2; command performs zero provider/DB/parquet/build/activation/repair calls. Rollback: remove the unused diagnostic and evidence modules; current runtime remains authoritative. Completion evidence: operator output names active/prior generation, evidence ref, failure and action without secrets or raw payloads. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 2.5 Run focused package/authority/evidence checks and commit the foundation unit. Objective: establish reversible migration mechanics before moving any behavior. Inputs: Tasks 2.2-2.4 and the implemented kernel package proof. Outputs: one or more validated commits that do not duplicate prerequisite package code. Affected contracts: build/install/import/authority/evidence developer contract. Validation: focused pytest, source/editable/wheel smoke, layout-status goldens, `python -m compileall src/trade trade_py tests`, architecture guard, `./trade dev check --show-plan`, `./trade dev check` and `git diff --check`; inspect generated files and stage no wheel/cache/venv artifacts. Rollback: revert the new authority/evidence commits while retaining the prerequisite package generation. Completion evidence: exact commands/outcomes, prerequisite SHA mapping and no production runtime behavior change. [validates:layout.package] [validates:layout.governance] [validation:test]

## 3. Native Boundary

- [ ] 3.1 Reconcile native binding source and build identity before editing CMake. Objective: prove what code defines the extension instead of assuming the referenced paths exist. Inputs: current CMake target, Git inventory, generated-source policy and supported build presets. Outputs: binding source/initializer/output/wheel inventory or an explicit blocker. Affected contracts: optional native module identity. Validation: source paths are tracked or reproducibly generated by a reviewed command; initializer and expected output can be inspected; no network fetch is required by the validation path without approval. Rollback: no edit when proof fails. Completion evidence: exact source and build identity or the native slice remains blocked. [validates:layout.native] [validation:test]

- [ ] 3.2 Rename and isolate the optional extension as `_trade_native` behind owner adapters. Objective: remove the `trade_py` namespace collision without changing algorithms. Inputs: Task 3.1 proof and implemented owner Port(s). Outputs: aligned CMake/initializer/output name, explicit capability probe and adapter-only imports. Affected contracts: optional native import and owner Port. Validation: clean build/install with native absent/present, architecture guard, no implicit import from `trade`/`trade_py`, CMake build/ctest and owner C++/Python differential fixtures; unsupported state is explicit. Rollback: disable the target native adapter and restore the prior optional build target without moving business code. Completion evidence: `_trade_native.__spec__` and artifact identity match; zero direct domain/use-case imports. [validates:layout.native] [validates:layout.package] [validation:test]

- [ ] 3.3 Commit the independently reversible native unit. Objective: keep native naming separate from Python owner moves. Inputs: passing Task 3.2. Outputs: one validated commit. Affected contracts: native capability only. Validation: repeat clean build/install, differential tests, relevant `./trade test <preset>`, Python import smoke and `git diff --check`. Rollback: revert the commit and leave native unavailable/legacy without changing Python semantics. Completion evidence: commands and supported/unsupported platform state. [validates:layout.native] [validation:test]

## 4. Owner-Ready Python Migration Slices

- [ ] 4.1 Select one bounded owner-ready module slice. Objective: move semantic ownership, not a cosmetic directory. Inputs: implemented owner child, complete fresh `ConsumerInventoryRef`, public contract/golden baseline and no more than 50 production modules/500 consumers. Outputs: slice manifest with old/target modules, exports, dynamic/patch consumers, state/DB/artifact/side effects and immutable `LayoutActivationPlanV1` naming exact current/target/rollback generation selectors and commands. Affected contracts: selected owner and legacy imports. Validation: every file has one owner/cell or is split-required; mixed files stay legacy; parent dependency/DB-owner rules pass; stale expected-generation compare-and-set refuses activation. Rollback: discard the slice before edits. Completion evidence: approved slice manifest, activation-plan digest and prerequisite SHAs. [validates:layout.package] [validates:layout.governance] [validation:review]

- [ ] 4.2 Move the selected implementation and add explicit legacy forwarders. Objective: establish one target authority while retaining supported imports. Inputs: Task 4.1 manifest and activation plan. Outputs: target owner modules, target-internal imports and narrow old-path forwarders with no side effects. Affected contracts: owner public imports and existing consumers. Validation: old/new import-order/concurrency and object-identity tests, owner unit/integration/contract tests, no reverse legacy import, no duplicate registry/worker/repository/provider call, source/editable/wheel smoke, immutable `MigrationEvidenceRef` and rollback generation fixture. Rollback: execute only the plan's prior-generation selector without changing durable data. Completion evidence: authority manifest advances only to `legacy_forwarding` or `target_authoritative` after one passed evidence manifest observes the target selector. [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 4.3 Repeat Tasks 4.1-4.2 only for independently approved owner slices. Objective: incrementally populate `src/trade` without a bulk rewrite. Inputs: remaining implemented Context/Platform/Process/Interface owners. Outputs: separate reviewable commits/PRs or child changes. Affected contracts: only the selected slice. Validation: each slice reruns package/architecture/owner/compatibility/rollback evidence and implementation six-role review where medium/large. Rollback: per-slice generation selector and commit revert. Completion evidence: no batch is justified by a directory name and no future unimplemented child is required for rollback. [validates:layout.package] [validates:layout.governance] [validation:review]

## 5. SDK, Notebook and Root Tools

- [ ] 5.1 Migrate the BTC notebook only after the installed SDK exists. Objective: make notebook analysis reproducible outside the checkout. Inputs: approved `cli-http-sdk-compatibility` SDK, immutable temporary fixtures and current paired notebook. Outputs: paired notebook under `examples/notebooks`, public SDK imports and no repository discovery. Affected contracts: SDK/notebook snapshot and error semantics. Validation: execute from a clean working directory with no `sys.path` mutation/network/direct parquet/repository/adapter import; compare immutable snapshot, quality/lifecycle, bars/findings and research metadata; verify cleared outputs/pairing. Rollback: retain the current notebook path and installed SDK; no data changes. Completion evidence: clean-environment run and prohibited-access guard pass. [validates:layout.governance] [validates:layout.package] [validation:test]

- [ ] 5.2 Classify each root script without bulk movement. Objective: separate public CLI, Platform backup, owner migration, deployment and historical responsibilities. Inputs: current script source and owner child contracts. Outputs: per-script disposition and, only when ready, narrow adapter/tool moves. Affected contracts: backup CLI/import and migration/deployment commands. Validation: wheel member guard, existing CLI snapshots, no migration import as library, temporary-root dry-run/backup/rollback evidence for any owner migration; `scripts/migrate_paths.sh` is never run during package validation. Rollback: retain old script and command path. Completion evidence: every moved script has an owner and every blocked/historical script remains intact. [validates:layout.governance] [validation:test]

- [ ] 5.3 Extract governed historical decisions before any `_bmad-output` retirement. Objective: remove obsolete planning output only when no valid rationale or reference is lost. Inputs: six tracked files, Git/source/docs link inventory and retention policy. Outputs: provenance ledger and governed doc/OpenSpec references; deletion candidates for cleanup child only. Affected contracts: developer documentation provenance. Validation: link/reference scan, decision comparison against actual code and parent OpenSpec, retention approval and Git restoration drill. Rollback: restore the individual tracked file. Completion evidence: no `_bmad-output` deletion occurs in this child implementation unless the cleanup child separately approves it. [validates:layout.governance] [validation:review]

## 6. ASGI and Web Layout

- [ ] 6.1 Migrate an owner-ready ASGI/BFF/Bootstrap slice while retaining `trade_web` compatibility. Objective: relocate interfaces only after Context queries, Process handles and one Bootstrap owner exist. Inputs: interface baseline, Platform/Bootstrap and `cli-http-sdk-compatibility` implementation, current runtime lifecycle tests. Outputs: target factory/handlers plus thin `trade_web:create_app` and child-module bridges; the external ASGI string remains stable and selection is by immutable Python deployment generation. Affected contracts: ASGI imports, routes, OpenAPI, payload/error/SSE/capability and shutdown. Validation: source/editable/wheel factory/reload/child process smoke; complete route/OpenAPI baseline including known failure status; payload/error/SSE/capability goldens; runtime resource/command/shutdown suites; architecture/DB-owner guards; compare-and-set selector and rollback evidence. Rollback: select the retained prior Python deployment generation while keeping `trade_web:create_app`; keep target unused. Completion evidence: one resource/shutdown owner, exact observed evidence generation and zero interface regression. [validates:layout.web] [validates:layout.package] [validates:layout.governance] [validation:test]

- [ ] 6.2 Move the Vite workspace to `web/` as an independent slice. Objective: establish the final frontend root without changing product behavior. Inputs: current lockfile/package scripts/Vite/TS/ESLint/Prettier/Vitest/Playwright configuration and Web asset baseline. Outputs: target workspace, updated tooling/default locator and legacy-root rollback selector. Affected contracts: npm commands, dev proxy, manifest/build output, static resources and SPA routes. Validation: install reproducibly, lint/format/typecheck/unit/build/bundle checks, manifest/member/digest comparison, smoke the `/api` and `/predict` development proxies, then smoke FastAPI static-resource and SPA routes; check desktop/mobile overlap and referenced resources where product UI requires them. Rollback: point tooling/default dist to `trade_web/frontend` and retain explicit `--web-dist`/`TRADE_WEB_DIST`. Completion evidence: backend generation is unchanged and both roots are independently selectable during the window. [validates:layout.web] [validates:layout.governance] [validation:test]

- [ ] 6.3 Commit and review ASGI and frontend units independently. Objective: avoid coupling server rollback to frontend rollback. Inputs: Tasks 6.1-6.2 as separately passing units. Outputs: separate commits and, if blast radius requires, separate implementation child PRs. Affected contracts: server or frontend only per unit. Validation: repeat each unit's focused gates, implementation six-role review, strict design digest check and `git diff --check`; review evidence records findings and consensus for each independent rollback generation. Rollback: revert/select only the failed generation. Completion evidence: commit/PR mapping names independent rollback commands. [validates:layout.web] [validates:layout.governance] [validation:review]

## 7. Test Layout and Retirement Handoff

- [ ] 7.1 Reclassify tests in bounded component-owned slices after corresponding production paths stabilize. Objective: reach unit/integration/contract/architecture/golden/e2e/fixtures structure without silent coverage loss. Inputs: current pytest/Vitest/Playwright/ctest inventories and stable production authority. Outputs: old/new test-node ledger and category moves. Affected contracts: test discovery and CI commands. Validation: before/after collection count and exact mapping, marker/fixture resolution, focused results, no duplicate/deselected tests and component runner ownership. Rollback: restore old paths/config without changing production code. Completion evidence: each old node maps exactly once. [validates:layout.governance] [validation:test]

- [ ] 7.2 Implement bounded bridge-use coverage at owner facades and deployment observability. Objective: own the evidence needed for truthful retirement without import-time I/O. Inputs: bridge registry, supported-consumer and deployment-population sets, existing owner facade telemetry ports and retention service. Outputs: bounded facade observation, collector-health evidence and `BridgeUseCoverageRef`; unsupported/unobservable consumers remain explicit inventory entries. Affected contracts: compatibility observability only. Validation: imports perform zero external/durable I/O; observations contain no user data/arguments/credentials/paths/stacks; complete/partial/unavailable/stale coverage, population mismatch, collector failure, missing interval and last-use cases are exact; request latency has no blocking exporter call. Rollback: disable the optional facade adapter and classify coverage unavailable; retain every bridge. Completion evidence: coverage owner, population digest, interval, freshness and collector health are queryable through layout-status. [validates:layout.package] [validates:layout.web] [validates:layout.governance] [validation:test]

- [ ] 7.3 Produce the bridge-retirement handoff for `tests-and-legacy-cleanup`. Objective: prevent premature removal after the layout appears complete. Inputs: complete `BridgeUseCoverageRef`, fresh `ConsumerInventoryRef`, release/deprecation evidence, clean installs and rollback drills. Outputs: only individually eligible bridge/script/notebook/history/test cleanup candidates. Affected contracts: legacy imports, ASGI/root paths, tools and docs. Validation: minimum 30-day window, zero observed supported use for the full window, complete deployment-population coverage with no missing/stale interval, clean source/editable/wheel run without candidate and successful restoration. Rollback: retain or restore the bridge. Completion evidence: non-eligible paths remain explicitly owned and tracked. [validates:layout.package] [validates:layout.web] [validates:layout.governance] [validation:review]

## 8. Final Validation and Delivery

- [ ] 8.1 Run the complete applicable implementation validation matrix. Objective: prove the delivered slices without real-data access or hidden checkout dependencies. Inputs: all implemented units. Outputs: package, architecture, CLI, HTTP/Web, SDK/notebook, native and test evidence. Affected contracts: all implemented child capabilities. Validation: focused pytest; existing CLI/import/Web/runtime/Observatory suites; source/editable/wheel smoke; compileall; frontend checks for Web changes; CMake/ctest/differential tests for native changes; `./trade dev check --show-plan`, `./trade dev check`, strict design check and `git diff --check`. Rollback: do not commit or activate a failing unit. Completion evidence: all required commands pass or an unavailable optional slice remains unactivated with an explicit blocker. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.governance] [validation:test]

- [ ] 8.2 Run the six-role implementation review in a separate review worktree and resolve every P0. Objective: audit the actual diff for duplicate authority, shutdown/process leakage, capacity, contract, temporal/data-access and future plugin/worker integration risk. Inputs: frozen implementation commits and validation reports. Outputs: six source-citing reports and consensus. Affected contracts: implemented package/layout slices. Validation: all roles complete, every P0 resolved, material P1 assigned, design artifacts/digest refreshed and reapproved if implementation changed assumptions. Rollback: correct/revert affected slice and repeat review. Completion evidence: consensus and zero P0. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.governance] [validation:review]

- [ ] 8.3 Stage only intentional files, commit each unit, push and open PR(s). Objective: deliver independently reversible implementation. Inputs: passing tests/review and clean worktree audit. Outputs: scoped commits, remote branches and PRs against `master`. Affected contracts: only each committed slice. Validation: `git status -sb`, staged diff and artifact scan show no data/cache/wheel/venv/frontend build/local config; commit body records validation, compatibility, rollback and exact co-author trailer; push after 3-5 commits and project-local official GitHub CLI creates PR. Rollback: close/revert the isolated PR or select prior generation; no data restoration. Completion evidence: commit SHAs and PR URLs. [validates:layout.package] [validates:layout.web] [validates:layout.native] [validates:layout.governance] [validation:review]
