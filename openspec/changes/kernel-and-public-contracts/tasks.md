## 1. Governed Design Approval

- [x] 1.1 Run the diagnostic child design check and resolve deterministic findings. Objective: make the second architecture child complete, internally consistent and traceable before implementation. Inputs: parent `restructure-trade-architecture-v1`, current EventBus/data/Observatory/Web runtime/packaging audit, child proposal/design/specs and immutable policy v1. Outputs: governed artifacts with public-contract and concurrency profiles, four mapped obligations and no placeholder or unsupported claim. Affected contracts: all new Kernel, operation/control, immutable-reference and compatibility contracts. Validation: `./trade dev design-check kernel-and-public-contracts` and `openspec validate kernel-and-public-contracts --strict` return zero with no unresolved warning. Rollback: revise only this child OpenSpec; no source or runtime change exists. Completion evidence: native strict validation passed; the diagnostic design check selected `core`, `contract`, `concurrency` and prospective `storage` safety, returned zero findings, and bound the four obligations to their spec and task evidence. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

- [ ] 1.2 Run the required six-role architecture review in a dedicated review worktree and resolve every P0 and implementation-blocking P1. Objective: challenge Kernel admission, package sequencing, actor trust, fingerprint secrecy, status certainty, immutable-reference ownership, serialization bounds, codec admission, fenced takeover and non-returning worker/shutdown semantics. Inputs: frozen child artifact digest and cited current source. Outputs: six source-citing judge reports, one consensus, finding dispositions and digest-bound `design-review.toml`. Affected contracts: package/import, wire, actor, operation/process/error/control and legacy mapping contracts. Validation: reliability, performance, architecture, data-quality, observability and news/future judges approve; every P0 is resolved and accepted P1/P2 has a named owner/child. Rollback: update design artifacts, invalidate the prior digest and repeat the panel. Completion evidence: review record contains current commit/date, policy/artifact digests, all roles and finding evidence. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

- [ ] 1.3 Obtain current-date strict design approval before implementation. Objective: bind code work to the reviewed contract generation. Inputs: resolved review record and unchanged governed artifacts. Outputs: strict approval report. Affected contracts: all child requirements. Validation: `./trade dev design-check kernel-and-public-contracts --strict` returns zero on the implementation start date. Rollback: do not create or edit production/config/test code; correct artifacts and re-review. Completion evidence: strict report records current date, full reviewed commit, policy digest, artifact digest and six approved roles. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

## 2. Package and Kernel Foundation

- [ ] 2.1 Prove and implement additive dual-root package discovery before adding target modules. Objective: make the new `trade` contracts real installed APIs as a narrow prerequisite without starting the full package migration. Inputs: current `pyproject.toml`, distribution `trade-py`, root `./trade`, console entry, locked environment, clean temporary environment and target `src/trade`. Outputs: the smallest explicit package-dir/discovery configuration that retains all existing `trade_py`/console behavior and installs `trade`; if proof fails, no package/code change and a promoted package-transition ADR blocker. Affected contracts: distribution, source/editable/wheel imports and console entry. Validation: build exactly one wheel; require its normalized members to equal the source-derived `trade_py`/`scripts`/`src/trade` Python member set plus one expected `trade_py-*.dist-info` family containing exactly `METADATA`, `WHEEL`, `entry_points.txt`, `top_level.txt` and `RECORD`; require every packaged Python member to byte-equal source; cap each of the five dist-info members at 65,536 bytes and their aggregate at 262,144 uncompressed bytes; with source Python bytes `S`, cap total uncompressed members at `S + 262,144` and wheel file size at `S + 524,288`; reject extra metadata/license members, test/data/frontend/engine/generated/vendor/cache/undeclared package-data or unexpected top-level members; install that exact artifact outside repository cwd with `--no-deps` and network disabled; import `trade_py` and `trade`; invoke existing console/root help in the locked environment; prove no shim, generated mirror, symlink, import hook or `sys.path` mutation. Rollback: revert only additive discovery; legacy package and entrypoints remain. Completion evidence: one wheel SHA/exact inventory/source-byte parity/byte-budget totals and source/editable/offline-wheel commands pass without reinstalling the heavy dependency graph. [validates:contracts.kernel] [validates:contracts.compatibility] [validation:test]

- [ ] 2.2 Implement minimal Kernel modules and focused invariant tests. Objective: provide exactly `ids`, `time`, `digest`, `errors`, `result` and `envelope`, with no generic reference/policy module. Inputs: approved Kernel table, numeric limits and static architecture guard. Outputs: six small immutable standard-library modules with explicit exports and no initialization side effects. Affected contracts: `kernel-primitives`; immutable references remain owner-local policy only. Validation: focused pytest covers namespace/value/duration/error bounds, malformed/boundary values, wall-clock/monotonic separation, exact Result branch, transport-only creation time, owner codec descriptor refusal/collision invariants and import isolation; `UtcInstant` golden cases cover year `0001`/`9999`, seconds `00`/`59`, zero/one/999,999 microseconds and reject naive or non-zero-offset datetime construction, `+00:00`, omitted/variable/more-than-six fractional digits and leap seconds without rounding; architecture fixtures reject `refs.py`, owner vocabulary and forbidden dependencies. Rollback: remove the unused Kernel modules/tests after reverting any dependent target contract commit. Completion evidence: tests pass at each exact limit, fail one unit beyond it and prove the six-module allowlist. [validates:contracts.kernel] [validates:contracts.immutable-refs] [validation:test]

- [ ] 2.3 Extend the static architecture guard for the approved target graph and runtime-adoption fence. Objective: make the parent dependency direction and shutdown-hardening prerequisite executable before contracts spread. Inputs: current `trade_py/devtools/architecture_guard.py`, six-module allowlist, Platform/Processes graph, four exact compatibility-module allowlist entries and current EventBus/CLI/FastAPI/runtime ownership paths. Outputs: diagnostics and negative fixtures for Kernel allowlist, Platform not importing Processes, Processes importing only Kernel/Platform public contracts, target packages not importing legacy, compatibility imports remaining owner-specific, no aggregate mapper re-export, no legacy runtime importing target Platform/Processes contracts, and no non-test `trade_py`/`trade_web` production module importing any of the four leaf compatibility adapters before hardening. Affected contracts: all target package/import, compatibility and runtime-adoption boundaries. Validation: direct, relative, aliased, literal dynamic-import, computed `importlib.import_module`, variable `__import__`, module `__getattr__`, dynamic re-export, package re-export, Platform-to-Processes `ProcessId`, EventBus, CLI, FastAPI lifespan, `RuntimeCommandRunner` and `WebResourceContainer` fixtures fail closed with importing file/symbol/edge whether the bypass targets a target contract or a compatibility adapter; an unresolved dynamic target in protected production roots fails unless it is a member of an exact reviewed finite allowlist; the current CLI loader passes only for its enumerated `trade_py.cli` legacy domains; permitted Processes-to-Platform and each exact owner mapper dependency pass, with focused contract tests as the only positive adapter consumers. Rollback: revert only the new guard rules/fixtures; no runtime imports have changed. Completion evidence: focused architecture guard tests prove every declared positive and negative edge, including computed targets and dynamic re-exports, and the guard source is listed in the reviewed impact. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

## 3. Platform and Processes Public Contracts

- [ ] 3.1 Implement trusted actor and typed command/query admission contracts. Objective: prevent payload-supplied authority, arbitrary dictionary contracts and enumerable public identity before Platform ingress exists. Inputs: Kernel primitives, exact root/child/retry/redelivery/duplicate/replay causal matrix, `ActorProvenanceRef`, exact `IdempotencySubjectV1`, `ReplayContextV1`, `ReplayAdmissionV1`, origin/principal/assurance taxonomy, exact `FingerprintV1`, retained-key lookup and `key_set_generation` rules, and immutable owner-codec descriptors. Outputs: immutable `ActorContext`, `IdempotencySubjectV1`, `ReplayContextV1`, `ReplayAdmissionV1`, admission-local non-wire `CommandEnvelope` without a keyed command fingerprint, admission-local non-wire `QueryEnvelope`, descriptor value/validator, bounded collision/freeze/binary-search invariants and pure owner-payload/fingerprint interfaces; no whole-envelope codec and no production Bootstrap registry builder. Affected contracts: actor wire schemas, admission-local message composites, owner payload/projection schemas and public fingerprint schemas. Validation: focused tests cover verified CLI/HTTP/scheduler/event/system evidence, future MCP/GraphQL/TUI/remote-worker origin mapping, anonymous read policy, payload spoofing, wire-decoded assurance downgrade, provenance/scope/delegation bounds, and exact length-prefixed subject encoding for distinct principals with equal scopes, reauthentication, scope rotation, delegation ordering and anonymous callers. Replay fixtures separate immutable historical attribution from current replay initiator and policy evidence for expired/revoked provenance, command replay, event replay and replay-derived commands; copied historical authority is never executable. Exact ReplayAdmission goldens cover authorized existing-operation resolution with unchanged receipt plus bounded current-initiator audit, missing operation returning `REPLAY_OPERATION_NOT_FOUND` without creation, denied current initiator before existence disclosure, revoked historical actor, digest/identity mismatch and replay-derived new command with current subject. Root/child/retry/redelivery/new duplicate/durable replay/new replay-derived message identities and original receipt causal identity preservation are golden-tested. Fingerprint tests cover provenance/scopes and low-entropy raw key non-disclosure, command/idempotency domain separation, 256-bit secret-store boundary, one active plus at most three retained keys, exact zero/one/multi-match outcomes, same-key/same-command replay, same-key/different-command `IDEMPOTENCY_COMMAND_CONFLICT`, any multi-match `IDEMPOTENCY_CLAIM_CORRUPT` including duplicate claims for one operation, retirement, both interleavings of paused admission versus generation-CAS rotation, and a fake-clock rotation storm proving that a changed generation ends its attempt and one admission performs at most three claim attempts. Exact counters prove zero-match creation, one-match replay and one-match command conflict each derive exactly one command HMAC only after the stable result; conflict-to-audit-unavailable retains that one derivation, while multi-match, contention, corruption-to-audit-unavailable, contention-to-audit-unavailable and pre-admission rejection derive zero; all paths stay within twelve idempotency-candidate plus one command HMAC, thirteen total, three candidate queries, three claim transaction/CAS acquisitions and one refusal-audit transaction under one remaining deadline. Golden fixtures freeze `IDEMPOTENCY_KEYSET_CONTENTION` only after audit commit and `IDEMPOTENCY_AUDIT_UNAVAILABLE` when audit has no start budget or fails. Duplicate/over-capacity codec registry, no authorization meaning, canonical ref-only news/L2 refusal while bounded Capture ingress remains legal, forbidden framework payloads and whole admission-envelope serialization are also covered. Rollback: remove unused target contracts; no interface has adopted them and no production key exists. Completion evidence: every mutation with unknown/unverified execution authority is denied, direct historical replay is resolve-only and never creates a missing operation, historical replay attribution never revives authority, retries across rotation resolve only a command-equivalent original claim, conflicting commands and multiple claims fail closed, rotation races leave exactly one claim, sustained contention terminates without background continuation or unaudited specific refusal, cross-domain values differ and raw guesses cannot be checked against an unkeyed public hash. [validates:contracts.operation-control] [validates:contracts.kernel] [validation:test]

- [ ] 3.2 Implement operation, process, error and query-status contracts with closed transitions. Objective: make durable admission, observation and recovery facts explicit without a repository or dual writer. Inputs: exact operation/process transition relations, Platform-sole command-admission claim and OperationReceipt ownership, Processes-sole internal `ProcessStartKeyV1`/workflow/ProcessView ownership, idempotency conflict/error identity, QueryStatus observation/condition/error matrix, Platform `OpaqueId` process links, `OperationReceipt`, `ProcessView`, history-window, `RecoveryActionDescriptor` and `ErrorEnvelope` schemas. Outputs: immutable DTOs, transition validators, deterministic latest-50 history metadata and exact codecs; no repository. Affected contracts: operation/process/error/query public schemas. Validation: exhaustive product tables cover every allowed/forbidden transition, terminal timestamps, command-equivalent duplicate receipt identity and preservation of the original admitted request/correlation/optional-causation identities, the exact reason/category/observation/retry/retry-after/request-message/correlation/direct-causation/link/recovery products for `IDEMPOTENCY_COMMAND_CONFLICT`, `IDEMPOTENCY_CLAIM_CORRUPT`, `IDEMPOTENCY_KEYSET_CONTENTION` and `IDEMPOTENCY_AUDIT_UNAVAILABLE`, no receipt/new operation ID on any refusal, absent receipt on persistence failure, one immutable 2,048-byte allowlisted terminal refusal audit or truthful audit-unavailable fallback linked to the current request message and optional direct cause, audit visibility only through the authorized Platform operator query with no ErrorEnvelope/HTTP/SDK/cross-Context projection and no claim/operation/dispatch write, terminal-only closed low-cardinality metric labels, one counter, one request-linked redacted bounded event and at most one corruption integrity-alert attempt under the same deadline with no retry/background continuation, generation retry represented only by bounded attempt count, and an explicit adoption test that rejects telemetry wiring until `platform-persistence-events-and-bootstrap-foundation` owns a bounded operator health observation. Process fixtures freeze the exact process type/operation ID/workflow digest tuple, prove that Platform admission keys, HMACs and secret material never enter Processes, require Processes to atomically deduplicate `ProcessStartKeyV1` with inbox/initial view only in its own repository, forbid any idempotency fingerprint or workflow key in ProcessView, and prove no shared transaction spans the two owners. Tests also cover every observation/condition/error combination, ProcessView reason required/optional/forbidden rules, owner deadline terminal evidence versus caller observation timeout, exact empty/truncated history invariants, duplicate/decreasing owner sequence, all recovery-action distinctions and numeric/error bounds. Architecture fixtures reject Platform importing Processes `ProcessId` or Processes constructing/updating OperationReceipt. Rollback: remove target DTOs after dependent tests; legacy status objects remain authoritative. Completion evidence: complete state-product tests and golden JSON fixtures pass with no generic `redrive`, false duplicate receipt, unsafe audit/metric/log field, public process idempotency leak, dual receipt writer, shared owner transaction or cross-owner type dependency; parent ownership-table clarification remains a strict prerequisite before any repository child starts. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.3 Implement truthful cancellation and shutdown control contracts and bounded fixtures. Objective: ensure a future control plane cannot report cancellation/completion early, lose initiator attribution, fabricate shutdown from a non-accepted control, admit stale writers or block after its declared deadline. Inputs: operation/opaque process links, trusted actor, error, owner/control/observation deadlines, owner instance/fence generation, exact `ControlReceipt` disposition/reason/error/link/claim product, exact accepted-control-to-shutdown linkage, exact shutdown state/stage/reason/error/residual/recovery product and current runtime shutdown audit. Outputs: immutable actor-bearing `ControlReceipt`/`ShutdownReceipt`, exact request/correlation/optional-causation linkage, accepted-control shutdown-link validator, control-claim/receipt/intent atomicity invariants, closed residual ownership taxonomy and a standard-library fake-clock test harness for shared-deadline stages. Affected contracts: cancellation, shutdown, fencing, audit attribution and finite observation. Validation: exhaustive state-product and golden codec tests cover every `accepted`, `already_terminal`, `denied`, `not_found`, `unavailable` and `deadline_exceeded` disposition with required/forbidden intent/outbox durability, terminal-target evidence, reason, safe error, retryability and links; every returned receipt is atomically claimed/persisted, accepted additionally commits intent/outbox, other dispositions commit neither, and receipt persistence failure returns `CONTROL_RECEIPT_UNAVAILABLE` with no receipt/intent/outbox. Crash fixtures immediately before and after commit prove exact recovery, duplicate retries resolve the same disposition, and application timeout after accepted appears only through finite observation. Root requests and linked shutdown receipts omit causation, child pairs preserve direct causation, transport redelivery reuses the request identity, a new duplicate uses its own request identity, and durable replay plus replay-derived control retain historical attribution separately from current authority. Linked shutdown receipts resolve an accepted shutdown control and preserve exact control ID, target links, request/correlation/optional-causation, unverified initiator, requested time, UTC deadline and finish ordering for the full retention period; all non-accepted dispositions and one-field mismatches fail. No-sleep fake-clock tests cover accepted-before-terminal cancellation, signal-not-terminal, denied/not-found/unavailable/deadline outcomes, caller observation timeout without owner mutation, residual worker requiring exit or durable fence before owner terminal deadline, concurrent callers, crash takeover/stale-writer rejection, persistent audit blockage, executor tail and exactly 1/10 owners in each residual category; one real child-process fixture uses a 2-second shared deadline, <=250 ms tolerance and 5-second suite ceiling. Rollback: remove unused contracts/harness; current RuntimeCommandRunner/WebResourceContainer behavior remains unchanged. Completion evidence: every fake/real path returns within its fixed budget with truthful request/actor/disposition/intent/stage/generation/residual state and no live non-daemon fixture process. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.4 Implement the bounded replay-audit contract and recovery fixtures. Objective: prevent direct replay from returning a historical receipt without durable current-initiator attribution or continuing after its declared budget. Inputs: `ReplayContextV1` current request/correlation/optional-causation tuple, exact `ReplayAdmissionV1` binding, finite Deadline, Platform owner namespace, required historical operation, internal domain-separated binding digest and ErrorEnvelope products. Outputs: immutable replay-audit DTO with `occurred_at: UtcInstant`, pure binding-digest function that excludes only the attempt deadline, owner-port contract for one audit-key lookup plus at most one operation/claim lookup and one transaction, and no production repository. Affected contracts: direct replay admission, audit identity, audit observation time, safe replay errors and finite recovery. Validation: golden/error/product tests cover root and child current replay causation, retry/redelivery tuple preservation, authorization before transaction/existence disclosure, exact `(owner_namespace, replay_request_message_id)` key, same-key/same-binding idempotent resolution, same-key/different historical digest/identity/operation/subject/actor/policy `REPLAY_ADMISSION_CONFLICT` before operation lookup, missing operation with no resolved audit, successful commit-before-unchanged-receipt, first-audit `occurred_at` sampled only after operation resolution and immediately before fact construction, and pre-commit versus post-commit/pre-response crashes. Single and persistent trusted-clock failure return exact `REPLAY_AUDIT_CLOCK_UNAVAILABLE` with `occurred_at` absent, no fallback/historical/monotonic time, no audit or receipt, and one clockless `clock_unavailable` health signal; no start budget/in-budget transaction or persistence failure returns `REPLAY_AUDIT_UNAVAILABLE` with ordinary required error occurrence time and distinct `transaction_unavailable` or `persistence_unavailable` health kind. Same-binding retry preserves the committed `occurred_at`, performs no second wall-clock sample and cannot reinterpret it as historical event, provider/source, publication, received, available, PIT, deadline, transaction-completion or response time. Fake counters prove one transaction, one audit-key lookup, at most one operation/claim lookup, zero persistence retries, zero background continuation and one bounded clockless health signal; a fresh attempt deadline changes neither binding digest nor durable identity and every retry reauthorizes. Error goldens preserve the current request/correlation/root-absent-or-child-direct causation, expose no receipt/operation/process link or binding digest, and distinguish missing/conflict/clock-unavailable/audit-unavailable. Rollback: remove the unused DTO/port/test doubles after dependent contract tests; no repository or caller has adopted them. Completion evidence: every authorized direct replay either returns one unchanged historical receipt only after one durable audit commit, returns one closed safe error within its original monotonic budget without fabricating time, or resolves the same post-crash audit with its original observation time and without a duplicate row. [validates:contracts.operation-control] [validation:test]

- [ ] 3.5 Separate local execution deadlines and authority from durable public evidence. Objective: prevent public codecs from serializing process-local monotonic clock state or reconstructing mutation authority from receipt bytes. Inputs: Kernel `Deadline`, canonical `UtcInstant`, `ActorContext.as_wire_attribution()`, existing OperationReceipt/ControlReceipt/ShutdownReceipt/ProcessView fields, and the exact v1 codec rules. Outputs: exact field matrix: `OperationReceipt` changes only `actor` to unverified attribution and never gains a deadline; `ControlReceipt` changes `initiator` and its existing `deadline` to `UtcInstant`; `ShutdownReceipt` changes `initiator` and its existing `deadline` to `UtcInstant`; `ProcessView` changes only its existing `deadline` to `UtcInstant` and never gains an actor field. Local command/control budgets retain `Deadline` outside durable DTOs. Affected contracts: Kernel time semantics, operation/process/control/shutdown wire schemas, replay and control admission authority. Validation: exact round-trip goldens prove the three declared public deadline fields encode only `YYYY-MM-DDTHH:MM:SS.ffffffZ` and that OperationReceipt has no deadline; negative fixtures reject `monotonic_expires_at`, floats, composite Deadline objects, receipt-finalization reserve fields, verified receipt actors and undeclared fields; decoding a receipt/view produces neither a local Deadline nor a mutation-capable ActorContext; admission tests separately retain verified current actors and finite monotonic budgets; linked control/shutdown receipts preserve identical UTC deadline evidence and unverified initiator attribution. Rollback: revert the unused DTO/codec adjustments together before any runtime or repository adopts the schemas; legacy runtime contracts remain unchanged. Completion evidence: owner schema codecs and type checks demonstrate that every declared public receipt/view field is deterministic and durable while every executable authority and monotonic wait budget is newly admitted, local and absent from historical wire bytes. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.6 Implement the authority-free durable envelope projection and bounded codec lookup invariants. Objective: make retry/replay identity deterministic without serializing verified authority, process-local deadlines or attempt state. Inputs: `EnvelopeMeta`, `OwnerCodecDescriptor`, one validated typed owner payload, domain `trade.durable-envelope-projection.v1`, unsigned four-byte big-endian framing, the 65,536-byte total envelope bound, closed nine-code `MessageContractFailureCodeV1` taxonomy and 4,096-descriptor registry bound. Outputs: `trade.platform.contracts.messages` owns immutable inert `DurableEnvelopeProjectionV1`, its sole binary encoder/decoder, bytes-consuming `derive_durable_envelope_digest_v1`, typed `OwnerPayloadCodec[T]`, non-wire `RegisteredOwnerCodecV1` descriptor/capability binding, typed request/readiness products, bounded operator cause/action values, whole-CommandEnvelope/QueryEnvelope serialization refusal, and pure frozen-registry capacity/binary-search invariants; Kernel remains metadata-only and no production Bootstrap builder is added. Affected contracts: Kernel envelope metadata, Platform message contracts and public serialization compatibility. Validation: golden bytes cover root/child causation presence, every projected metadata/descriptor component and binary payload framing; the validated payload bytes must exactly equal envelope canonical bytes and are reused without another owner-codec call; actor/deadline/attempt changes preserve bytes; distinct projected metadata, descriptor identity or payload yields distinct bytes or fails validation; the projection encoder runs once and replay digest equals `ContentDigest.from_bytes(exact_projection_bytes)`, every altered projected component yields a distinct digest, and actor/deadline remain irrelevant; a payload within its descriptor limit is still rejected when total framing exceeds 65,536 bytes; decode yields neither ActorContext nor Deadline; generic/JSON serialization rejects both admission composites and any second JSON representation of the projection. Descriptor resolution returns the exact registered descriptor plus executable codec binding at 0/1/4,096 entries; 4,097, duplicates, static binding mismatch and unresolved required historical codecs block readiness with their distinct exact codes; binary search over the immutable sorted tuple uses at most 13 key comparisons. Retirement fixtures use the bounded required-codec snapshot, retain descriptor plus executable codec through all Platform-owned projection/replay/receipt horizons, count retained entries toward capacity and reject early retirement without scanning dependent rows. Architecture fixtures reject ProcessRepository canonical payload/projection/codec fields and every Processes write through a Platform retention port. Request-scoped malformed/overflow/missing/mismatch/payload-invalid fixtures return exact safe code and bounded owner/schema/version/purpose identity with no payload, actor evidence, path, authority, deadline, operation or dispatch; Bootstrap failures expose only code/descriptor count/permitted manifest count, while an authorized operator observation proves the exact closed cause/action mapping without descriptor, key, payload, callback, exception or path leakage. Rollback: remove the unused Platform projection/lookup contract and tests before any transport or repository adopts it. Completion evidence: exact cross-install goldens prove durable bytes/digest are stable, singular and authority-free, and registry tests prove finite capacity, lifecycle, startup proof and diagnostics without linear resolution, durable-row scans or cross-owner writes. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.7 Freeze the exact public DTO field matrix and durable owner-deadline homes. Objective: prevent accidental OperationReceipt deadline expansion and silent process deadline mutation while preserving restart-safe evidence without pretending UTC can reconstruct elapsed time. Inputs: `ActorContext.as_wire_attribution()`, canonical `UtcInstant`, OperationReceipt, ControlReceipt, ShutdownReceipt, ProcessView, internal `OperationAdmissionDeadlineV1` and `ProcessAdmissionDeadlineV1` invariants. Outputs: exact matrix: OperationReceipt changes only `actor` to unverified attribution and has no deadline; ControlReceipt changes `initiator` plus `deadline: UtcInstant`; ShutdownReceipt changes `initiator` plus `deadline: UtcInstant`; ProcessView changes only `deadline: UtcInstant` and has no actor. ProcessView deadline becomes immutable for one process identity; no v1 in-place renewal exists. This child owns the internal non-re-exported operation/process deadline facts, owner-local runtime bindings and pure matching/resolution ports in their respective Platform/Processes contracts; future owning persistence children alone own repositories, unique constraints, atomic writes and retention. Affected contracts: operation/process/control/shutdown wire schemas and Platform/Processes persistence prerequisites. Validation: exact schema goldens reject added OperationReceipt deadline or ProcessView actor fields; negative transition fixtures reject ProcessView deadline changes; pure fixtures require immutable record deadlines to equal admitted local wall-clock expiry without resampling, reject mismatched source/caller-observation/envelope-creation/retry deadlines, cap live same-domain retry by the original monotonic binding, fail closed on restart/lost clock domain, reject expired owners and require a new causal identity for a new owner deadline. Fake-port matching tests run here; real atomicity/unique-constraint/retention fixtures remain mandatory in named persistence children. Rollback: revert unused DTO/validator/internal-contract changes; no repository or runtime has adopted them. Completion evidence: field-set, transition and fake-port tests prove each deadline has one source/owner, no attempt extends it, no wall-clock jump changes a retry result, and no durable wire value reconstructs a local budget. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.8 Require ShutdownReceipt to link one accepted shutdown control. Objective: prevent a shutdown lifecycle receipt from being fabricated from denied, unavailable or otherwise non-admitted control. Inputs: exact ControlReceipt disposition product and ShutdownReceipt linkage fields. Outputs: validator requiring `control_kind=shutdown`, `disposition=accepted`, exact control ID, operation/process targets, request/correlation/optional-causation identity, unverified initiator, requested time and UTC deadline, plus `shutdown.finished_at >= control.finished_at`; every corruption returns exact `SHUTDOWN_CONTROL_LINK_CORRUPT`. Affected contracts: control/shutdown audit linkage and receipt corruption detection. Validation: one accepted golden passes; every non-accepted disposition fails; one-at-a-time mismatches for every linked field and reversed finish ordering fail; root causation remains absent and child causation remains exact. Composed-query fixtures map corruption only to `QueryStatus(unavailable, condition=None)` with no receipt and the exact non-retryable unavailable ErrorEnvelope; it is never completed/failed/missing/healthy. One corruption identity admits one redacted observation/outbox, concurrent/repeated reads admit none, one fenced claim permits at most one external call, and ordinary completion terminalizes as delivered or delivery-failed; a post-claim crash terminalizes as delivery-outcome-unknown without an automatic second call. Only safe owner/control/request/fence/signal identity may appear. Rollback: remove the unused validator/composed products before runtime adoption. Completion evidence: exhaustive negative and crash fixtures prove only a durably accepted shutdown intent/outbox can anchor a ShutdownReceipt and every corrupt link, delivery failure or uncertain outcome is operator-visible without leaking evidence. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.9 Close codec capability, retention and readiness mechanics. Objective: make executable codec binding and historical retention finite, deterministic and implementable without durable-row scans, a global hot counter or cross-owner transactions. Inputs: `OwnerCodecDescriptor`, exact `OwnerCodecManifestV1` framing/digest, `OwnerPayloadCodec[T]`, `ValidatedOwnerPayloadV1[T]`, 16 Platform retention shards, authoritative `RequiredOwnerCodecManifestV1` snapshot, registry capacity and nine closed failure codes. Outputs: immutable manifest/validated-payload/retention-shard/snapshot-header/required-entry DTOs, exact descriptor-manifest-capability validator, one-encode typed path, one-decode/one-re-encode durable path, deterministic shard selector using the unsigned 32-bit big-endian value of the first four SHA-256 bytes modulo 16, bounded authoritative-snapshot validator and request/readiness/operator-health products; no production Bootstrap or persistence implementation. Affected contracts: owner codec registration, envelope projection, replay identity, Platform/Processes ownership and Bootstrap readiness. Validation: call-counter and object-identity fixtures prove fresh typed admission performs exactly one owner encode, envelope/projection reuse the returned immutable bytes, projection uses one bounded destination buffer, digest consumes that encoding and peak simultaneous payload-sized buffers do not exceed owner bytes plus one projection buffer; durable validation performs exactly one decode plus one encode and requires exact round trip. Manifest goldens bind every field and exact digest; static descriptor/capability/manifest mismatch returns `CODEC_REGISTRY_BINDING_MISMATCH` before ingress. Pure shard fixtures freeze exact length-framed identity bytes, full SHA-256 digest, first-four-byte unsigned big-endian integer and shard index for cross-language vectors including leading-zero and high-bit-set prefixes; they reject little-endian or host-native interpretation and prove deterministic distribution, one-row update scope, maximum three CAS attempts and no whole-snapshot copy. Snapshot fixtures cover absent pointer, incomplete/non-committed/mixed/stale owner-or-fence generation, count/source-revision/entry-digest mismatch, create/retire versus Bootstrap interleavings, retention-mutation gate closure, in-flight shard drain, exclusive snapshot lease and exact frozen 16-shard revision-vector comparison, 0/1/4,096/4,097 aggregate entries, duplicate/zero-count/missing/mismatched codec, finite deadline, at most 65,536 shard rows aggregated while ingress is closed, at most 13 registry comparisons per aggregate entry and a fail-fast sentinel proving no dependent-row enumeration. Exact `CODEC_REGISTRY_REQUIRED_CODEC_UNAVAILABLE` products expose only code/descriptor-count/manifest-count, while request-time non-required absence remains `OWNER_CODEC_NOT_FOUND`; authorized health fixtures cover every exact cause/action without leaking descriptor identity or implementation details. Processes boundary fixtures reject codec-dependent durable fields and Platform retention writes. Rollback: remove unused pure contracts/tests before any Bootstrap adoption; no persisted data exists. Completion evidence: deterministic tests prove codec identity, canonicality, language-independent shard selection, bounded memory/calls/write amplification, current snapshot proof, owner isolation and readiness taxonomy without scanning or leaking a descriptor. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.10 Close operation/process owner deadline behavior across retries and restarts. Objective: prevent UTC evidence or a lost monotonic clock domain from silently extending an existing owner. Inputs: `OperationAdmissionDeadlineV1`, `ProcessAdmissionDeadlineV1`, owner-local runtime bindings, immutable ProcessView deadline and four closed owner-deadline results. Outputs: internal non-re-exported operation/process deadline facts and pure matching/resolution ports plus owner-local runtime-binding validators; no repository, scheduler or runtime adoption. Affected contracts: operation retry, process retry/redelivery, ProcessView and recovery ownership. Validation: same-process fake-clock fixtures prove live matching bindings consume only original monotonic remaining time; exact expiry returns `OWNER_DEADLINE_EXPIRED`; monotonic read failure returns `OWNER_DEADLINE_CLOCK_UNAVAILABLE`; identity/UTC mismatch returns `OWNER_DEADLINE_EVIDENCE_MISMATCH`; restart/cross-process/lost binding returns non-retryable `OWNER_DEADLINE_CLOCK_DOMAIN_LOST` and executes zero work. Wall-clock rollback, forward jump and unavailability do not alter any outcome or create a budget. Process admission facts exactly match and are atomically co-owned with the future start/inbox/initial-view transaction; fake ports prove public views cannot authorize retries. Rollback: remove unused facts/ports/validators before a repository or runtime adopts them. Completion evidence: counters show zero execution after clock-domain loss and no path subtracts current UTC from an owner expiry; a new deadline requires a new causally linked owner identity. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.11 Close the shutdown-link corruption query and signal product. Objective: make every corrupt accepted-control linkage return one safe result and one idempotent operator-visible signal outcome without claiming impossible exactly-once external delivery. Inputs: link validator, `QueryStatus`, exact `ErrorEnvelope`, corruption identity, stable signal identity, bounded integrity observation and fenced claim. Outputs: pure `ShutdownLinkQueryResultV1`, `ShutdownLinkIntegrityObservationV1`, idempotency-key derivation and exact `pending -> claimed -> delivered|delivery_failed|delivery_outcome_unknown` transition validator; no production outbox, health query or runtime reroute. Affected contracts: shutdown query truthfulness, integrity observability and future Platform outbox adoption. Validation: all non-accepted and one-field mismatch fixtures return exactly unavailable/none plus non-retryable unavailable `SHUTDOWN_CONTROL_LINK_CORRUPT`, exact safe message/hint/current-query causality and only safely validated links; no completed/failed/missing/healthy receipt is synthesized. First observation samples trusted wall time once and reuses it; clock failure returns exact clockless `SHUTDOWN_LINK_INTEGRITY_CLOCK_UNAVAILABLE` with no audit/outbox/fallback time; observation transaction failure returns `SHUTDOWN_LINK_INTEGRITY_AUDIT_UNAVAILABLE` without claiming signal admission. Concurrent/repeated reads resolve one corruption identity/outbox. Crash fixtures cover before claim, after claim before send, and after send before terminal commit: only a live owner/fence claim may call externally; post-claim ambiguity becomes `delivery_outcome_unknown`; no automatic second call, retry or background continuation occurs. Ordinary result terminalizes at `delivered` or `delivery_failed`; every failure/unknown remains visible. Redaction fixtures reject credentials, raw actor evidence, payload, path and mismatched field values. Runtime adoption fails until the named Platform foundation supplies persistence/outbox/operator-health and claim-recovery fixtures. Rollback: remove unused pure composed products before production delivery exists. Completion evidence: exact product goldens and call counters prove one admission, one time sample, at most one external call, visible failure/unknown and no semantic ambiguity, fabricated time or evidence leak. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

## 4. Serialization and Legacy Compatibility

- [ ] 4.1 Implement exact bounded v1 canonical JSON and immutable-reference policy tests. Objective: make JSON wire bytes deterministic and reject parser materialization amplification, unsupported evolution or unsafe owner references. Inputs: all target JSON DTOs, explicitly excluding the binary-only `DurableEnvelopeProjectionV1`, exact decoded-scalar key ordering, Unicode-preservation/no-normalization policy, canonical control/quote/reverse-solidus/no-solidus escaping, sole `YYYY-MM-DDTHH:MM:SS.ffffffZ` instant syntax, raw/output bytes, pre-materialization scanner, depth, per-container, aggregate member/node, decoded string, 19-digit integer and owner-codec limits. Outputs: explicit per-JSON-schema encoders/decoders, deterministic lexical/structural scanner, bounded parse hooks and golden bytes/fingerprints; no generic `default=str`, projection JSON codec, unbounded recursive serializer or shared reference implementation. Affected contracts: all v1 public JSON wire schemas and owner-reference policy. Validation: round-trip/property tables cover exact fields/version, pre-parse 65,536-byte input, canonical output, zero/one/999,999 microseconds, year `0001`/`9999`, reject `+00:00`, omitted/variable/over-microsecond fractions and leap seconds, CJK, composed/decomposed combining values as distinct bytes, controls, solidus, reverse solidus, quotation mark, escape-equivalent duplicate keys and decoded-key ordering, duplicate keys, BOM, strict UTF-8/surrogates, nonfinite/binary/float, depth 8/9/1,500, 19/20-digit integers, exact depth algorithm, per-container/aggregate member/node/string limits, deterministic Python 3.10/highest-supported error parity, source/editable/offline-wheel parity, digest tamper, owner unresolved, unauthorized/unapproved policy, unproved PIT/quality/publication and no current/latest fallback; generic JSON encoding of the binary projection fails. Rollback: remove JSON codecs before DTOs; no persisted payload exists. Completion evidence: identical scalar sequences produce identical bytes/fingerprints in all install modes, canonically distinct sequences stay distinct, every one-unit-over-budget/tamper/proof-boundary case fails before materialization with a bounded structural error, and no competing projection representation exists. [validates:contracts.kernel] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

- [ ] 4.2 Implement pure one-way owner-specific legacy mappers and compatibility snapshots without rerouting callers. Objective: document and test how current facts map conservatively while preserving all external behavior. Inputs: `trade_py.bus`, inventoried legacy-only data operations, mixed-origin naive `job_runs` times/statuses, `/api/run`, CLI event/run, Observatory error/artifact and runtime shutdown shapes. Outputs: only leaf adapters `bus_contracts`, `job_run_contracts`, `observatory_contracts` and `runtime_contracts`, mapper inventory with source/target version/lossiness/retirement, explicit Data Operations canonical-refusal disposition, and legacy/target golden fixtures; no aggregate re-export or production inbound import. Affected contracts: EventBus admission, Data Operations legacy snapshots, Web run, CLI waits, job statuses/times, Observatory errors/artifacts and shutdown-incomplete mapping. Validation: existing focused suites remain byte/field/exit compatible; EventBus mixed combinations preserve total and accepted/saturated/stopping/failed counts plus latest-50 summary/truncation through 1,024 source results and refuse 1,025/10x before proportional target allocation; same-event live UTC, naive local persisted, force-labelled replay, malformed, missing and DST fold/gap times all produce an observation with no event-time field and no fallback clock; `/api/run` accepted yields only `LegacyRunAdmissionObservation`, never a formal receipt; Data Operations remains unchanged and mapping is refused; job-run malformed/mixed UTC-local/DST gap/DST overlap/missing terminal-time tokens remain bounded `unproven` with no UtcInstant, while current running state requires row-bound owner instance/fence generation/unexpired liveness evidence and stale/missing/mismatch yields unknown/owner-lost; exact per-value tests exclude EventBus back-reference, event time, exception, path, PID and raw idempotency; refuse unknown statuses; never infer cancellation/formal refs/completion; cover the exact `LegacyArtifactObservation` state product, pure mapper not-checked, owner verifier provenance, root containment/no-follow/regular-file/pre-post identity and generation, traversal, symlink replacement, TOCTOU and one-byte tamper. Verification evidence samples `verified_at: UtcInstant` only after the post-hash identity/root/generation stability check succeeds, and tests prove it is verifier observation time rather than event, available or PIT time. Mappers never access DB/provider/pointer/process control outside the temporary owner verifier port; architecture tests reject aggregate facade/cross-owner imports and every non-test production inbound import to the four adapters. Rollback: remove owner mapper modules/tests; current callers were never switched. Completion evidence: old snapshots are unchanged and every mapped/lossy/refused value, omitted EventBus time, unproven job time/current-evidence rule, proof boundary and retirement condition is named in mapper evidence. [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

## 5. Validation and Delivery

- [ ] 5.1 Run focused contract, packaging and legacy compatibility validation plus required code-quality gates. Objective: verify the complete child without runtime or data behavior drift. Inputs: implementation, golden fixtures and existing tests. Outputs: passing pytest, import/build, compile, architecture, quality and whitespace evidence. Affected contracts: all child and retained legacy contracts. Validation: run focused new tests; existing `tests/test_cli_event_trigger.py`, `tests/test_runtime_commands.py`, `tests/test_web_runtime_commands.py`, `tests/test_web_runtime_resources.py`, relevant Observatory/API tests and architecture guard; run source/editable/wheel smoke, `python -m compileall src/trade trade_py tests`, `./trade dev check --show-plan`, `./trade dev check`, and `git diff --check`. Rollback: do not commit a failing unit; correct the owning package/contract/mapper/test. Completion evidence: all commands pass and tests use only temporary roots with no real DB/parquet/provider/network access. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

- [ ] 5.2 Run the six-role implementation review in a separate review worktree and resolve every P0. Objective: validate actual package boundaries, security/trust, compatibility, deterministic wire behavior and process-cleanup cost before merge. Inputs: frozen implementation diff and command evidence. Outputs: six source-citing reports and consensus dispositions. Affected contracts: all child contracts and retained external surfaces. Validation: all required roles inspect actual code; zero unresolved P0; accepted P1/P2 has a named child/owner; strict design digest still matches or is refreshed. Rollback: correct implementation, rerun focused gates and repeat affected review. Completion evidence: consensus confirms no production route/running behavior/data was changed and no bounded control path retains an unowned process. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

- [ ] 5.3 Inspect worktree safety, commit each validated unit, push after 3-5 commits and open a PR against `master`. Objective: deliver one independently reversible architecture prerequisite. Inputs: passing validation/review and strict approval. Outputs: intentional commits, remote branch and PR. Affected contracts: reviewed additive package and public DTO contracts only. Validation: `git status -sb` and staged diff contain no unrelated data/cache/config; commit bodies record validation, compatibility/no-data risk and exact TRAE co-author trailer; project-local official GitHub CLI auth/push/PR succeeds. Rollback: close/revert the isolated PR and stop new child consumers; no runtime/data rollback. Completion evidence: commit SHAs, push and PR URL are recorded. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

- [ ] 5.4 Create and strictly approve `runtime-owner-shutdown-and-recovery-hardening-v1` before any current runtime adopts these contracts. Objective: fix the audited close-hang paths without folding behavior changes into this contract child. Inputs: EventBus persistence retry/idle wait, WebResourceContainer concurrent stop/startup cleanup, RuntimeCommandRunner executor tail, FastAPI lifespan, generation/fence contract and current CLI/HTTP snapshots. Outputs: independent OpenSpec with wall-clock rollback fixture, monotonic deadlines, bounded process-tree ownership and compatibility gates. Affected contracts: current EventBus/Web/FastAPI/CLI runtime behavior and new control DTO adoption only. Validation: strict design approval precedes code; implementation proves real Uvicorn signal-to-reap, persistence failure, crash takeover, stale writer, concurrent stop and executor-tail paths while snapshots remain unchanged. Rollback: keep all current runtimes on legacy paths and remove the unused adoption wiring. Completion evidence: hardening child strict gate, implementation review and focused wall-clock/compatibility results are linked before any adoption PR merges. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:review]
