## 1. Governed Design Approval

- [x] 1.1 Run the diagnostic child design check and resolve deterministic findings. Objective: make the second architecture child complete, internally consistent and traceable before implementation. Inputs: parent `restructure-trade-architecture-v1`, current EventBus/data/Observatory/Web runtime/packaging audit, child proposal/design/specs and immutable policy v1. Outputs: governed artifacts with public-contract and concurrency profiles, four mapped obligations and no placeholder or unsupported claim. Affected contracts: all new Kernel, operation/control, immutable-reference and compatibility contracts. Validation: `./trade dev design-check kernel-and-public-contracts` and `openspec validate kernel-and-public-contracts --strict` return zero with no unresolved warning. Rollback: revise only this child OpenSpec; no source or runtime change exists. Completion evidence: native strict validation passed; the diagnostic design check selected `core`, `contract` and `concurrency`, returned zero findings, and bound the four obligations to their spec and task evidence. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

- [ ] 1.2 Run the required six-role architecture review in a dedicated review worktree and resolve every P0 and implementation-blocking P1. Objective: challenge Kernel admission, package sequencing, actor trust, status certainty, immutable-reference ownership, serialization bounds and non-returning worker/shutdown semantics. Inputs: frozen child artifact digest and cited current source. Outputs: six source-citing judge reports, one consensus, finding dispositions and digest-bound `design-review.toml`. Affected contracts: package/import, wire, actor, operation/process/error/control and legacy mapping contracts. Validation: reliability, performance, architecture, data-quality, observability and news/future judges approve; every P0 is resolved and accepted P1/P2 has an owner. Rollback: update design artifacts, invalidate the prior digest and repeat the panel. Completion evidence: review record contains current commit/date, policy/artifact digests, all roles and finding evidence. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

- [ ] 1.3 Obtain current-date strict design approval before implementation. Objective: bind code work to the reviewed contract generation. Inputs: resolved review record and unchanged governed artifacts. Outputs: strict approval report. Affected contracts: all child requirements. Validation: `./trade dev design-check kernel-and-public-contracts --strict` returns zero on the implementation start date. Rollback: do not create or edit production/config/test code; correct artifacts and re-review. Completion evidence: strict report records current date, full reviewed commit, policy digest, artifact digest and six approved roles. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

## 2. Package and Kernel Foundation

- [ ] 2.1 Prove and implement additive dual-root package discovery before adding target modules. Objective: make the new `trade` contracts real installed APIs without starting the full package migration. Inputs: current `pyproject.toml`, distribution `trade-py`, root `./trade`, console entry, clean build environment and target `src/trade`. Outputs: the smallest reviewed packaging configuration that retains all existing `trade_py`/console behavior and installs `trade`; if proof fails, no package/code change and a package-transition ADR blocker. Affected contracts: distribution, source/editable/wheel imports and console entry. Validation: temporary source, editable and clean wheel tests import `trade_py` and `trade`, invoke existing console/root help, inspect wheel members and prove no shim, symlink or `sys.path` mutation. Rollback: revert only additive discovery; legacy package and entrypoints remain. Completion evidence: build/install commands and wheel inventory pass from a directory outside the repository. [validates:contracts.kernel] [validates:contracts.compatibility] [validation:test]

- [ ] 2.2 Implement minimal Kernel modules and focused invariant tests. Objective: provide only opaque IDs, UTC time/deadline, SHA-256 digest, structural errors/result, envelope metadata and immutable-reference/policy identity. Inputs: approved Kernel table and static architecture guard. Outputs: small immutable standard-library modules with explicit exports and no initialization side effects. Affected contracts: `kernel-primitives` and generic portion of `immutable-reference-policy`. Validation: focused pytest covers valid values, every malformed/boundary value, wall-clock/monotonic separation, exact Result branch, owner codec refusal and path/moving-alias rejection; import isolation proves no forbidden module loads. Rollback: remove the unused Kernel modules/tests after reverting any dependent target contract commit. Completion evidence: tests pass at exact 1x limits and fail one unit beyond each limit. [validates:contracts.kernel] [validates:contracts.immutable-refs] [validation:test]

## 3. Platform and Processes Public Contracts

- [ ] 3.1 Implement trusted actor and typed command/query envelope contracts. Objective: prevent payload-supplied authority and arbitrary dictionary contracts before Platform ingress exists. Inputs: Kernel primitives, origin/principal/assurance taxonomy and adapter provenance rules. Outputs: immutable `ActorContext`, `CommandEnvelope` and `QueryEnvelope` values plus pure canonical codecs. Affected contracts: actor and message wire schemas. Validation: focused tests cover verified CLI/HTTP/scheduler/event/system evidence, anonymous read policy, payload spoofing, wire-decoded assurance downgrade, scope/delegation bounds, raw idempotency secrecy and forbidden framework payloads. Rollback: remove unused target contracts; no interface has adopted them. Completion evidence: every mutation with unknown/unverified actor is denied in fixtures and trusted origins round-trip exactly. [validates:contracts.operation-control] [validation:test]

- [ ] 3.2 Implement operation, process, error and query-status contracts with closed transitions. Objective: make durable admission, observation and recovery facts explicit without a repository. Inputs: approved orthogonal taxonomies, `OperationReceipt`, `ProcessView` and `ErrorEnvelope` schemas. Outputs: immutable DTOs, transition validators, bounded history/window metadata and exact codecs. Affected contracts: operation/process/error/query public schemas. Validation: state-table tests cover every allowed and forbidden transition, terminal timestamps, duplicate receipt identity, absent receipt on persistence failure, unknown/not-observed/unavailable/empty/partial/stale/quarantined distinctions, history truncation and unsafe error-field rejection. Rollback: remove target DTOs after dependent tests; legacy status objects remain authoritative. Completion evidence: exhaustive transition matrix and golden JSON fixtures pass. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

- [ ] 3.3 Implement truthful cancellation and shutdown control contracts and bounded fixtures. Objective: ensure a future control plane cannot report cancellation/completion early or block after its declared deadline. Inputs: operation/process IDs, actor, error, owner/control/observation deadlines and current runtime shutdown audit. Outputs: immutable `ControlReceipt`/`ShutdownReceipt`, residual ownership taxonomy and a standard-library test harness for shared-deadline stages. Affected contracts: cancellation, shutdown and finite observation. Validation: tests cover accepted-before-terminal cancellation, signal-not-terminal, denied/not-found/unavailable/deadline outcomes, caller observation timeout without owner mutation, ignored graceful child termination, concurrent stop callers, 1x/10x residual owners, persistent audit blockage and an executor tail join that must not run unbounded; elapsed assertions and thread/process cleanup pass. Rollback: remove unused contracts/harness; current RuntimeCommandRunner/WebResourceContainer behavior remains unchanged. Completion evidence: every control path returns by the shared test deadline with truthful residual state and no live non-daemon fixture process. [validates:contracts.operation-control] [validates:contracts.compatibility] [validation:test]

## 4. Serialization and Legacy Compatibility

- [ ] 4.1 Implement exact bounded v1 canonical JSON and immutable-reference policy tests. Objective: make wire bytes deterministic and reject unsupported evolution or unsafe formal references. Inputs: all target DTOs, size/depth/item/string limits and owner codec registry. Outputs: explicit per-schema encoders/decoders and golden bytes/digests; no generic `default=str` or recursive object serializer. Affected contracts: all v1 public wire schemas and reference identity. Validation: round-trip/property-table tests cover exact field sets, unknown field/version, nonfinite/binary/float input, depth/bytes/items/strings, deterministic ordering, source/editable/wheel parity, missing content as owner-unavailable and no fallback to current/latest. Rollback: remove codecs before DTOs; no persisted payload exists. Completion evidence: identical values produce identical bytes/digests in three install modes and every one-unit-over-budget case fails boundedly. [validates:contracts.kernel] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

- [ ] 4.2 Implement pure one-way legacy mappers and compatibility snapshots without rerouting callers. Objective: document and test how current facts map conservatively while preserving all external behavior. Inputs: `trade_py.bus`, data operations, `job_runs`, `/api/run`, CLI event/run, Observatory error/artifact and runtime shutdown shapes. Outputs: narrow `trade_py.compat.public_contracts` modules or smaller owner-named modules, mapper inventory with source/target version/lossiness/retirement, and legacy/target golden fixtures. Affected contracts: EventBus admission, Web run, CLI waits, job statuses, Observatory errors/artifacts and shutdown-incomplete mapping. Validation: existing focused suites remain byte/field/exit compatible; mapper tests exclude EventBus back-reference, exception, path, PID and raw idempotency from canonical DTOs, refuse unknown statuses, never infer cancellation/formal refs/completion, never access DB/provider/pointer/process control, and import only target public modules. Rollback: remove mapper modules/tests; current callers were never switched. Completion evidence: old snapshots are unchanged and every lossy/refused field is named in mapper evidence. [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

## 5. Validation and Delivery

- [ ] 5.1 Run focused contract, packaging and legacy compatibility validation plus required code-quality gates. Objective: verify the complete child without runtime or data behavior drift. Inputs: implementation, golden fixtures and existing tests. Outputs: passing pytest, import/build, compile, architecture, quality and whitespace evidence. Affected contracts: all child and retained legacy contracts. Validation: run focused new tests; existing `tests/test_cli_event_trigger.py`, `tests/test_runtime_commands.py`, `tests/test_web_runtime_commands.py`, `tests/test_web_runtime_resources.py`, relevant Observatory/API tests and architecture guard; run source/editable/wheel smoke, `python -m compileall src/trade trade_py tests`, `./trade dev check --show-plan`, `./trade dev check`, and `git diff --check`. Rollback: do not commit a failing unit; correct the owning package/contract/mapper/test. Completion evidence: all commands pass and tests use only temporary roots with no real DB/parquet/provider/network access. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:test]

- [ ] 5.2 Run the six-role implementation review in a separate review worktree and resolve every P0. Objective: validate actual package boundaries, security/trust, compatibility, deterministic wire behavior and process-cleanup cost before merge. Inputs: frozen implementation diff and command evidence. Outputs: six source-citing reports and consensus dispositions. Affected contracts: all child contracts and retained external surfaces. Validation: all required roles inspect actual code; zero unresolved P0; accepted P1/P2 has a named child/owner; strict design digest still matches or is refreshed. Rollback: correct implementation, rerun focused gates and repeat affected review. Completion evidence: consensus confirms no production route/running behavior/data was changed and no bounded control path retains an unowned process. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]

- [ ] 5.3 Inspect worktree safety, commit each validated unit, push after 3-5 commits and open a PR against `master`. Objective: deliver one independently reversible architecture prerequisite. Inputs: passing validation/review and strict approval. Outputs: intentional commits, remote branch and PR. Affected contracts: reviewed additive package and public DTO contracts only. Validation: `git status -sb` and staged diff contain no unrelated data/cache/config; commit bodies record validation, compatibility/no-data risk and exact TRAE co-author trailer; project-local official GitHub CLI auth/push/PR succeeds. Rollback: close/revert the isolated PR and stop new child consumers; no runtime/data rollback. Completion evidence: commit SHAs, push and PR URL are recorded. [validates:contracts.kernel] [validates:contracts.operation-control] [validates:contracts.immutable-refs] [validates:contracts.compatibility] [validation:review]
