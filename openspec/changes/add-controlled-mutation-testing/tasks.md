# Tasks

## 1. Governance

- [ ] 1.1 Complete the repository audit and diagnostic design check. Objective: freeze actual language/build/test/coverage/CI/core/exclusion/baseline facts and validate governed artifacts. Inputs: current source tree, pyproject, entrypoint, tests, remote, Cosmic Ray 8.4.6 APIs, full and mapped test timings. Outputs: proposal, design, spec, tasks, and `design-quality.toml`. Affected contracts: mutation CLI, report contract, process lifecycle, and CI routing. Validation: `openspec validate add-controlled-mutation-testing --strict` and `./trade dev design-check add-controlled-mutation-testing` pass with zero findings. Rollback: revise design-only files. Completion evidence: audit records 1161 pass/1 stable failure full baseline and the exact six-file 92-test mapped cohort/core 90-test collection with no real-data access. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 1.2 Run the six-role design review in a dedicated review worktree. Objective: challenge operator safety, deterministic scope, process-tree termination, concurrency, score integrity, CI cost, and temporal/news exclusions. Inputs: frozen governed artifacts and actual repository paths. Outputs: six source-cited reports, consensus, resolved P0/P1 design findings, and `design-review.toml`. Affected contracts: all controlled mutation requirements. Validation: every role reviews the exact digest, reports source-cited findings, reaches consensus with no unresolved P0, and resolves each P1 or assigns a named owner. Rollback: revise artifacts and repeat one review round. Completion evidence: digest-bound panel review record. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]

- [ ] 1.3 Obtain strict design approval before implementation. Objective: bind code to the reviewed generation. Inputs: six-role attestation. Outputs: strict approval. Affected contracts: all. Validation: review evidence names the verified commit and digest, and `./trade dev design-check add-controlled-mutation-testing --strict` returns zero findings. Rollback: do not implement if stale or failing. Completion evidence: current-date verified commit/digest and zero findings. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:review]

- [ ] 1.4 Gate execution on the external runner child change. Objective: prevent a nonexistent or weak runner label from becoming an implicit implementation dependency. Inputs: GitHub repository owner `huanwei1208`-accountable `provision-trade-mutation-v1-runner` change, named platform operator, immutable image digest, scale-set definition, and readiness evidence. Outputs: reviewed prerequisite reference and default-disabled execution switch. Affected contracts: runner identity, CI admission, delegated cgroup, kernel isolation, and schedule enablement. Validation: three consecutive readiness jobs prove pickup within five minutes, one-job destruction, finite delegated cgroup v2, Landlock/seccomp notification/addfd, no production mounts/secrets, and cleanup; absent evidence leaves only hosted plan-only CI and cannot queue execution. Rollback: keep execution switch disabled and remove no local controller capability. Completion evidence: child-change approval, named operator acknowledgement, image digest, readiness run URLs, accountable owner acknowledgement, and enabled-mode decision. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.ci-routing] [validation:review]

## 2. Controller and Configuration

- [ ] 2.1 Add the exact mutation dependency group and closed configuration parser. Objective: make Cosmic Ray 8.4.6 and coverage.py 7.10.7 API/operator/traversal compatibility, the exact seven-source definition/test/clock/entropy/dependency-manifest matrix, lowering-only mode/source/AST/import-closure/private-tree/report/retention budgets, safe output root, and mutant-ID exceptions machine-checkable. Inputs: approved spec and actual paths. Outputs: `pyproject.toml`, lockfile, `config/mutation-testing.toml`, `config/mutation-baseline.json`, `config/mutation-exceptions.toml`, parser, and tests. Affected contracts: mutation config v1. Validation: focused tests reject unknown keys, broader overrides, missing APIs/operators/definitions/tests, unsupported `fixed` clock/entropy, undeclared temporal/entropy/dynamic imports, any unreviewed row-field change, broad paths, unsafe output roots/symlinks, wildcards, ambiguous/stale exceptions, unsupported tool versions, and unsafe limits; exact operator-local `get_ast`/`ast_nodes`/`MutationVisitor` compatibility and valid config parse; Ruff, BasedPyright, TOML, and JSON checks pass. Rollback: remove optional group/config/parser. Completion evidence: focused pytest and syntax-check results. [validates:mutation.first-order-selection] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.2 Implement deterministic Git/source/test/mutant planning. Objective: seal and revalidate base/merge-base/HEAD/index/staged/worktree/diff identity, select only versioned canonical mutation positions on changed lines without broad fallback, report unlisted/untracked production paths as deferred, keep core/full on the exact definition matrix, and enumerate first-order allowlisted positions under file/byte/AST/dependency/visit/candidate/deadline budgets. Inputs: config, explicit base, exact Cosmic Ray traversal, tracked regular UTF-8 source. Outputs: `GitScopeSnapshot`, `CanonicalMutationPositionV1`, deferred inventory, unique nested definition/test map, execution/definition occurrence identities, deterministic plan, closed scanned-reason counts, and zero-work behavior. Affected contracts: changed/core/full planning, first-order invariant, and candidate algebra. Validation: temporary Git repository tests cover add/modify/content-rename/rename-only/delete, index/worktree drift before copy/publication, deferred untracked/unmapped paths, no eligible work, replacement/compound-token/`AddNot` insertion byte offsets including Unicode, ambiguous canonicalization rejection, multiline refusal and no enclosing-definition fallback, nested definition/lambda ownership, source/graph/visit ceilings, deadline checkpoints, `candidate_positions_scanned = selected + not_selected_scanned`, generated equals selected, unknown unscanned remainder, deterministic truncation, definition exclusions, and multi-mutation rejection. Rollback: remove planners. Completion evidence: focused planner pytest and versioned plan snapshots. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validation:test]

- [ ] 2.3 Implement the pre-`uv` sole-spawner supervisor and bootstrap contract. Objective: establish one `CLOCK_BOOTTIME`/boot-ID deadline domain, run ID, bounded phase-journal receipt, subreaper, sole OS spawn ownership, authenticated numerically bounded spawn/signal/wait/fallback protocol, opaque worker handles, fork-stop cgroup admission, signal handling, controller-loss fallback, and cleanup evidence before any project child. Inputs: approved mode constants, output-root contract, absolute Python 3.7+ interpreter, and shell command-entry uptime anchor. Outputs: `bootstrap_contract.py`, Python 3.7-compatible `protocol.py`/`supervisor.py`, per-run capability and peer authentication, one multiplexer, receipt transition protocol, cgroup/process-group owner, sole fallback writer, guard verifier/forced-close receipts, and focused tests. Affected contracts: invocation receipt, deadlines, protocol v1, process ownership, controller-loss recovery, and cleanup truth. Validation: syntax tests run under Python 3.7; static/runtime tests prove controller modules contain no spawn primitive and cover interpreter resolution/version rejection, no pre-supervisor `uv`, HMAC/peer/replay/request-correlation/unknown command/argv/environment/frame/8-FD-role rejection, 64/48-KiB ceilings, one client/no reconnect, `workers` in-flight/active and `2*workers+4` queue saturation with typed deadline failure, handle removal, hung `uv`, bootstrap/controller crash, controller SIGKILL and unique-cgroup proven/unknown OOM, wrapper SIGTERM, child fork-before-move prevention, pidfd/start-token identity, delegated cgroup membership/resource read-back, unsupported cgroup plan-only refusal, guard verifier key ownership, valid forced timeout/cancel close versus invalid EOF/truncation, D-state/cleanup-unconfirmed evidence, controller fallback request, and no false killed/clean result. Rollback: remove supervisor/protocol/bootstrap contract and root dispatch hook. Completion evidence: focused process tests show one OS spawner/receipt/fallback/guard-verification owner, bounded protocol saturation, and no ordinary supervisor-managed descendant after completion. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.4 Implement private source trees, mutation and coverage adapters. Objective: build a bounded reviewed import closure, prove private-tree provenance, apply exactly one position-verified Cosmic Ray mutation, and retain independently recomputable target-only line evidence. Inputs: deterministic plan, matrix/dependency manifest, tool compatibility, and supervisor deadline. Outputs: private-tree builder, engine adapter, minimal hashed coverage rcfile, `CoverageTupleEvidenceV1`, provenance plugin, source restore/purge logic, and tests. Affected contracts: first-order proof, source immutability, coverage/no-coverage classification, clock/entropy cache eligibility, and private-tree capacity. Validation: tests cover canonical-position/occurrence/span handshake, source drift, original hashes, closure manifest and 10x unrelated modules, alias-aware clock/entropy/static import detection and unresolved dynamic-import non-cacheability, same-size fixed-mtime bytecode isolation, exact mapped tests, multiple imported modules with one filtered target, cleared `COVERAGE_*`, malformed/missing/duplicate/oversized coverage, tuple source/node/argv/rcfile/environment/provenance/version/executed-line digest persistence, bundle recomputation of `no_coverage_line`, missing matrix mapping validation failure, baseline-unavailable, private-tree file/byte/copy/free-space limits, and unchanged checkout hashes. Rollback: remove tree/engine/coverage modules. Completion evidence: focused tests mutate only temporary copies and produce deterministic plans plus independently validated coverage tuples. [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.5 Implement fail-closed Linux worker isolation. Objective: deny and independently observe network, process creation, and non-private data access without pathname races or forgeable evidence. Inputs: private tree, reviewed allowlists, worker identity, policy digest, and supervisor-owned cgroup/start gate. Outputs: Landlock launcher, seccomp user-notification broker, `SCM_RIGHTS` listener handshake, controller-side `openat2` plus `SECCOMP_IOCTL_NOTIF_ADDFD`, child write-only guard stream, controller-private syscall audit, and tests. Affected contracts: kernel isolation, descriptor ownership, guard/audit integrity, and plan-only fallback. Validation: tests cover listener/tracee identity and FD closure, ACK/liveness failure, brokered allowlisted opens, symlink/pathname races, real SQLite/pandas-pyarrow/os.open/Path.open/mmap/DuckDB denial, caught Python/native violations overriding exit 0/1, credential/proxy scrubbing, reviewed native thread clone, denied process clone/fork/exec/network, complete/empty/truncated/duplicate/wrong-token independent streams, unsupported Landlock/seccomp fail-closed, and non-Linux plan-only. Rollback: remove isolation modules and leave execution disabled, not weakened. Completion evidence: focused isolation tests expose no real-data/network/process path and cannot have one evidence channel forge the other. [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.6 Implement bounded scheduling and terminal classification. Objective: admit work by CPU plus enforced per-worker cgroup memory controls, enforce per-mutant/global deadlines and output bounds, and assign one truthful terminal/phase/cleanup tuple under every race. Inputs: plan, baseline timings/`memory.peak`, supervisor opaque-handle API, isolated launcher, and closed model. Outputs: logical scheduler, finite queue, supervisor-handle client, sticky-integrity typed state machine, process-tree cancellation requests, immutable outcomes, and cache eligibility facts. Affected contracts: workers, timeout, cancellation, OOM, fifteen-terminal algebra, and result cache identity. Validation: tests cover effective CPU calculation, controller/safety/renderer/worker memory equation and `memory.max/high/swap/pids` read-back, no-delegated-cgroup plan-only refusal, native threads one, closed baseline failure reasons and baseline timeout exclusion from mutant timeout, changed 60-second cap, execution-deadline admission, bounded output, every control-race order plus late integrity after natural exit, notification drain and verified normal/forced-close orders, the exact terminal x mutation-applied x worker-spawned x test-started x cleanup table including allowed infrastructure-unconfirmed tuples, run precedence `report_failed > degraded_infrastructure > signal_cancelled`, exact pytest exits, proven OOM/unknown SIGKILL as phase-appropriate infrastructure reasons, cleanup override, no full-suite fallback, and every closed terminal. Rollback: remove scheduler/executor modules. Completion evidence: focused race/count tests leave no ordinary child and never classify timeout/infrastructure/cancellation/cleanup failure as killed or cacheable. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.7 Implement generation-atomic report storage and recovery. Objective: publish one bounded root-hash-bound report generation under an independent report sequence, preserve sole-supervisor fallback truth, and provide safe deterministic inspection/repair. Inputs: immutable execution/coverage records, receipt journal, sealed Git scope, safe output root, closed error registry, and render limits. Outputs: report models/renderers/store, leases, redaction, report reservation/tombstone, exact `current.json`, fallback request, quarantine, raw-evidence retention DAG/evidence tombstones, inspect/repair APIs, and tests. Affected contracts: `trade.mutation.report.v1`, invocation/fallback/manifest/inspect/repair/evidence-tombstone/error-code schemas, status/count algebra, and exits. Validation: golden tests cover every exact terminal/phase/cleanup tuple and mixed run, fifteen-terminal/scanned/generated equations, coverage tuple recomputation, manifest non-self hash, report reserve before pointer, exact run/sequence/root/size `current.json` recovery versus tombstone, interruption at every fsync/rename/pointer boundary, supervisor fallback/controller loss, every inspect state and text/JSON exit, first-run/corrupt/symlink/missing-generation recovery, detail-evidence expiry while compact trend anchors survive but cannot establish baseline/comparability, Git drift, safe-root/lease races, closed error/remediation registry, redaction, truncation and renderer limits. Rollback: remove report store/render/recovery modules while preserving ignored evidence for manual audit. Completion evidence: every run status exposes one receipt-bound generation or supervisor-owned typed fallback and never stale current/unrooted evidence. [validates:mutation.truthful-report] [validation:test]

- [ ] 2.8 Implement disposable committed result cache. Objective: accelerate exact repeated outcomes without treating cache as evidence or weakening truth. Inputs: published generation, complete cache identity, staged eligible fresh outcomes, and output lock. Outputs: content-addressed entries, run commit markers, deterministic local eviction, bounded remote archive import/export and restore diagnostics, and tests. Affected contracts: cache identity/eligibility/retention. Validation: tests cover uncommitted/partial/degraded/cleanup-unconfirmed/stale/corrupt/clock-entropy-ineligible misses, same-key races, exact marker binding, 20000-entry/512-MiB eviction, at most 20 remote candidates/two downloads/512 MiB/deadlines, numeric run-attempt ordering, attempted-carrier/rejection/count/byte/elapsed/truncation schema, scheduled-only upload, 14-artifact/7-GiB quota skip without deletion, seven-day retention metadata, and correctness with no cache. Rollback: remove cache projection; reports remain authoritative. Completion evidence: cache hit/miss/eviction/restore/quota snapshots. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.9 Implement trend ledger, aggregate, and reconciliation. Objective: make core/full trend continuity auditable under an independent sequence and bounded remote carriers. Inputs: published core/full report root/report sequence, score/key-set digests, GitHub run/attempt metadata, UTC anchor, and output lock. Outputs: trend reservation/source/tombstone chain, high-water/checkpoint, `trend.jsonl`, bounded rolling aggregate validator, restore diagnostics, `reconcile` service, and tests. Affected contracts: trend sequence/epoch/projection/retention schemas. Validation: tests cover independent report/trend sequences, changed/plan runs consuming no trend sequence, high-water/hash-chain/checkpoint recovery, strictly monotonic publishing tuple and old-workflow rerun `carrier_not_monotonic`, duplicate same-high-water carrier, corrupt-highest `predecessor_invalid` without old fallback, unavailable epoch, 20-candidate/20-MiB metadata/two-download/64-MiB/180-second restore limits and diagnostics, 64-MiB aggregate, scheduled-only upload, 90-artifact/5760-MiB quota skip, detail-evidence-expired continuity without comparison/baseline, projection gaps, and dry-run/apply reconcile without fabricated predecessor/score. Rollback: remove trend projection/aggregate while reports remain readable. Completion evidence: deterministic chain, rerun, retention, diagnostic and epoch golden fixtures. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.10 Implement baseline and exception governance. Objective: evaluate score comparison only from verified detail evidence and allow only exact expiring exceptions. Inputs: reviewed baseline/exceptions, immutable bundles with coverage tuples, comparison identities, and trusted UTC anchors. Outputs: detail-recomputing baseline evaluator, exception validator, paired realtime/boottime temporal validator, and tests. Affected contracts: `trade.mutation.baseline.v1`, exception registry, gradual policy, and temporal high-water. Validation: tests cover bundle/report/run/commit/tool/environment/key-set and coverage-line binding, stored aggregate/detail-expired refusal, exact comparison/denominator/exception/no-coverage sets, `CanonicalMutationPositionV1` plus operator arguments and execution occurrence, wildcards/stale source rejection, RFC3339 future/five-minute skew/within-run and cross-run rollback/reboot/missing-anchor cases, and 180-day interval. Rollback: leave baseline unestablished and exception registry empty. Completion evidence: baseline, exception, and temporal golden decisions. [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.11 Implement capacity qualification governance. Objective: admit scheduled modes only on a fresh representative qualified runner and preserve outer-phase reserves. Inputs: reviewed capacity file, immutable qualification bundles, exact runner identity, source/test tuple distribution, protocol timing, and trusted UTC anchor. Outputs: `capacity.py`, serial/capacity proposal/validator, deterministic tuple list-schedule projection, outer phase-budget validator, and tests. Affected contracts: `trade.mutation.capacity.v1`, schedule admission, execution time/memory/disk headroom. Validation: tests cover immutable runner identity versus throttling, every source row/test tuple, at least `max(30,5*tuple_count)` and five completions per tuple, tuple p95 spawn/admission/execution/finalization/cleanup, deterministic selected-limit list scheduling and 20% time headroom, memory/disk including report+bundle+upload scratch, 15/45/110 outer equations, current/expired/mismatched qualification, trusted UTC rollback/missing anchor, and proposal never editing reviewed files. Rollback: leave capacity false and scheduled execution disabled. Completion evidence: representative qualification and phase-budget golden decisions. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.12 Implement immutable CI bundle construction and independent validation. Objective: package exact receipt-bound evidence in the execution workspace and validate downloaded bytes without path trust. Inputs: receipt, referenced generation/fallback, coverage tuples, workflow/repository/run/attempt identity, and closed error registry. Outputs: `bundle.py`, `trade.mutation.bundle.v1`, local builder/validator, artifact metadata outputs, `trade.mutation.ci-summary.v1`, fresh-directory validator, and tests. Affected contracts: bundle/validation/CI-summary schemas and evidence integrity. Validation: tests cover exact receipt/generation-or-fallback/coverage binding, manifest/member/count/phase/cleanup/root digests, unknown error code, extra/path-escaping/missing/corrupt members, run-attempt mismatch, expected/actual digest, exported artifact identity, no global-current/newest selection, validation from copied/downloaded bytes, and authoritative valid/missing/invalid/hard-loss summaries with original controller exit and remediation. Rollback: disable workflow evidence route; local reports remain. Completion evidence: cross-directory golden bundle validation and trusted summary snapshots. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.13 Implement canonical CLI and root/facade contracts. Objective: expose one seven-command supervisor-first grammar with deterministic argv/stdout/stderr/receipt/exit behavior. Inputs: controller services, supervisor protocol, root `trade` routing, and optional frozen dependency group. Outputs: Python CLI, pre-`uv` `trade dev mutation` dispatch, all-command `scripts/mutation-test` facade, help and text/JSON renderers. Affected contracts: `changed|core|full|inspect|repair|qualify|reconcile`, option applicability, lowering-only options, plan-only, verbosity, schemas, error registry, and exits. Validation: golden tests cover root/facade argv/output/exit parity for every command/state, option rejection, help, mutually exclusive quiet/verbose, JSON single-document stdout and diagnostic-only stderr, text/TTY/no-ANSI behavior, bounded progress, receipt export, plan-only, all eight inspect states, repair, qualification statuses, reconcile statuses, missing dependency/preflight diagnostics, no pre-supervisor `uv`, frozen uv forwarding, Bash syntax and ShellCheck. Rollback: remove additive command/facade. Completion evidence: CLI golden snapshots and parity tests. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:test]

## 3. CI and Documentation

- [ ] 3.1 Add report-only GitHub Actions routing with strict cross-job evidence validation. Objective: provide hosted plan-only routing immediately and enable changed/core/full execution only after runner and capacity prerequisites, without making mutation outcomes or full a PR gate. Inputs: canonical command, invocation receipt/bundle artifact metadata, task 1.4 readiness decision, reviewed capacity file, outer phase equations, remote quotas, and rolling aggregate contract. Outputs: `.github/workflows/mutation-testing.yml` with default-disabled execution switch. Affected contracts: CI events, runner readiness/preflight, exact concurrency semantics, outer timeouts, result cache versus trend storage, capacity admission, bundle retention, and trusted summary behavior. Validation: static workflow tests assert ordinary hosted plan-only when execution is disabled; enabled routes use only `runs-on: [self-hosted, linux, x64, ephemeral, trade-mutation-v1]`; no secrets for fork PR; image/namespace/mount/delegated-cgroup/memory/Landlock/seccomp preflight; explicit base SHA; literal crons; recomputed 15/45/110 minute equations; real `${{ github.repository }}`/PR expressions; PR cancellation; shared long group with `cancel-in-progress: false`; exact manual input; report-only execution exit capture; untrusted execution summary label; execution-job `if: always()` local bundle validation/upload; separate job fresh-directory byte validation and authoritative `trade.mutation.ci-summary.v1`; 14-day bundle retention; scheduled-only cache/aggregate upload with count/byte quota skip; old-workflow rerun monotonic refusal; capacity/runner readiness refusal; hard-runner-loss summary; and no PR core/full. YAML parses if available. Rollback: disable execution switch or remove workflow; local reports remain. Completion evidence: workflow pytest, config parse, hosted plan-only run, and prerequisite-gated execution snapshot. [validates:mutation.ci-routing] [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 3.2 Add concise mutation-testing operations documentation. Objective: document rationale, exact included/deferred scopes, modes, commands, lowering-only budgets, run/mutant statuses, survivors, exact exceptions, matched-mutant baseline policy, CI triggers, artifact/trend recovery, and anti-gaming. Inputs: final CLI/config/report contracts. Outputs: `docs/mutation-testing.md` and README developer link. Affected contracts: developer guidance. Validation: documentation review covers unresolved base, unsupported tool/API, baseline/provenance failure, no coverage, stale/corrupt cache, source/report limits, cancellation/orphan cleanup, missing artifact, equivalent exceptions, and no meaningless assertions, with zero implementation inconsistencies. Rollback: remove docs/link. Completion evidence: source-cited review finding set and text hygiene pass. [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:review]

## 4. Validation and Delivery

- [ ] 4.1 Run focused controller/config/CLI/workflow tests and the original stable core unit suite. Objective: prove mutation infrastructure and its selected tests are stable before a real mutation. Inputs: completed implementation. Outputs: passing focused evidence. Affected contracts: all. Validation: focused pytest, Ruff, BasedPyright, compileall, config/TOML/JSON syntax, bash syntax, and workflow static tests pass. Rollback: fix owning unit before commit. Completion evidence: command/result table. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.2 Run real bounded mutation smoke and capacity modes. Objective: demonstrate real high-value mutants without touching production source and produce reviewable scheduled-capacity proposals only on the accepted runner. Inputs: clean worktree, eligible changed fixture/branch diff, core/full config, task 1.4 readiness evidence, cold cache, serial and capacity profiles on the exact `trade-mutation-v1` runner. Outputs: changed report/bundle, bounded core smoke, and cold-cache representative core/full qualification evidence; if task 1.4 is unavailable, plan-only evidence and a documented hard blocker instead of weakened execution. Affected contracts: changed/core/full execution, resource admission, capacity schema, immutable bundle, and report format. Validation: changed generated/executed stay below 150 and 600 seconds; bounded core preserves upper limits; qualification covers every source row/test tuple with `max(30,5*tuple_count)` and five per tuple, records tuple-specific spawn/admission/execution/finalization p95, list-schedule projection, planning/copy/baseline/render/fsync/cleanup, cgroup memory, bundle scratch and disk headroom; 15/45/110 equations are recomputed; reports/bundles confirm counts, survivor details, cleanup state, roots and unchanged source hashes. Reviewed capacity and CI execution remain disabled unless every prerequisite passes. Rollback: discard ignored reports/cache and keep execution disabled. Completion evidence: validated bundle/CI summaries, qualification proposal, capacity table, runner evidence or explicit blocker, and unchanged source hashes. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.3 Run repository gates and record the known full-suite baseline. Objective: ensure no production behavior change and distinguish pre-existing debt. Inputs: final diff. Outputs: quality/test evidence. Affected contracts: existing repository. Validation: `./trade dev check --show-plan`, `./trade dev check`, focused and stable core pytest, `python -m compileall trade_py tests`, `git diff --check`, and full pytest run; only the pre-existing `test_verify_reports_current_then_stale` failure may remain unless upstream fixes it, and any new failure blocks delivery. Rollback: fix or revert mutation-owned files. Completion evidence: before/after failure comparison. [validates:mutation.narrow-scope] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.4 Run the six-role implementation review on one frozen commit, resolve every P0 and implementation-blocking P1, refresh strict approval if governed artifacts changed, then commit/push/open a PR. Objective: deliver one isolated reversible feature branch. Inputs: frozen diff and validation evidence. Outputs: final consensus, strict pass, intentional commits, remote branch, and PR. Affected contracts: developer mutation testing only. Validation: review cites actual code and evidence, reaches consensus with zero P0 and no implementation-blocking P1, confirms a clean worktree and required commit trailers, verifies execution remains disabled when runner evidence is absent, and records the project-local GitHub CLI PR result. Rollback: close PR/revert commits and disable workflow execution; no data rollback. Completion evidence: commit SHAs, review disposition, gate output, readiness state, and PR URL. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]
