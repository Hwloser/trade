# Tasks

## 1. Governance

- [ ] 1.1 Complete the repository audit and diagnostic design check. Objective: freeze actual language/build/test/coverage/CI/core/exclusion/baseline facts and validate governed artifacts. Inputs: current source tree, pyproject, entrypoint, tests, remote, Cosmic Ray 8.4.6 APIs, full and mapped test timings. Outputs: proposal, design, spec, tasks, and `design-quality.toml`. Affected contracts: mutation CLI, report contract, process lifecycle, and CI routing. Validation: `openspec validate add-controlled-mutation-testing --strict` and `./trade dev design-check add-controlled-mutation-testing` pass with zero findings. Rollback: revise design-only files. Completion evidence: audit records 1161 pass/1 stable failure full baseline and the exact six-file 92-test mapped cohort/core 90-test collection with no real-data access. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 1.2 Run the six-role design review in a dedicated review worktree. Objective: challenge operator safety, deterministic scope, process-tree termination, concurrency, score integrity, CI cost, and temporal/news exclusions. Inputs: frozen governed artifacts and actual repository paths. Outputs: six source-cited reports, consensus, resolved P0/P1 design findings, and `design-review.toml`. Affected contracts: all controlled mutation requirements. Validation: every role reviews the exact digest, reports source-cited findings, reaches consensus with no unresolved P0, and resolves each P1 or assigns a named owner. Rollback: revise artifacts and repeat one review round. Completion evidence: digest-bound panel review record. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]

- [ ] 1.3 Obtain strict design approval before implementation. Objective: bind code to the reviewed generation. Inputs: six-role attestation. Outputs: strict approval. Affected contracts: all. Validation: review evidence names the verified commit and digest, and `./trade dev design-check add-controlled-mutation-testing --strict` returns zero findings. Rollback: do not implement if stale or failing. Completion evidence: current-date verified commit/digest and zero findings. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:review]

- [ ] 1.4 Gate execution on the external runner child change. Objective: prevent a nonexistent or weak runner label from becoming an implicit implementation dependency. Inputs: GitHub repository owner `huanwei1208`-accountable `provision-trade-mutation-v1-runner` change, named platform operator, immutable image digest, scale-set definition, and readiness evidence. Outputs: reviewed prerequisite reference and default-disabled execution switch. Affected contracts: runner identity, CI admission, delegated cgroup, kernel/process-control isolation, protected genesis namespace, and schedule enablement. Validation: three consecutive readiness jobs prove pickup within five minutes, one-job destruction, finite delegated cgroup v2 including freezer, private controller PID/mount namespace and procfs, `PR_SET_DUMPABLE=0`, empty capabilities, process-control seccomp denials, Landlock/seccomp notification/addfd, no production mounts/secrets, cleanup, and create-only/no-update/no-delete rules for `refs/tags/trade-mutation-genesis-v1/*`; changed execution additionally requires one exact cold-cache 150-mutant/600-second smoke on that ready profile, while scheduled core/full separately require task 2.11 capacity qualification; absent evidence leaves only hosted plan-only CI and cannot queue execution. Rollback: keep execution switch disabled and remove no local controller capability. Completion evidence: child-change approval, named operator acknowledgement, image digest, readiness run URLs, process-control/freezer/genesis-policy probe bundle, changed-smoke bundle, accountable owner acknowledgement, and per-mode enabled decision. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.ci-routing] [validation:review]

## 2. Controller and Configuration

- [ ] 2.1 Add the exact mutation dependency group and closed configuration parser. Objective: make Cosmic Ray 8.4.6 and coverage.py 7.10.7 API/operator/traversal compatibility, the exact seven-source definition/test/clock/entropy/dependency-manifest matrix, lowering-only mode/source/AST/import-closure/private-tree/report/retention/journal/bundle budgets, safe output root, reviewed 512-MiB controller/1024-MiB baseline bootstrap caps, one immutable trend genesis marker plus create-only consumption-ref name per configuration epoch, and mutant-ID exceptions machine-checkable. Inputs: approved spec and actual paths. Outputs: `pyproject.toml`, lockfile, `config/mutation-testing.toml`, `config/mutation-baseline.json`, `config/mutation-exceptions.toml`, parser, and tests. Affected contracts: mutation config v1. Validation: focused tests reject unknown keys, broader overrides, missing APIs/operators/definitions/tests, unsupported `fixed` clock/entropy, undeclared temporal/entropy/dynamic imports, any unreviewed row-field change, broad paths, unsafe output roots/symlinks, non-finite/raised bootstrap caps, invalid journal ordinary-frame/count/byte/terminal-reserve or bundle bounds, missing/duplicate/reused/mismatched genesis markers/ref names, wildcards, ambiguous/stale exceptions, unsupported tool versions, and unsafe limits; they recompute the 1932/10432/50432-entry and 2,921,916/11,668,416/52,828,416-byte normal journal demand from 128 invocation frames, ten frames per mutant, 1029 encoded bytes and the terminal reserve and reject any demand reaching a mode cap; exact exceptions remain selected `equivalent_exception` records rather than `exception_filtered`; exact operator-local `get_ast`/`ast_nodes`/`MutationVisitor` compatibility and valid config parse; Ruff, BasedPyright, TOML, and JSON checks pass. Rollback: remove optional group/config/parser. Completion evidence: focused pytest and syntax-check results. [validates:mutation.first-order-selection] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.2 Implement deterministic Git/source/test/mutant planning. Objective: seal and revalidate base/merge-base/HEAD/index/staged/worktree/diff identity, select only versioned canonical mutation positions on changed lines without broad fallback, report unlisted/untracked production paths as deferred, keep core/full on the exact definition matrix, and enumerate first-order allowlisted positions under file/byte/AST/dependency/visit/candidate/deadline budgets. Inputs: config, explicit base, exact Cosmic Ray traversal, tracked regular UTF-8 source. Outputs: `GitScopeSnapshot`, `CanonicalMutationPositionV1`, deferred inventory, unique nested definition/test map, execution/definition occurrence identities, deterministic plan, closed scanned-reason counts, and zero-work behavior. Affected contracts: changed/core/full planning, first-order invariant, and candidate algebra. Validation: temporary Git repository tests cover add/modify/content-rename/rename-only/delete, index/worktree drift before copy/publication, deferred untracked/unmapped paths, no eligible work, replacement/compound-token/`AddNot` insertion byte offsets including Unicode, ambiguous canonicalization rejection, multiline refusal and no enclosing-definition fallback, nested definition/lambda ownership, source/graph/visit ceilings, deadline checkpoints, `candidate_positions_scanned = selected + not_selected_scanned`, generated equals selected, unknown unscanned remainder, deterministic truncation, definition exclusions, and multi-mutation rejection. Rollback: remove planners. Completion evidence: focused planner pytest and versioned plan snapshots. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validation:test]

- [ ] 2.3 Implement the pre-`uv` sole-spawner supervisor and bootstrap contract. Objective: establish one `CLOCK_BOOTTIME`/boot-ID deadline domain, run ID, bounded mutable invocation state, sole OS spawn ownership, non-filesystem controller/supervisor isolation, authenticated bounded work/control lanes, one-use bootstrap-to-application handoff, terminally sealed supervisor attestation, immutable sealed-core and wrapper finalization, controller-loss fallback, and cleanup evidence before any project child. Inputs: approved mode constants, output-root contract, absolute Python 3.7+ interpreter, shell command-entry uptime anchor, and process-control/write-denial policy. Outputs: `bootstrap_contract.py`, Python 3.7-compatible `protocol.py`/`supervisor.py`, stdlib-only `evidence_finalizer.py`, `BootstrapResultV1` transition FD/read-only memfd handoff directly to `application.py`, per-run channel capability and peer authentication, priority multiplexer, `trade.mutation.invocation-state.v1` transition protocol, cgroup/process-group owner, immutable `trade.mutation.sealed-receipt-core.v1`, immutable `trade.mutation.invocation.v1` wrapper, sole fallback/final-generation/local-bundle writer, and 2048/12288/61440-entry plus 8/48/192-MiB journal whose normal demand is 1932/10432/50432 entries and 2,921,916/11,668,416/52,828,416 bytes including the 304-entry/1,246,704-byte four-worker terminal reserve. Affected contracts: bootstrap/application/finalizer runtime ownership, mutable state, sealed core, invocation wrapper, deadlines, protocol v1, process ownership, attestation provenance, controller-loss recovery, and cleanup truth. Validation: syntax tests run under Python 3.7; static/runtime tests prove controller modules contain no spawn primitive, CLI is never the launched runtime entry, and final evidence ownership is unique; cover interpreter resolution/version rejection, no pre-supervisor `uv`, one DTO/EOF/digest/sealed-memfd consumption and replay/duplicate/trailing-byte rejection; HMAC/peer/replay/request/argv/environment/frame/8-FD-role rejection; one client/no reconnect; `workers` in-flight/active, `2*workers` work-lane saturation, and reserved `2*workers+8` priority control capacity; `PR_SET_DUMPABLE=0`, private PID/procfs, empty capabilities and denied process-control attempts even with the channel key; immediate spawn-before-registration journal entry; channel failure after fork before cgroup move, after move before registration ACK, and after ACK, each cleaning every spawned/unreaped registry entry; journal ordinary-demand/derived-reserve saturation, maximum four-worker notification drain, torn frame, valid-prefix truncation, append-after-seal, missing/extra/reordered entries and sealed-core anchor mismatch; application candidate submission/exit, worker/application cleanup, seal, sealed-core read-back, finalizer publication, wrapper and bundle ordering; directed `report -> sealed_core` and `wrapper -> sealed_core + report_or_fallback` binding with no back-edge or digest cycle; bootstrap/application crash, D-state/cleanup-unconfirmed, fallback, and no false killed/clean result. Rollback: remove supervisor/protocol/bootstrap/finalizer contract and root dispatch hook. Completion evidence: focused process tests show one OS spawner/listener/state/core/wrapper/fallback/attestation/final-evidence owner, one bootstrap-to-application DTO consumption, source-exclusive sealed journal with formula-derived normal and terminal capacity, acyclic immutable evidence, and no ordinary supervisor-managed descendant after completion. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.4 Implement private source trees, mutation and coverage adapters. Objective: build a bounded reviewed import closure, prove private-tree provenance, apply exactly one position-verified Cosmic Ray mutation, and retain independently recomputable target-only line evidence. Inputs: deterministic plan, matrix/dependency manifest, tool compatibility, and supervisor deadline. Outputs: private-tree builder, engine adapter, minimal hashed coverage rcfile, `CoverageTupleEvidenceV1`, provenance plugin, source restore/purge logic, and tests. Affected contracts: first-order proof, source immutability, coverage/no-coverage classification, clock/entropy cache eligibility, and private-tree capacity. Validation: tests cover canonical-position/occurrence/span handshake, source drift, original hashes, closure manifest and 10x unrelated modules, alias-aware clock/entropy/static import detection and unresolved dynamic-import non-cacheability, same-size fixed-mtime bytecode isolation, exact mapped tests, multiple imported modules with one filtered target, cleared `COVERAGE_*`, malformed/missing/duplicate/oversized coverage, tuple source/node/argv/rcfile/environment/provenance/version/executed-line digest persistence, bundle recomputation of `no_coverage_line`, missing matrix mapping validation failure, baseline-unavailable, private-tree file/byte/copy/free-space limits, and unchanged checkout hashes. Rollback: remove tree/engine/coverage modules. Completion evidence: focused tests mutate only temporary copies and produce deterministic plans plus independently validated coverage tuples. [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.5 Implement fail-closed Linux worker isolation. Objective: deny and independently observe network, process creation, and non-private data access without pathname races, controller-forgeable evidence, listener competition, or forced-close notification races. Inputs: private tree, reviewed allowlists, worker identity, policy digest, supervisor-owned cgroup/start gate/freezer, and supervisor attestation sink. Outputs: Landlock launcher, bounded pre-start seccomp user-notification handshake whose raw listener is transferred once to and retained only by the supervisor after ACK, supervisor-side `openat2` plus `SECCOMP_IOCTL_NOTIF_ADDFD`, child write-only guard stream whose read end belongs only to the supervisor, supervisor-owned live syscall audit, opaque-handle forced close, and tests. Affected contracts: kernel isolation, listener/descriptor ownership, guard/audit integrity, forced-close drain, and plan-only fallback. Validation: tests prove no application/test import or notification decision occurs before ACK, the launcher never receives a notification and closes its listener/transfer endpoint immediately after ACK, and the supervisor is the first/only consumer. Tests also cover listener/tracee identity, ACK/liveness failure, brokered allowlisted opens, symlink/pathname races, real SQLite/pandas-pyarrow/os.open/Path.open/mmap/DuckDB denial, caught Python/native violations overriding exit 0/1, credential/proxy scrubbing, reviewed native thread clone, denied process clone/fork/exec/network, complete/empty/truncated/duplicate/wrong-token streams, and controller lacking listener, guard verifier, syscall-audit writer, and attestation writer. Adversarial tests attempt `dup|dup2|dup3|F_DUPFD|SCM_RIGHTS|pidfd_getfd`, retain a transfer endpoint after ACK, and race a second notification consumer; every attempt must be impossible or `incomplete_evidence`. Normal and forced close retain the same supervisor listener owner while freeze/read-back, frozen drain to EAGAIN, unchanged membership, supervisor audit completion, TERM/cgroup.kill without unfreeze, wait/reap/empty cgroup, final drain/guard EOF execute; notifications injected at every boundary are recorded/denied or make evidence incomplete. Rollback: remove isolation modules and leave execution disabled, not weakened. Completion evidence: focused isolation tests expose no real-data/network/process path, prove bounded trusted transfer followed by supervisor-exclusive listener/audit ownership, and demonstrate no competing consumer or enqueue window. [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.6 Implement bounded scheduling and terminal classification. Objective: admit work by the same stable CPU plus finite measured-memory scheduler used in qualification, enforce deadlines/output bounds, and assign one truthful terminal/phase/cleanup/score tuple under every race. Inputs: stable selected cohort, reviewed bootstrap caps, tuple `memory.peak`, supervisor opaque-handle API, isolated launcher, and closed model. Outputs: dual-resource logical scheduler, finite queue, supervisor-handle client, sticky-integrity typed state machine, immutable outcomes, run-score eligibility decision, and invocation-kind cache facts. Affected contracts: workers, memory bootstrap, timeout, cancellation, OOM, fifteen-terminal algebra, phase/cleanup facts, nine-state run precedence, score precedence, exception resolution, and result cache identity. Validation: tests cover effective CPU, finite bootstrap caps and transition to measured reservations, `pids.max=64`, 64-notification cap, cgroup read-back, no-delegation plan-only, native threads one, changed 60-second cap, deadline admission, and stable runtime list scheduling that admits only when worker slot plus `controller+safety+renderer+sum(worker memory.max) <= effective_memory*0.80`, including CPU-pass/memory-fail and alternating-size cohorts. Selection tests prove cap first then exception lookup, with no exception promotion. Race/table tests cover every normative terminal x four phase facts x cleanup tuple, factual equations, all nine run statuses with pairwise/full truth-table precedence and retained causes, especially `report_failed` plus degraded facts, and score precedence through `incomplete_outcome`; every mixed killed/survived plus incomplete terminal nulls score and forbids cache/baseline/trend/aggregate. Non-scheduled invocations serialize `cache=not_applicable` and perform no staging/commit/eviction. Rollback: remove scheduler/executor modules. Completion evidence: focused scheduling/race/count/precedence tests independently recompute every fact and never classify or project incomplete work as killed/score-eligible/cacheable. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.7 Implement candidate-plus-post-seal generation storage and recovery. Objective: render one bounded immutable candidate after worker finalization, then let the supervisor seal the journal, write the immutable core, and use the closed standard-library finalizer to publish one root-hash-bound evidence generation, immutable wrapper, current pointer, and local bundle under an independent report sequence. Inputs: immutable execution/coverage records, final per-worker attestation digests, safe output root, closed error registry, and render limits. Outputs: report candidate models/renderers/store, single-use candidate capability, `evidence_finalizer.py`, leases, pre-persistence redaction, report reservation/tombstone, exact `current.json`, immutable core/wrapper/fallback, quarantine, separate run-root/bundle inspect APIs, and tests. Affected contracts: report-candidate/report/invocation-state/sealed-core/invocation-wrapper/attestation/fallback/manifest/inspect/repair/evidence-tombstone/error-code schemas, status/count/score/mutation-coverage algebra, and exits. Validation: golden tests cover application candidate fsync/submission/exit, all worker/application cleanup, terminal journal seal, sealed-core fsync/read-back, evidence-anchor/final manifest/immutable-generation/wrapper/current/local-bundle publication in that order; interruption at every boundary must expose the previous current generation, a wrapper-bound new generation, or typed fallback, never an unsealed success. Tests prove report members never reference the wrapper, wrapper references exact core and report/fallback, fallback wrapper never advances current, neither immutable object is rewritten, and no hash cycle exists. Tests cover derived ordinary/terminal journal capacity and anchor, all mixed runs and status/score precedence, independent `mutation_coverage_ratio=(killed+survived)/(killed+survived+no_coverage_line)` plus advisory threshold, and separate inspect matrices: run-root only `unsafe|internal_error|active_lease|stale_expected_digest|evidence_expired|repairable|first_run|healthy`, bundle only `unsafe|internal_error|bundle_invalid|evidence_expired|bundle_valid`, with fact vectors/precedence/exits and unreachable-state rejection. Redaction tests scan mutable state, every journal payload, core, wrapper, candidate/final members, fallback, validation and retained staging for credentials, URL userinfo, controlled home/temp/runner roots and clear brokered paths before persistence. Every remediation comes from a typed argv factory with actual facts, round-trips the parser, contains no shell operator or runtime placeholder, and otherwise returns `no_automatic_remediation`. Rollback: remove candidate/finalizer/store/recovery modules while preserving ignored evidence. Completion evidence: every run exposes one acyclic post-seal core/wrapper/attestation-bound generation or finalizer-owned fallback and never stale current, valid journal prefix, unrooted evidence, leaked durable diagnostics, or ambiguous inspect state. [validates:mutation.truthful-report] [validation:test]

- [ ] 2.8 Implement disposable committed result cache. Objective: accelerate exact repeated outcomes without treating cache as evidence, weakening truth, or consuming an unreserved changed/manual remote phase. Inputs: published score-eligible generation, complete cache identity, staged eligible fresh outcomes, invocation kind, and output lock. Outputs: content-addressed entries, run commit markers, deterministic local eviction, scheduled-core/full-only bounded remote archive import/export and restore diagnostics, and tests. Affected contracts: cache identity/eligibility/retention and outer CI budgets. Validation: tests cover uncommitted/partial/degraded/integrity-override/cleanup-unconfirmed/score-ineligible/stale/corrupt/clock-entropy-ineligible misses, same-key races, exact marker binding, 20000-entry/512-MiB eviction; changed and manual invocations make zero remote cache list/read/download/upload calls; scheduled core/full inspect at most 20 remote candidates, download at most two/512 MiB/180 seconds, and record numeric run-attempt ordering plus attempted-carrier/rejection/count/byte/elapsed/truncation diagnostics. Before upload, namespace-aware stable pagination must prove at least the newest 15 cache-relevant records and the complete seven-day window; incomplete inventory skips upload without deletion, while a complete inventory enforces 14 artifacts/7 GiB. Tests also cover seven-day retention metadata and correctness with no cache. Rollback: remove cache projection; reports remain authoritative. Completion evidence: local/scheduled/manual cache hit/miss/eviction/restore/quota snapshots, zero-remote changed/manual trace, and incomplete-pagination skip proof. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.9 Implement scheduled projection, trend ledger, credentialed carrier adapter, genesis candidate, and artifact-aware reconciliation. Objective: make scheduled core/full continuity auditable in a separate post-validation process while manual runs remain cold factual diagnostics, immutable invocation evidence is never rewritten, and isolated code never receives GitHub credentials. Inputs: supervisor-finalized score-eligible bundle, workflow tuple, immutable carrier receipts, reviewed genesis marker/ref, all applicable UTC anchors, explicit reconcile carrier bytes, and output lock. Outputs: `projection.py`, immutable `trade.mutation.projection-receipt.v1`, trend chain/high-water/checkpoint, one closed-schema `carrier_adapter.py`, immutable carrier request/receipt schemas, content-addressed genesis candidate upload/read-back receipt, `GenesisConsumptionReceipt`, protected CAS, aggregate/cache carrier services, `trend.jsonl`, bounded validator, and reconcile proposal. Affected contracts: projection/trend/genesis/carrier/quota/reconciliation schemas. Validation: tests prove projection starts only after local and fresh-directory bundle validation, validates exact wrapper/core/report/bundle/workflow/carrier-receipt digests, is the sole local cache/trend/high-water writer, emits one immutable `complete|projection_degraded` receipt, and cannot rewrite wrapper, core, report, or carrier receipts. Tests prove only carrier adapter imports/calls first-party shared cache/aggregate/genesis/recovery artifact/ref APIs or receives shared credentials; the separate standard artifact action is limited to one exact factual-bundle upload/download and grants no shared authority; workflow/application/supervisor isolation is enforced. First scheduled writer uploads and fresh-directory read-backs exact candidate bytes before CAS; CAS binds artifact ID/name/run/attempt/size/root/digest; upload/read-back failure consumes nothing, while post-CAS aggregate failure recovers only from that carrier and cannot reuse genesis. Tests cover concurrent CAS, expiry/pause/deletion, invalid/missing refs, predecessor/high-water/old-rerun rules, quota pagination, scheduled authority, manual/changed zero shared-carrier and projection calls, explicit reconcile approval/revalidation, structured baseline non-comparability reason counts, durable diagnostic redaction, and maximum-of-all-valid UTC lineage with missing/invalid applicable anchor refusal. Rollback: remove projection/carrier/trend projections while reports and immutable invocation evidence remain readable; never delete a consumption ref. Completion evidence: deterministic projection-receipt/adapter/scheduled/manual/genesis-candidate/CAS/rerun/retention/reconcile fixtures. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.10 Implement baseline and exception governance. Objective: evaluate score comparison only from a verified score-eligible detail bundle, explain every refusal structurally, and allow only exact expiring exceptions. Inputs: reviewed baseline/exceptions, immutable bundles with coverage tuples and supervisor attestation, comparison identities, and trusted UTC anchors. Outputs: detail-recomputing baseline evaluator, score-eligibility gate, closed baseline non-comparability reason enum, independent mutation-coverage-ratio evaluator, exception validator, paired realtime/boottime temporal validator, and tests. Affected contracts: `trade.mutation.baseline.v1`, exception registry, gradual policy, run-score eligibility, mutation coverage visibility, and temporal high-water. Validation: tests cover bundle/report/run/commit/tool/environment/key-set/attestation and coverage-line binding, stored aggregate/detail-expired/score-ineligible refusal, factual numerator/denominator preservation with null displayed score, exact comparison/denominator/exception/no-coverage sets, each closed non-comparability reason and no convenient-intersection fallback, `mutation_coverage_ratio` null/defined equations and non-gating below-70-percent warning, `CanonicalMutationPositionV1` plus operator arguments and execution occurrence, wildcards/stale source rejection, RFC3339 future/five-minute skew/within-run and cross-run rollback/reboot/missing-anchor cases, and 180-day interval. Rollback: leave baseline unestablished and exception registry empty. Completion evidence: baseline, exception, score-eligibility, mutation-coverage and temporal golden decisions. [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.11 Implement capacity qualification governance. Objective: admit core/full only from a fresh exact-runner qualification and use the identical dual-resource scheduler at runtime. Inputs: reviewed capacity file, immutable bundles, exact runner identity, stable tuple distribution, bootstrap caps, and trusted lineage. Outputs: `capacity.py`, exact serial/capacity proposal/validator, runtime scheduler contract, changed-smoke validator, outer phase/disk/journal validator, and tests. Affected contracts: `trade.mutation.capacity.v1`, qualification envelope, runtime admission, execution time/concurrent-memory/disk/journal headroom. Validation: let `N=max(30,5*tuple_count)`; tests require exactly N stable identities in serial and the same N in capacity, exactly `2N` executions, five per tuple, no replacement, one shared 1800/5400-second controller deadline and one 45/110-minute outer containment. Both qualification and runtime admit only when a slot and `controller+safety+renderer+sum(memory.max) <= effective_memory*0.80` fit, with stable finish/slot/tuple ties; adversarial CPU-pass/memory-fail and alternating-size schedules must agree. Tests cover `pids.max=64`, 64-notification terminal reserve, the independent ordinary-capacity formula of 128 invocation plus ten per mutant at 1029 encoded bytes and its 1932/10432/50432-entry plus 2,921,916/11,668,416/52,828,416-byte demand, finite bootstrap transition, and disk formula `generation+journal+2*bundle+scratch+restore+temp` with scratch 32/80/208 MiB, restore 0 or 576 MiB, temp 32 MiB, pre-margin 248/1152/2032 MiB, and minimum available 760/1664/2544 MiB. Qualification rejects any profile/count/deadline/outer/journal/disk drift or projected cap exhaustion; changed remains a separate zero-remote smoke. Rollback: leave capacity false and execution disabled. Completion evidence: exact-2N core/full proposals, runtime-equivalence tests, journal/disk tables, and changed-smoke decision. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.12 Implement immutable CI bundle construction and independent validation. Objective: package exact immutable wrapper/sealed-core/attestation evidence and validate downloaded bytes without path trust. Inputs: invocation wrapper, sealed core with final journal anchor, complete sealed journal, referenced generation/fallback, coverage tuples, workflow identity, invocation expectation, and error registry. Outputs: `bundle.py`, bounded `trade.mutation.bundle.v1`, local validator, artifact outputs, `trade.mutation.ci-summary.v1`, fresh-directory validator, artifact-aware inspect, and tests. Affected contracts: wrapper/core/attestation/bundle/validation/CI-summary schemas, retention membership, and evidence integrity. Validation: tests cover directed wrapper/core/report equations; exact journal head/count/sequence/size/seal and generation/fallback/coverage binding; reject back-edges, digest cycles, legal prefixes, torn/extra/post-seal bytes; mode bundle caps 64/160/416 MiB; local not-applicable versus CI required; extra/path escape/corruption/run mismatch/no global-current; copied-byte inspection; durable-member redaction; `valid > invalid > missing > hard_loss`; and full controller-exit table over trusted wrapper/core, command-started/finished sentinels, execution conclusion, cancellation and download state. Invalid evidence never exposes routing output as trusted numeric exit; all unavailable reasons and root-aware actionable remediations snapshot. Rollback: disable workflow evidence route; local reports remain. Completion evidence: cross-directory golden bundle/inspect validation and trusted valid/invalid/missing/hard-loss/exit-reason snapshots. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.13 Implement canonical CLI, direct application orchestration, and root/facade contracts. Objective: expose one seven-command supervisor-first grammar with deterministic behavior, direct bootstrap-to-application launch, parser/render-only CLI, one candidate owner, one closed final-evidence owner, one scheduled projection owner, and an acyclic complete module graph. Inputs: use cases, protocol, sealed `BootstrapResultV1`, root routing, optional dependency group, and reviewed DAG. Outputs: parser/renderer-only `cli.py`, launched non-spawning controller `application.py`, candidate `report_store.py`, stdlib-only `evidence_finalizer.py`, scheduled-only `projection.py`, pre-`uv` dispatch, facade, help/renderers. Affected contracts: all seven commands, inspect kinds, option applicability, plan-only, bootstrap/application/candidate/core/wrapper/projection ownership, typed remediation, DAG, and exits. Validation: golden parity covers every command/state; run-root and bundle inspect matrices are separate; every typed remediation uses real facts, has no placeholder/shell operator, parses, and executes or reports no automatic remediation. Bootstrap tests prove exactly one sealed DTO is handed directly to `application.py`, never CLI; application alone coordinates use cases and candidate rendering; supervisor plus closed finalizer alone seal, create core/wrapper, and publish final generation/bundle; scheduled `projection.py` alone writes cache/trend/high-water and its immutable receipt after bundle validation; carrier adapter alone owns shared cache/aggregate/genesis/recovery APIs, while the standard artifact action only transfers an exact factual bundle. Static architecture tests enumerate direct edges and reject cycles, wrapper/report back-edges, CLI business logic, bootstrap/application final publication, finalizer controller imports, projection wrapper mutation, duplicate owners, or controller spawn/listener primitives. Rollback: remove additive modules. Completion evidence: CLI/remediation snapshots, direct handoff/post-seal finalization tests, projection receipt tests, and complete import/ownership graph. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:test]

## 3. CI and Documentation

- [ ] 3.1 Add report-only GitHub Actions routing with strict cross-job evidence validation. Objective: provide hosted plan-only routing and enable execution only after prerequisites, while structurally separating PR, scheduled authority, and manual factual/reconcile runs. Inputs: canonical command, finalized bundle metadata, readiness/capacity evidence, genesis candidate/CAS policy, carrier adapter, projection process, outer equations, and quotas. Outputs: `.github/workflows/mutation-testing-pr.yml`, `.github/workflows/mutation-testing-scheduled.yml`, `.github/workflows/mutation-testing-manual.yml`, default-disabled switches, evidence validation, scheduled projection receipt, and protected reconcile route. Affected contracts: CI authority, credentials, timeouts, cache/trend/projection ownership, bundle retention, recovery, and trusted summary. Validation: static tests assert hosted plan-only fallback; execution runner preflight; explicit base; literal crons; changed 15, core 45, full 110 equations; report-only exit capture; post-seal exact wrapper/core-bound bundle upload and fresh-job validation; full trusted status/controller-exit tables. Scheduled workflow has no dispatch, is the only ordinary route with carrier credentials/API/ref authority, and invokes `projection.py` only after fresh validation with exact immutable carrier receipts. PR and manual workflows neither import nor invoke projection/carrier code. Manual core/full reject reconcile fields/apply, serialize cache not applicable, receive no shared credentials, and make zero list/read/download/upload/ref calls; reconcile alone validates exact carrier, dry-runs, enters protected approval for apply, revalidates, and publishes a newer tuple. Genesis tests upload/read-back a content-addressed candidate before CAS and bind the exact carrier. Quota inventory, old-rerun refusal, qualification, hard-loss, projection degradation, YAML parse, path filters, and no PR core/full are covered. Rollback: disable execution or remove the three workflows; local reports remain. Completion evidence: workflow/adapter/projection tests, authority/credential snapshots, zero-remote PR/manual trace, genesis-candidate fixture, trusted CI-summary/projection-receipt snapshots, and prerequisite decisions. [validates:mutation.ci-routing] [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 3.2 Add concise mutation-testing operations documentation. Objective: document rationale, exact included/deferred scopes, modes, commands, lowering-only budgets, isolation prerequisites, run/mutant/score-eligibility statuses, mutation coverage visibility, survivors, exact exceptions, matched-mutant baseline policy, CI triggers, evidence/projection ownership, and anti-gaming. Inputs: final CLI/config/report contracts. Outputs: `docs/mutation-testing.md` and README developer link. Affected contracts: developer and CI operator guidance. Validation: documentation review covers unresolved base, unsupported tool/API/kernel/runner, baseline/provenance failure and every closed non-comparability reason, no coverage versus independent mutation coverage ratio, stale/corrupt cache, source/report/journal/bundle limits and normal-capacity formula, `incomplete_outcome` null score and projection prohibition, timeout versus invalid/infrastructure classification, cancellation/forced-close/orphan cleanup, sealed journal/core/wrapper verification, missing/invalid/hard-loss artifacts and controller-exit reasons, custom-output-root inspect/repair/reconcile commands, exact equivalent exceptions, protected one-use genesis, scheduled-only shared authority and projection receipts, manual/changed zero-remote behavior, closed `operation=core|full|reconcile` inputs, quota-inventory skip, and no meaningless assertions. Every example command uses a real documented placeholder syntax that cannot be emitted as runtime remediation, and the document states that absent readiness/qualification leaves hosted plan-only rather than weakened local execution. Review finds zero implementation inconsistencies. Rollback: remove docs/link. Completion evidence: source-cited operator-journey review, command round-trip samples, and text hygiene pass. [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:review]

## 4. Validation and Delivery

- [ ] 4.1 Run focused controller/config/CLI/workflow tests and the original stable core unit suite. Objective: prove mutation infrastructure and its selected tests are stable before a real mutation. Inputs: completed implementation. Outputs: passing focused evidence. Affected contracts: all. Validation: focused pytest, Ruff, BasedPyright, compileall, config/TOML/JSON syntax, bash syntax, and workflow static tests pass. Rollback: fix owning unit before commit. Completion evidence: command/result table. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.2 Run real bounded mutation smoke and exact capacity modes. Objective: demonstrate real high-value mutants without touching production source and produce reviewable admission evidence only on the accepted runner. Inputs: clean worktree, changed fixture, config, readiness, cold cache, exact runner. Outputs: cold changed report/wrapper/core/bundle, bounded core smoke, and exact core/full qualification; absent runner yields hosted plan-only plus hard blocker. Affected contracts: execution isolation, resource admission, capacity, post-seal bundle, projection, and reports. Validation: probes prove bounded pre-start listener transfer and supervisor first/only consumption by attacking duplicate/SCM_RIGHTS/competing consumer, plus process-control isolation, freezer, and genesis policy. Protocol-failure probes disconnect after fork before cgroup move, after move before registration ACK, and after ACK and confirm all spawned children reaped. Changed stays <=150/600 and zero remote. Journal proves 128-invocation plus ten-per-mutant ordinary demand, 1029-byte ordinary frame, 1932/10432/50432 mode-entry and 2,921,916/11,668,416/52,828,416-byte demand, 304-entry/1,246,704-byte terminal reserve, and seal rejection. Qualification executes exactly N serial plus the same N capacity under one deadline/outer timeout, uses identical runtime 80% memory admission, and proves 248/1152/2032 MiB pre-margin plus 760/1664/2544 MiB available-space bounds. Reports prove acyclic core/wrapper/report binding, mixed-run null score, independent mutation coverage ratio, structured baseline refusal, status precedence, dual inspect matrices, typed remediation, durable-surface redaction, and post-application seal/finalization. Scheduled smoke proves genesis candidate upload/read-back before CAS, max UTC lineage, and an immutable complete/degraded projection receipt; manual core proves zero projection/carrier credentials/calls. Rollback: discard ignored reports/cache and keep execution disabled. Completion evidence: validated bundles/summaries/projection receipts, adversarial isolation/protocol/journal probes, exact-2N proposals, journal/disk/runtime tables, authority traces, runner evidence or blocker, and unchanged source hashes. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.3 Run repository gates and record the known full-suite baseline. Objective: ensure no production behavior change and distinguish pre-existing debt. Inputs: final diff. Outputs: quality/test evidence. Affected contracts: existing repository. Validation: `./trade dev check --show-plan`, `./trade dev check`, focused and stable core pytest, `python -m compileall trade_py tests`, `git diff --check`, and full pytest run; only the pre-existing `test_verify_reports_current_then_stale` failure may remain unless upstream fixes it, and any new failure blocks delivery. Rollback: fix or revert mutation-owned files. Completion evidence: before/after failure comparison. [validates:mutation.narrow-scope] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.4 Run the six-role implementation review on one frozen commit, resolve every P0 and implementation-blocking P1, refresh strict approval if governed artifacts changed, then commit/push/open a PR. Objective: deliver one isolated reversible feature branch. Inputs: frozen diff and validation evidence. Outputs: final consensus, strict pass, intentional commits, remote branch, and PR. Affected contracts: developer mutation testing only. Validation: review cites actual code and evidence, reaches consensus with zero P0 and no implementation-blocking P1, confirms a clean worktree and required commit trailers, verifies execution remains disabled when runner evidence is absent, and records the project-local GitHub CLI PR result. Rollback: close PR/revert commits and disable workflow execution; no data rollback. Completion evidence: commit SHAs, review disposition, gate output, readiness state, and PR URL. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]
