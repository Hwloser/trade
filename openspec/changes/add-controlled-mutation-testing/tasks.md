# Tasks

## 1. Governance

- [ ] 1.1 Complete the repository audit and diagnostic design check. Objective: freeze actual language/build/test/coverage/CI/core/exclusion/baseline facts and validate governed artifacts. Inputs: current source tree, pyproject, entrypoint, tests, remote, Cosmic Ray 8.4.6 APIs, full and mapped test timings. Outputs: proposal, design, spec, tasks, and `design-quality.toml`. Affected contracts: mutation CLI, report contract, process lifecycle, and CI routing. Validation: `openspec validate add-controlled-mutation-testing --strict` and `./trade dev design-check add-controlled-mutation-testing` pass with zero findings. Rollback: revise design-only files. Completion evidence: audit records 1161 pass/1 stable failure full baseline and the exact six-file 92-test mapped cohort/core 90-test collection with no real-data access. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 1.2 Run the six-role design review in a dedicated review worktree. Objective: challenge operator safety, deterministic scope, process-tree termination, concurrency, score integrity, CI cost, and temporal/news exclusions. Inputs: frozen governed artifacts and actual repository paths. Outputs: six source-cited reports, consensus, resolved P0/P1 design findings, and `design-review.toml`. Affected contracts: all controlled mutation requirements. Validation: every role reviews the exact digest, reports source-cited findings, reaches consensus with no unresolved P0, and resolves each P1 or assigns a named owner. Rollback: revise artifacts and repeat one review round. Completion evidence: digest-bound panel review record. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]

- [ ] 1.3 Obtain strict design approval before implementation. Objective: bind code to the reviewed generation. Inputs: six-role attestation. Outputs: strict approval. Affected contracts: all. Validation: review evidence names the verified commit and digest, and `./trade dev design-check add-controlled-mutation-testing --strict` returns zero findings. Rollback: do not implement if stale or failing. Completion evidence: current-date verified commit/digest and zero findings. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:review]

- [ ] 1.4 Gate execution on the external runner child change. Objective: prevent a nonexistent or weak runner label from becoming an implicit implementation dependency. Inputs: GitHub repository owner `huanwei1208`-accountable `provision-trade-mutation-v1-runner` change, named platform operator, immutable image digest, scale-set definition, and readiness evidence. Outputs: reviewed prerequisite reference and default-disabled execution switch. Affected contracts: runner identity, CI admission, delegated cgroup, kernel/process-control isolation, protected genesis namespace, and schedule enablement. Validation: three consecutive readiness jobs prove pickup within five minutes, one-job destruction, finite delegated cgroup v2 including freezer, private controller PID/mount namespace and procfs, `PR_SET_DUMPABLE=0`, empty capabilities, process-control seccomp denials, Landlock/seccomp notification/addfd, no production mounts/secrets, cleanup, and create-only/no-update/no-delete rules for `refs/tags/trade-mutation-genesis-v1/*`; changed execution additionally requires one exact cold-cache 150-mutant/600-second smoke on that ready profile, while scheduled core/full separately require task 2.11 capacity qualification; absent evidence leaves only hosted plan-only CI and cannot queue execution. Rollback: keep execution switch disabled and remove no local controller capability. Completion evidence: child-change approval, named operator acknowledgement, image digest, readiness run URLs, process-control/freezer/genesis-policy probe bundle, changed-smoke bundle, accountable owner acknowledgement, and per-mode enabled decision. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.ci-routing] [validation:review]

## 2. Controller and Configuration

- [ ] 2.1 Add the exact mutation dependency group and closed configuration parser. Objective: make Cosmic Ray 8.4.6 and coverage.py 7.10.7 API/operator/traversal compatibility, the exact seven-source definition/test/clock/entropy/dependency-manifest matrix, lowering-only mode/source/AST/import-closure/private-tree/report/retention/journal/bundle budgets, safe output root, reviewed 512-MiB controller/1024-MiB baseline bootstrap caps, one immutable trend genesis marker plus create-only consumption-ref name per configuration epoch, and mutant-ID exceptions machine-checkable. Inputs: approved spec and actual paths. Outputs: `pyproject.toml`, lockfile, `config/mutation-testing.toml`, `config/mutation-baseline.json`, `config/mutation-exceptions.toml`, parser, and tests. Affected contracts: mutation config v1. Validation: focused tests reject unknown keys, broader overrides, missing APIs/operators/definitions/tests, unsupported `fixed` clock/entropy, undeclared temporal/entropy/dynamic imports, any unreviewed row-field change, broad paths, unsafe output roots/symlinks, non-finite/raised bootstrap caps, invalid journal entry/count/byte/reserve or bundle bounds, missing/duplicate/reused/mismatched genesis markers/ref names, wildcards, ambiguous/stale exceptions, unsupported tool versions, and unsafe limits; exact exceptions remain selected `equivalent_exception` records rather than `exception_filtered`; exact operator-local `get_ast`/`ast_nodes`/`MutationVisitor` compatibility and valid config parse; Ruff, BasedPyright, TOML, and JSON checks pass. Rollback: remove optional group/config/parser. Completion evidence: focused pytest and syntax-check results. [validates:mutation.first-order-selection] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.2 Implement deterministic Git/source/test/mutant planning. Objective: seal and revalidate base/merge-base/HEAD/index/staged/worktree/diff identity, select only versioned canonical mutation positions on changed lines without broad fallback, report unlisted/untracked production paths as deferred, keep core/full on the exact definition matrix, and enumerate first-order allowlisted positions under file/byte/AST/dependency/visit/candidate/deadline budgets. Inputs: config, explicit base, exact Cosmic Ray traversal, tracked regular UTF-8 source. Outputs: `GitScopeSnapshot`, `CanonicalMutationPositionV1`, deferred inventory, unique nested definition/test map, execution/definition occurrence identities, deterministic plan, closed scanned-reason counts, and zero-work behavior. Affected contracts: changed/core/full planning, first-order invariant, and candidate algebra. Validation: temporary Git repository tests cover add/modify/content-rename/rename-only/delete, index/worktree drift before copy/publication, deferred untracked/unmapped paths, no eligible work, replacement/compound-token/`AddNot` insertion byte offsets including Unicode, ambiguous canonicalization rejection, multiline refusal and no enclosing-definition fallback, nested definition/lambda ownership, source/graph/visit ceilings, deadline checkpoints, `candidate_positions_scanned = selected + not_selected_scanned`, generated equals selected, unknown unscanned remainder, deterministic truncation, definition exclusions, and multi-mutation rejection. Rollback: remove planners. Completion evidence: focused planner pytest and versioned plan snapshots. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validation:test]

- [ ] 2.3 Implement the pre-`uv` sole-spawner supervisor and bootstrap contract. Objective: establish one `CLOCK_BOOTTIME`/boot-ID deadline domain, run ID, bounded receipt, sole OS spawn ownership, non-filesystem controller/supervisor isolation, authenticated bounded work/control lanes, one-use bootstrap handoff, terminally sealed supervisor attestation, controller-loss fallback, and cleanup evidence before any project child. Inputs: approved mode constants, output-root contract, absolute Python 3.7+ interpreter, shell command-entry uptime anchor, and process-control/write-denial policy. Outputs: `bootstrap_contract.py`, Python 3.7-compatible `protocol.py`/`supervisor.py`, `BootstrapResultV1` transition FD/read-only memfd handoff, per-run channel capability and peer authentication, priority multiplexer, receipt transition protocol, cgroup/process-group owner, sole fallback writer, 4096-byte-entry 2048/12288/61440-entry and 8/48/192-MiB journal with 32-entry/128-KiB terminal reserve, final head/count/sequence/size seal, guard verifier/forced-close entries, and focused tests. Affected contracts: bootstrap/application runtime ownership, invocation receipt, deadlines, protocol v1, process ownership, attestation provenance, controller-loss recovery, and cleanup truth. Validation: syntax tests run under Python 3.7; static/runtime tests prove controller modules contain no spawn primitive and bootstrap/report ownership is unique; cover interpreter resolution/version rejection, no pre-supervisor `uv`, one DTO/EOF/digest/sealed-memfd consumption and replay/duplicate/trailing-byte rejection; HMAC/peer/replay/request/argv/environment/frame/8-FD-role rejection; one client/no reconnect; `workers` in-flight/active, `2*workers` work-lane saturation, and reserved `2*workers+8` priority cancellation/wait/fallback/seal capacity; `PR_SET_DUMPABLE=0`, private PID/procfs, empty capabilities and denied `ptrace|process_vm_*|pidfd_open|getfd|kcmp|cross-namespace signal` attempts even with the channel key; immediate spawn-before-registration journal entry; placement failure cleanup; unique-cgroup OOM; journal ordinary/reserve saturation, torn frame, valid-prefix truncation, append-after-seal, missing/extra/reordered entries and receipt/fallback seal mismatch; bootstrap/controller crash, D-state/cleanup-unconfirmed, fallback, and no false killed/clean result. Rollback: remove supervisor/protocol/bootstrap contract and root dispatch hook. Completion evidence: focused process tests show one OS spawner/receipt/fallback/attestation owner, one bootstrap-to-application DTO consumption, source-exclusive sealed journal, priority cleanup capacity, and no ordinary supervisor-managed descendant after completion. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.4 Implement private source trees, mutation and coverage adapters. Objective: build a bounded reviewed import closure, prove private-tree provenance, apply exactly one position-verified Cosmic Ray mutation, and retain independently recomputable target-only line evidence. Inputs: deterministic plan, matrix/dependency manifest, tool compatibility, and supervisor deadline. Outputs: private-tree builder, engine adapter, minimal hashed coverage rcfile, `CoverageTupleEvidenceV1`, provenance plugin, source restore/purge logic, and tests. Affected contracts: first-order proof, source immutability, coverage/no-coverage classification, clock/entropy cache eligibility, and private-tree capacity. Validation: tests cover canonical-position/occurrence/span handshake, source drift, original hashes, closure manifest and 10x unrelated modules, alias-aware clock/entropy/static import detection and unresolved dynamic-import non-cacheability, same-size fixed-mtime bytecode isolation, exact mapped tests, multiple imported modules with one filtered target, cleared `COVERAGE_*`, malformed/missing/duplicate/oversized coverage, tuple source/node/argv/rcfile/environment/provenance/version/executed-line digest persistence, bundle recomputation of `no_coverage_line`, missing matrix mapping validation failure, baseline-unavailable, private-tree file/byte/copy/free-space limits, and unchanged checkout hashes. Rollback: remove tree/engine/coverage modules. Completion evidence: focused tests mutate only temporary copies and produce deterministic plans plus independently validated coverage tuples. [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.5 Implement fail-closed Linux worker isolation. Objective: deny and independently observe network, process creation, and non-private data access without pathname races, controller-forgeable evidence, or forced-close notification races. Inputs: private tree, reviewed allowlists, worker identity, policy digest, supervisor-owned cgroup/start gate/freezer, and supervisor attestation sink. Outputs: Landlock launcher, seccomp user-notification broker, `SCM_RIGHTS` listener handshake, controller-side `openat2` plus `SECCOMP_IOCTL_NOTIF_ADDFD`, child write-only guard stream whose read end belongs only to the supervisor, controller-private live syscall audit, supervisor-completed forced-close audit, freeze/drain/terminate/final-drain transfer, and tests. Affected contracts: kernel isolation, descriptor ownership, guard/audit integrity, forced-close drain, and plan-only fallback. Validation: tests cover listener/tracee identity and FD closure, ACK/liveness failure, brokered allowlisted opens, symlink/pathname races, real SQLite/pandas-pyarrow/os.open/Path.open/mmap/DuckDB denial, caught Python/native violations overriding exit 0/1, credential/proxy scrubbing, reviewed native thread clone, denied process clone/fork/exec/network, complete/empty/truncated/duplicate/wrong-token streams, controller lacking guard verifier and attestation writer, normal listener drain, and every forced-close ordering: freeze/read-back, frozen drain to EAGAIN, unchanged membership, supervisor audit completion, TERM/cgroup.kill without unfreeze, wait/reap/empty cgroup, final drain/guard EOF; inject notification between old empty-check and kill, after frozen boundary, during kill, and before listener close and require either recorded denial or `incomplete_evidence`; reject unsupported/failed freeze, membership drift, invalid notification IDs, late/missing ACK, listener loss, natural exit/budget use, and unconfirmed cleanup. Rollback: remove isolation modules and leave execution disabled, not weakened. Completion evidence: focused isolation tests expose no real-data/network/process path, prove one owner for each descriptor/drain transition, and demonstrate no enqueue window between authoritative drain and termination. [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.6 Implement bounded scheduling and terminal classification. Objective: admit work by CPU plus finite bootstrap and measured per-worker cgroup memory controls, enforce deadlines/output bounds, and assign one truthful terminal/phase/cleanup/score tuple under every race. Inputs: plan, reviewed bootstrap caps, tuple `memory.peak`, supervisor opaque-handle API, isolated launcher, and closed model. Outputs: logical scheduler, finite queue, supervisor-handle client, sticky-integrity typed state machine, process-tree cancellation requests, immutable outcomes, run-score eligibility decision, and cache eligibility facts. Affected contracts: workers, memory bootstrap, timeout, cancellation, OOM, fifteen-terminal algebra, phase/cleanup facts, nine-state run precedence, score precedence, and result cache identity. Validation: tests cover effective CPU, finite bootstrap caps and transition to measured reservations, cgroup read-back, no-delegation plan-only, native threads one, baseline reasons and timeout, changed 60-second cap, deadline admission, bounded output, all control races and forced-close handoffs, every normative terminal x four phase facts x cleanup tuple, factual phase equations, all nine run statuses with pairwise/table-driven precedence, ties and causation retention, and score-ineligibility precedence `report_invalid > integrity_override > cleanup_unconfirmed > degraded_infrastructure > incomplete_outcome > zero_denominator`; every mixed case `killed|survived + timeout|cancel|baseline_unavailable|invalid|infrastructure|not_run_plan|not_run_budget|not_run_cancelled` retains factual counts but nulls score and forbids cache/baseline/trend/aggregate; only valid no-coverage and exact selected equivalent exceptions may be excluded from an otherwise complete partition; exact pytest exits, OOM/SIGKILL, cleanup override, no full-suite fallback, and every closed terminal. Rollback: remove scheduler/executor modules. Completion evidence: focused bootstrap/race/count/precedence tests leave no ordinary child, independently recompute every fact, and never classify or project an incomplete outcome as killed/score-eligible/cacheable. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.7 Implement generation-atomic report storage and recovery. Objective: publish one bounded root-hash-bound report generation under an independent report sequence, preserve sole-supervisor sealed fallback/attestation truth, and provide safe deterministic inspection/repair. Inputs: immutable execution/coverage records, sealed receipt and supervisor-attestation journal, sealed Git scope, safe output root, closed error registry, and render limits. Outputs: report models/renderers/store, leases, redaction, report reservation/tombstone, exact `current.json`, fallback request, quarantine, invocation-kind-aware raw-evidence retention DAG/evidence tombstones, inspect/repair APIs, and tests. Affected contracts: `trade.mutation.report.v1`, invocation/attestation/fallback/manifest/inspect/repair/evidence-tombstone/error-code schemas, status/count/score algebra, and exits. Validation: golden tests cover every terminal/phase/cleanup tuple, all mixed runs and nine-state precedence, factual numerator/denominator plus complete `incomplete_outcome` eligibility, coverage recomputation, journal per-entry/count/byte/reserve limits and receipt/fallback `head|count|final_sequence|byte_size|sealed` anchor; reject valid prefixes, torn tails, append-after-seal, missing/extra/reordered entries and seal-before-crash; manifest/pointer sequence recovery; interruption at every sync/rename boundary; fallback/controller loss; custom-root inspect/repair; all inspect states/exits; retention tombstones; Git drift, safe-root/lease races, redaction and render/bundle limits. Every remediation argv is generated with actual mode/base/output-root/carrier/bundle identity, parsed back through canonical CLI, and either executes in a golden temp root or stops at its explicit approval boundary; no default-root or placeholder command is accepted. Rollback: remove report store/render/recovery modules while preserving ignored evidence for manual audit. Completion evidence: every run status exposes one sealed receipt- and attestation-bound generation or supervisor-owned typed fallback and never stale current, legal-prefix journal, or unrooted evidence. [validates:mutation.truthful-report] [validation:test]

- [ ] 2.8 Implement disposable committed result cache. Objective: accelerate exact repeated outcomes without treating cache as evidence, weakening truth, or consuming an unreserved changed/manual remote phase. Inputs: published score-eligible generation, complete cache identity, staged eligible fresh outcomes, invocation kind, and output lock. Outputs: content-addressed entries, run commit markers, deterministic local eviction, scheduled-core/full-only bounded remote archive import/export and restore diagnostics, and tests. Affected contracts: cache identity/eligibility/retention and outer CI budgets. Validation: tests cover uncommitted/partial/degraded/integrity-override/cleanup-unconfirmed/score-ineligible/stale/corrupt/clock-entropy-ineligible misses, same-key races, exact marker binding, 20000-entry/512-MiB eviction; changed and manual invocations make zero remote cache list/read/download/upload calls; scheduled core/full inspect at most 20 remote candidates, download at most two/512 MiB/180 seconds, and record numeric run-attempt ordering plus attempted-carrier/rejection/count/byte/elapsed/truncation diagnostics. Before upload, namespace-aware stable pagination must prove at least the newest 15 cache-relevant records and the complete seven-day window; incomplete inventory skips upload without deletion, while a complete inventory enforces 14 artifacts/7 GiB. Tests also cover seven-day retention metadata and correctness with no cache. Rollback: remove cache projection; reports remain authoritative. Completion evidence: local/scheduled/manual cache hit/miss/eviction/restore/quota snapshots, zero-remote changed/manual trace, and incomplete-pagination skip proof. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.9 Implement trend ledger, aggregate, and artifact-aware reconciliation. Objective: make scheduled core/full trend continuity auditable under an independent sequence and bounded remote carriers while manual runs remain cold factual diagnostics. Inputs: published score-eligible scheduled report root/sequence, score/key-set digests, workflow tuple, reviewed epoch genesis marker/create-only ref, UTC anchor, explicit reconcile carrier bytes, and output lock. Outputs: trend reservation/source/tombstone chain, high-water/checkpoint, `GenesisConsumptionReceipt`, `trend.jsonl`, bounded aggregate validator, restore diagnostics, `reconcile --carrier` service and repaired-carrier proposal, and tests. Affected contracts: trend sequence/epoch/genesis/projection/retention/reconciliation schemas. Validation: tests cover independent report/trend sequences; changed/plan/manual/score-ineligible runs performing no cache/aggregate listing, consuming no trend sequence and publishing no shared carrier; first scheduled writer atomically creates/read-backs the protected consumption ref; concurrent CAS accepts only identical bytes; aggregate-upload failure, 90-day expiry, pause and deletion cannot reuse genesis; missing/mutable/mismatched ref becomes `predecessor_invalid`; later missing carrier becomes `predecessor_unavailable`; high-water/hash-chain recovery, monotonic tuple and old rerun refusal, corrupt-highest no fallback, scheduled restore limits, 64-MiB aggregate, scheduled-only upload, namespace-aware 91-record aggregate quota proof and incomplete-inventory skip, detail-expired continuity, explicit reconcile carrier validation, dry-run before apply, custom-root local proposal without upload, and no fabricated predecessor/sequence/score. Rollback: remove trend projection/aggregate while reports remain readable; never delete a genesis consumption ref. Completion evidence: deterministic scheduled/manual/genesis-CAS/rerun/retention/reconcile golden fixtures. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.10 Implement baseline and exception governance. Objective: evaluate score comparison only from a verified score-eligible detail bundle and allow only exact expiring exceptions. Inputs: reviewed baseline/exceptions, immutable bundles with coverage tuples and supervisor attestation, comparison identities, and trusted UTC anchors. Outputs: detail-recomputing baseline evaluator, score-eligibility gate, exception validator, paired realtime/boottime temporal validator, and tests. Affected contracts: `trade.mutation.baseline.v1`, exception registry, gradual policy, run-score eligibility, and temporal high-water. Validation: tests cover bundle/report/run/commit/tool/environment/key-set/attestation and coverage-line binding, stored aggregate/detail-expired/score-ineligible refusal, factual numerator/denominator preservation with null displayed score, exact comparison/denominator/exception/no-coverage sets, `CanonicalMutationPositionV1` plus operator arguments and execution occurrence, wildcards/stale source rejection, RFC3339 future/five-minute skew/within-run and cross-run rollback/reboot/missing-anchor cases, and 180-day interval. Rollback: leave baseline unestablished and exception registry empty. Completion evidence: baseline, exception, score-eligibility, and temporal golden decisions. [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.11 Implement capacity qualification governance. Objective: admit every core/full execution only on a fresh representative qualified runner, validate changed separately, and preserve outer-phase CPU/memory/disk reserves. Inputs: reviewed capacity file, immutable bundles, exact runner identity, tuple distribution with p95 time and `memory.max`, bootstrap caps, and trusted UTC. Outputs: `capacity.py`, serial/capacity proposal/validator, deterministic dual-resource list-schedule projection, changed-smoke validator, outer phase-budget validator, and tests. Affected contracts: `trade.mutation.capacity.v1`, per-mode admission, execution time/concurrent-memory/disk headroom. Validation: tests cover every row/tuple and cohort minimum, tuple p95 spawn/admission/execution/finalization/cleanup plus enforced memory cap, stable list schedule admitting only when worker slot and `sum(concurrent memory.max)` fit 80% effective memory, adversarial alternating large/small tuples, tail using slowest time/largest memory, deterministic ties, and rejection when CPU-only schedule would pass but memory-constrained schedule misses deadline; finite bootstrap measurement/OOM/cap identity, transition to measured peaks; disk includes 8/48/192-MiB journal, 64/160/416-MiB bundle, same-sized read-back copy and scratch; changed `2+10+2+1=15` with zero remote API, core/full equations, current/expired/mismatched qualification, trusted UTC, and proposal never edits reviewed files. Rollback: leave capacity false and execution disabled. Completion evidence: changed-smoke and representative core/full dual-resource qualification/phase-budget golden decisions. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.12 Implement immutable CI bundle construction and independent validation. Objective: package exact sealed receipt/attestation evidence and validate downloaded bytes without path trust. Inputs: receipt with final journal anchor, complete sealed journal, referenced generation/fallback, coverage tuples, workflow identity, invocation expectation, and error registry. Outputs: `bundle.py`, bounded `trade.mutation.bundle.v1`, local validator, artifact outputs, `trade.mutation.ci-summary.v1`, fresh-directory validator, artifact-aware inspect, and tests. Affected contracts: attestation/bundle/validation/CI-summary schemas, retention membership, and evidence integrity. Validation: tests cover exact journal head/count/sequence/size/seal and generation/fallback/coverage binding; reject legal prefixes, torn/extra/post-seal bytes; mode bundle caps 64/160/416 MiB; local not-applicable versus CI required; extra/path escape/corruption/run mismatch/no global-current; copied-byte inspection; `valid > invalid > missing > hard_loss`; and full controller-exit table over trusted receipt, command-started/finished sentinels, execution conclusion, cancellation and download state. Invalid evidence never exposes routing output as trusted numeric exit; all unavailable reasons and root-aware actionable remediations snapshot. Rollback: disable workflow evidence route; local reports remain. Completion evidence: cross-directory golden bundle/inspect validation and trusted valid/invalid/missing/hard-loss/exit-reason snapshots. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.13 Implement canonical CLI, application orchestration, and root/facade contracts. Objective: expose one seven-command supervisor-first grammar with deterministic argv/stdout/stderr/receipt/exit behavior, one bootstrap-to-application handoff, one report owner, and an acyclic complete module graph. Inputs: controller use cases, supervisor protocol, sealed `BootstrapResultV1` memfd, root `trade` routing, optional frozen dependency group, and reviewed import DAG. Outputs: parser/renderer-only `cli.py`, use-case-only `application.py`, pre-`uv` `trade dev mutation` dispatch, all-command `scripts/mutation-test` facade, help and text/JSON renderers. Affected contracts: `changed|core|full|inspect|repair|qualify|reconcile`, `inspect --bundle`, run-root `--output-dir`, `reconcile --carrier`, option applicability, lowering-only options, plan-only, verbosity, bootstrap/application/report ownership, schemas, error registry, module dependency graph, and exits. Validation: golden tests cover root/facade argv/output/exit parity for every command/state, option rejection, help, mutually exclusive quiet/verbose, JSON single-document stdout and diagnostic-only stderr, text/TTY/no-ANSI behavior, bounded progress, receipt export, plan-only, run inspection/repair under a custom owned output root, immutable bundle inspection without original runner/dependencies, all eight inspect states, repair, qualification statuses, artifact-aware reconcile dry-run/apply statuses, missing dependency/preflight diagnostics, no pre-supervisor `uv`, frozen uv forwarding, Bash syntax and ShellCheck. Bootstrap/application tests prove exactly one `BootstrapResultV1` is read to EOF, digest-verified, sealed read-only, consumed once, and handed only to `application.py`; only `application.py` invokes the report-store use case, while bootstrap, CLI, adapters, and renderers cannot publish a report. Every remediation command is generated from actual mode/base/output-root/carrier/bundle facts, round-trips through the canonical parser, and either runs in a golden custom root or stops at the named approval boundary; placeholder/default-root remediation is rejected. Static architecture tests enumerate every first-party module/direct edge, allow only the reviewed DAG, make `application.py` the orchestration owner, and reject cycles, adapter-to-CLI calls, CLI business logic, duplicate report owners, or controller spawn primitives. Rollback: remove additive command/facade/application module. Completion evidence: CLI golden/parity/remediation snapshots, one-use bootstrap handoff test, and complete import/ownership graph test. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:test]

## 3. CI and Documentation

- [ ] 3.1 Add report-only GitHub Actions routing with strict cross-job evidence validation. Objective: provide hosted plan-only routing immediately and enable each execution mode only after its runner/readiness/capacity prerequisites, without making mutation outcomes or full a PR gate. Inputs: canonical command, invocation receipt/attestation/bundle artifact metadata, task 1.4 readiness and changed-smoke decision, reviewed core/full capacity file, epoch genesis marker and protected consumption-ref policy, outer phase equations, remote quotas, and rolling aggregate contract. Outputs: `.github/workflows/mutation-testing.yml` with default-disabled execution switches and an environment-approved reconcile recovery route. Affected contracts: CI events, runner readiness/preflight, exact concurrency semantics, outer timeouts, result cache versus trend ownership, per-mode capacity admission, bundle retention, recovery publication, and trusted summary behavior. Validation: static workflow tests assert ordinary hosted plan-only when execution is disabled and never claim the execution runner; enabled execution routes alone use `runs-on: [self-hosted, linux, x64, ephemeral, trade-mutation-v1]`; no secrets for fork PR; image/private-PID-procfs/mount/capability/process-control/delegated-cgroup-freezer/bootstrap-memory/Landlock/seccomp preflight; explicit base SHA; literal crons; changed remote list/read/download/upload absent and equations exactly `2+10+2+1=15`, core `4+3+30+5+3=45`, full `4+3+90+10+3=110`; changed gated by readiness plus exact smoke but not the core/full qualification record; real `${{ github.repository }}`/PR expressions; PR cancellation; scheduled/manual/reconcile shared long group with `cancel-in-progress: false`; report-only execution exit capture; untrusted routing output label; execution-job `if: always()` local sealed-attestation-bound bundle validation/upload; separate job fresh-directory byte validation and normative valid/invalid/missing/hard-loss `trade.mutation.ci-summary.v1`. Manual dispatch uses exactly `operation=core|full|reconcile`: core/full reject every reconcile field and `apply=true`; reconcile requires exact `trend_epoch`, `source_run_id`, `source_run_attempt`, `artifact_name`, `expected_sha256`, and boolean `apply`, runs dry-run first, and permits apply only through the named GitHub environment. The complete controller-exit truth table binds trusted receipt, command-started/finished sentinels, execution conclusion, workflow cancellation, artifact presence/download, and validation state; invalid evidence never promotes an untrusted routing output to numeric `controller_exit`, and every null reason is deterministic. Fourteen-day bundle retention, mode caps, and complete journal seal are checked. All core/full execution requires qualification, but scheduled core/full alone may list/restore shared carriers, atomically create/read-back the protected genesis consumption ref, reserve trend sequence, advance high-water, or upload cache/aggregate; manual runs perform no remote list/read/download and publish factual bundles only. Before upload, namespace-aware pagination must prove at least the newest 15 cache-relevant and 91 aggregate-relevant records were completely inventoried; truncation or incompleteness skips upload without deletion. Old-workflow reruns are monotonic refusals. Artifact-aware reconcile binds exact name/digest, revalidates after approval, and publishes only a strictly newer tuple. Capacity/runner readiness refusal, whole-workflow cancellation best effort, and no PR core/full are explicit; YAML parses if available. Rollback: disable execution switches or remove workflow; local reports remain. Completion evidence: workflow pytest, config parse, hosted plan-only snapshot, complete controller-exit/hard-loss table, dispatch cross-field snapshots, zero-remote manual/changed trace, quota pagination fixtures, protected-genesis CAS fixture, manual/scheduled/reconcile authority snapshots, and prerequisite-gated execution snapshot. [validates:mutation.ci-routing] [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 3.2 Add concise mutation-testing operations documentation. Objective: document rationale, exact included/deferred scopes, modes, commands, lowering-only budgets, isolation prerequisites, run/mutant/score-eligibility statuses, survivors, exact exceptions, matched-mutant baseline policy, CI triggers, evidence ownership, and anti-gaming. Inputs: final CLI/config/report contracts. Outputs: `docs/mutation-testing.md` and README developer link. Affected contracts: developer and CI operator guidance. Validation: documentation review covers unresolved base, unsupported tool/API/kernel/runner, baseline/provenance failure, no coverage, stale/corrupt cache, source/report/journal/bundle limits, `incomplete_outcome` null score and projection prohibition, timeout versus invalid/infrastructure classification, cancellation/forced-close/orphan cleanup, sealed journal verification, missing/invalid/hard-loss artifacts and controller-exit reasons, custom-output-root inspect/repair/reconcile commands, exact equivalent exceptions, protected one-use genesis, scheduled-only shared authority, manual/changed zero-remote behavior, closed `operation=core|full|reconcile` inputs, quota-inventory skip, and no meaningless assertions. Every example command uses a real documented placeholder syntax that cannot be emitted as runtime remediation, and the document states that absent readiness/qualification leaves hosted plan-only rather than weakened local execution. Review finds zero implementation inconsistencies. Rollback: remove docs/link. Completion evidence: source-cited operator-journey review, command round-trip samples, and text hygiene pass. [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:review]

## 4. Validation and Delivery

- [ ] 4.1 Run focused controller/config/CLI/workflow tests and the original stable core unit suite. Objective: prove mutation infrastructure and its selected tests are stable before a real mutation. Inputs: completed implementation. Outputs: passing focused evidence. Affected contracts: all. Validation: focused pytest, Ruff, BasedPyright, compileall, config/TOML/JSON syntax, bash syntax, and workflow static tests pass. Rollback: fix owning unit before commit. Completion evidence: command/result table. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.2 Run real bounded mutation smoke and capacity modes. Objective: demonstrate real high-value mutants without touching production source and produce reviewable per-mode admission evidence only on the accepted runner. Inputs: clean worktree, eligible changed fixture/branch diff, core/full config, task 1.4 readiness evidence, cold cache, reviewed bootstrap caps, serial and capacity profiles on the exact `trade-mutation-v1` runner. Outputs: exact cold-cache changed readiness report/bundle, bounded core smoke, and cold-cache representative core/full qualification evidence; if task 1.4 is unavailable, hosted plan-only evidence and a documented hard blocker instead of weakened execution or a fabricated smoke. Affected contracts: changed/core/full execution, process-control isolation, resource admission, capacity schema, immutable sealed-attestation-bound bundle, and report format. Validation: runner probes first prove private controller PID/procfs, `PR_SET_DUMPABLE=0`, empty capabilities, denied ptrace/process-vm/pidfd/kcmp/cross-namespace signal, delegated cgroup freezer, and create-only genesis namespace. Changed generated/executed stay below 150 and 600 seconds, make zero remote API calls, and satisfy `2+10+2+1=15`; bounded core preserves upper limits. A forced-close smoke injects notifications at freeze/drain/kill/final-drain boundaries and accepts only recorded denial or `incomplete_evidence`; a journal smoke proves mode entry/byte/reserve caps, final seal, legal-prefix/torn/post-seal rejection, and receipt/bundle anchor equality; a mixed-run smoke proves factual counts with null score and no cache/baseline/trend/aggregate. Qualification covers every source row/test tuple with `max(30,5*tuple_count)` and five per tuple, records tuple-specific spawn/admission/execution/finalization/cleanup p95 plus enforced `memory.max`, and uses a deterministic dual-resource list schedule whose concurrent memory is at most 80% effective memory; it includes adversarial CPU-pass/memory-fail and alternating-size projections, planning/copy/baseline/render/fsync/cleanup, finite 512-MiB controller/1024-MiB baseline bootstrap measurements and transition, journal/bundle/read-back/scratch disk headroom, and recomputed core/full 45/110 equations. Reports/bundles independently confirm complete sealed attestation, factual phase algebra, score eligibility, survivor details, cleanup, actual-root remediation round trips, and unchanged source hashes. Scheduled smoke additionally proves protected genesis CAS, complete quota pagination or typed upload skip, while manual core proves zero remote list/read/download and no shared projection. Changed may be admitted only by readiness plus this smoke; scheduled core/full require reviewed capacity; all CI execution remains disabled unless its exact prerequisites pass. Rollback: discard ignored reports/cache and keep every execution switch disabled. Completion evidence: validated bundle/CI summaries, isolation/forced-close/journal/mixed-run probe bundle, changed-smoke decision, qualification proposal, dual-resource capacity table, manual/scheduled authority traces, runner evidence or explicit blocker, and unchanged source hashes. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.3 Run repository gates and record the known full-suite baseline. Objective: ensure no production behavior change and distinguish pre-existing debt. Inputs: final diff. Outputs: quality/test evidence. Affected contracts: existing repository. Validation: `./trade dev check --show-plan`, `./trade dev check`, focused and stable core pytest, `python -m compileall trade_py tests`, `git diff --check`, and full pytest run; only the pre-existing `test_verify_reports_current_then_stale` failure may remain unless upstream fixes it, and any new failure blocks delivery. Rollback: fix or revert mutation-owned files. Completion evidence: before/after failure comparison. [validates:mutation.narrow-scope] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.4 Run the six-role implementation review on one frozen commit, resolve every P0 and implementation-blocking P1, refresh strict approval if governed artifacts changed, then commit/push/open a PR. Objective: deliver one isolated reversible feature branch. Inputs: frozen diff and validation evidence. Outputs: final consensus, strict pass, intentional commits, remote branch, and PR. Affected contracts: developer mutation testing only. Validation: review cites actual code and evidence, reaches consensus with zero P0 and no implementation-blocking P1, confirms a clean worktree and required commit trailers, verifies execution remains disabled when runner evidence is absent, and records the project-local GitHub CLI PR result. Rollback: close PR/revert commits and disable workflow execution; no data rollback. Completion evidence: commit SHAs, review disposition, gate output, readiness state, and PR URL. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]
