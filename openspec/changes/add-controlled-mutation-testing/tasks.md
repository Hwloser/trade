# Tasks

## 1. Governance

- [ ] 1.1 Complete the repository audit and diagnostic design check. Objective: freeze actual language/build/test/coverage/CI/core/exclusion/baseline facts and validate governed artifacts. Inputs: current source tree, pyproject, entrypoint, tests, remote, Cosmic Ray 8.4.6 APIs, full and core test timings. Outputs: proposal, design, spec, tasks, and `design-quality.toml`. Affected contracts: mutation CLI, report contract, process lifecycle, and CI routing. Validation: `openspec validate add-controlled-mutation-testing --strict` and `./trade dev design-check add-controlled-mutation-testing` pass with zero findings. Rollback: revise design-only files. Completion evidence: audit records 1161 pass/1 stable failure full baseline and 97/97 core baseline with no real-data access. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 1.2 Run the six-role design review in a dedicated review worktree. Objective: challenge operator safety, deterministic scope, process-tree termination, concurrency, score integrity, CI cost, and temporal/news exclusions. Inputs: frozen governed artifacts and actual repository paths. Outputs: six source-cited reports, consensus, resolved P0/P1 design findings, and `design-review.toml`. Affected contracts: all controlled mutation requirements. Validation: every role reviews the exact digest, reports source-cited findings, reaches consensus with no unresolved P0, and resolves each P1 or assigns a named owner. Rollback: revise artifacts and repeat one review round. Completion evidence: digest-bound panel review record. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]

- [ ] 1.3 Obtain strict design approval before implementation. Objective: bind code to the reviewed generation. Inputs: six-role attestation. Outputs: strict approval. Affected contracts: all. Validation: review evidence names the verified commit and digest, and `./trade dev design-check add-controlled-mutation-testing --strict` returns zero findings. Rollback: do not implement if stale or failing. Completion evidence: current-date verified commit/digest and zero findings. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:review]

## 2. Controller and Configuration

- [ ] 2.1 Add the exact mutation dependency group and closed configuration parser. Objective: make Cosmic Ray 8.4.6 and coverage.py 7.10.7 API/operator/traversal compatibility, the exact seven-source definition/test/clock/entropy/dependency-manifest matrix, lowering-only mode/source/AST/import-closure/private-tree/report/retention budgets, safe output root, and mutant-ID exceptions machine-checkable. Inputs: approved spec and actual paths. Outputs: `pyproject.toml`, lockfile, `config/mutation-testing.toml`, `config/mutation-baseline.json`, `config/mutation-exceptions.toml`, parser, and tests. Affected contracts: mutation config v1. Validation: focused tests reject unknown keys, broader overrides, missing APIs/operators/definitions/tests, undeclared clock/entropy/dynamic imports, broad paths, unsafe output roots/symlinks, wildcards, ambiguous/stale exceptions, unsupported tool versions, and unsafe limits; exact operator-local `get_ast`/`ast_nodes`/`MutationVisitor` compatibility and valid config parse; Ruff, BasedPyright, TOML, and JSON checks pass. Rollback: remove optional group/config/parser. Completion evidence: focused pytest and syntax-check results. [validates:mutation.first-order-selection] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.2 Implement deterministic Git/source/test/mutant planning. Objective: select merge-base plus staged/unstaged tracked changed lines without broad fallback, report unlisted/untracked production paths as deferred, keep core/full on the exact definition matrix, and enumerate first-order allowlisted positions under file/byte/AST/dependency/visit/candidate/deadline budgets. Inputs: config, explicit base, exact Cosmic Ray traversal, tracked regular source. Outputs: dirty-tree Git scope, deferred inventory, definition/test map, execution ID/comparison key/span, deterministic plan, and zero-work behavior. Affected contracts: changed/core/full planning and first-order invariant. Validation: temporary Git repository tests cover add/modify/content-rename/rename-only/delete, deferred untracked/unmapped paths, no eligible work, exact line/definition priority, source/graph/visit ceilings, deadline checkpoints, unknown unscanned remainder, deterministic truncation, definition exclusions, and multi-mutation rejection. Rollback: remove planners. Completion evidence: focused planner pytest and plan snapshots. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validation:test]

- [ ] 2.3 Implement isolated mutation execution. Objective: revalidate operator-local occurrence/span, apply one `get_operator` plus `mutate_code` transformation to each bounded import-closure tree, require overlay provenance and coverage.py 7.10.7 line evidence, deny network/credentials/real data/external clients, record authenticated guard violations outside application handling, let the parent own all PGIDs under one spawn/cancel gate, and classify fresh/cache/cancelled outcomes truthfully. Inputs: plan and receipt-bound hard/execution deadlines. Outputs: engine and coverage adapters, isolation owner, process supervisor, bounded scheduler, active PGID registry, complete cache identity, and immutable outcome records. Affected contracts: first-order proof, process isolation, timeout, cancellation, cache, provenance, and classification. Validation: tests prove operator-local API/span mapping, source drift, overlay import, original hashes, mapping/line no-coverage, malformed coverage data, baseline-unavailable, exact pytest exits, caught and uncaught violation sidecars overriding exit 0/1, fresh/cache killed/survived/timeout/invalid/infrastructure/cancelled states, clock/entropy cache refusal, 10x unrelated-module closure stability, native thread limits, canonical socket/real-data/external-client denial, private-tree limits, same-size fixed-mtime bytecode isolation, cancellation before/during/after spawn and while queued, worker crash/repeated signal/process-tree reaping, execution-deadline admission, output truncation, and restoration. Rollback: remove execution modules/cache. Completion evidence: focused pytest leaves no descendants, network access, real-data access, stale bytecode, modified originals, or unclassified selected mutant. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.4 Implement generation-atomic reports, baseline evaluation, and CLI facade. Objective: create `trade.mutation.invocation.v1` before every `uv` child, publish one bounded `trade.mutation.report.v1` generation with fresh/cache/plan/cancellation algebra, commit cache only after complete-run marker, derive trend from committed generations, recover corrupt pointers, apply aggregate retention, and emit `trade.mutation.fallback.v1` without changing current on failure. Inputs: execution records and config. Outputs: immutable models, renderers, report store, comparison-key baseline, committed cache, trend projection, import-light bootstrap, parent watchdog, Python CLI, and executable wrapper. Affected contracts: public developer CLI, receipt/report/fallback formats, run status, and exit codes. Validation: golden receipt/report/fallback/count tests; absolute deadline survives bootstrap/full-controller handoff; interruption at cache commit, report pointer, and trend reconciliation; first-run/malformed/symlink/missing-generation/digest pointer recovery; safe-root/lease races; redaction sentinels; per-format/generation/aggregate retention/renderer ceilings; deterministic truncation/eviction; corrupt/uncommitted cache and trend rebuild; exact `uv --frozen --no-sync` forwarding and missing-tool remediation; plan-only and comparison tests; `bash -n` and ShellCheck if installed. Rollback: remove facade/storage modules. Completion evidence: zero-work, plan-only, complete, budget-partial, cancelled, and degraded runs publish one receipt-bound consistent generation; failed stages leave one typed bounded fallback and never expose stale current as this invocation. [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validation:test]

## 3. CI and Documentation

- [ ] 3.1 Add non-blocking GitHub Actions routing. Objective: run changed only for PR eligible definitions, core at `17 18 * * 1-6` UTC/manual, and full at `17 18 * * 0` UTC/manual without making full a PR check. Inputs: script and invocation receipt paths. Outputs: `.github/workflows/mutation-testing.yml`. Affected contracts: CI events, exact concurrency semantics, outer timeouts, cache/trend, artifact retention, and summary behavior. Validation: static workflow tests assert explicit base SHA, literal crons, 15/35/100 minute limits, PR cancellation, shared `mutation-long-${repository}` with `cancel-in-progress: false`, native one-running/latest-pending semantics without lossless-queue claims, exact manual input, continue-on-error, mode isolation, receipt path through `$GITHUB_OUTPUT`, `if: always()` validation/upload that never reads global current/newest glob, 14-day retention, hard-runner-loss best effort, publication-sequence trend ordering, and no PR core/full; YAML parses if available. Rollback: remove workflow. Completion evidence: workflow pytest and config parse pass. [validates:mutation.ci-routing] [validation:test]

- [ ] 3.2 Add concise mutation-testing operations documentation. Objective: document rationale, exact included/deferred scopes, modes, commands, lowering-only budgets, run/mutant statuses, survivors, exact exceptions, matched-mutant baseline policy, CI triggers, artifact/trend recovery, and anti-gaming. Inputs: final CLI/config/report contracts. Outputs: `docs/mutation-testing.md` and README developer link. Affected contracts: developer guidance. Validation: documentation review covers unresolved base, unsupported tool/API, baseline/provenance failure, no coverage, stale/corrupt cache, source/report limits, cancellation/orphan cleanup, missing artifact, equivalent exceptions, and no meaningless assertions, with zero implementation inconsistencies. Rollback: remove docs/link. Completion evidence: source-cited review finding set and text hygiene pass. [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:review]

## 4. Validation and Delivery

- [ ] 4.1 Run focused controller/config/CLI/workflow tests and the original stable core unit suite. Objective: prove mutation infrastructure and its selected tests are stable before a real mutation. Inputs: completed implementation. Outputs: passing focused evidence. Affected contracts: all. Validation: focused pytest, Ruff, BasedPyright, compileall, config/TOML/JSON syntax, bash syntax, and workflow static tests pass. Rollback: fix owning unit before commit. Completion evidence: command/result table. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.2 Run real bounded mutation smoke modes. Objective: demonstrate the tool generates and executes real high-value mutants without touching production source. Inputs: clean worktree, one eligible changed fixture/branch diff or explicit test branch, and core config. Outputs: changed report and a small core run or plan. Affected contracts: changed/core execution and report format. Validation: `scripts/mutation-test changed` stays below 150/600 seconds and a bounded core smoke stays below its supplied lower test cap while preserving normative upper limits; report tests confirm all requested counts and survivor details. Rollback: discard generated ignored reports/cache. Completion evidence: report summaries and unchanged source hashes. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.3 Run repository gates and record the known full-suite baseline. Objective: ensure no production behavior change and distinguish pre-existing debt. Inputs: final diff. Outputs: quality/test evidence. Affected contracts: existing repository. Validation: `./trade dev check --show-plan`, `./trade dev check`, focused and stable core pytest, `python -m compileall trade_py tests`, `git diff --check`, and full pytest run; only the pre-existing `test_verify_reports_current_then_stale` failure may remain unless upstream fixes it, and any new failure blocks delivery. Rollback: fix or revert mutation-owned files. Completion evidence: before/after failure comparison. [validates:mutation.narrow-scope] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.4 Run the six-role implementation review on one frozen commit, resolve every P0 and implementation-blocking P1, refresh strict approval if governed artifacts changed, then commit/push/open a PR. Objective: deliver one isolated reversible feature branch. Inputs: frozen diff and validation evidence. Outputs: final consensus, strict pass, intentional commits, remote branch, and PR. Affected contracts: developer mutation testing only. Validation: review cites actual code and evidence, reaches consensus with zero P0 and no implementation-blocking P1, confirms a clean worktree and required commit trailers, and records the project-local GitHub CLI PR result. Rollback: close PR/revert commits; no data rollback. Completion evidence: commit SHAs, review disposition, gate output, and PR URL. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]
