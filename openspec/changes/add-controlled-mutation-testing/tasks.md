# Tasks

## 1. Governance

- [ ] 1.1 Complete the repository audit and diagnostic design check. Objective: freeze actual language/build/test/coverage/CI/core/exclusion/baseline facts and validate governed artifacts. Inputs: current source tree, pyproject, entrypoint, tests, remote, Cosmic Ray 8.4.6 APIs, full and mapped test timings. Outputs: proposal, design, spec, tasks, and `design-quality.toml`. Affected contracts: mutation CLI, report contract, process lifecycle, and CI routing. Validation: `openspec validate add-controlled-mutation-testing --strict` and `./trade dev design-check add-controlled-mutation-testing` pass with zero findings. Rollback: revise design-only files. Completion evidence: audit records 1161 pass/1 stable failure full baseline and the exact six-file 92-test mapped cohort/core 90-test collection with no real-data access. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 1.2 Run the six-role design review in a dedicated review worktree. Objective: challenge operator safety, deterministic scope, process-tree termination, concurrency, score integrity, CI cost, and temporal/news exclusions. Inputs: frozen governed artifacts and actual repository paths. Outputs: six source-cited reports, consensus, resolved P0/P1 design findings, and `design-review.toml`. Affected contracts: all controlled mutation requirements. Validation: every role reviews the exact digest, reports source-cited findings, reaches consensus with no unresolved P0, and resolves each P1 or assigns a named owner. Rollback: revise artifacts and repeat one review round. Completion evidence: digest-bound panel review record. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]

- [ ] 1.3 Obtain strict design approval before implementation. Objective: bind code to the reviewed generation. Inputs: six-role attestation. Outputs: strict approval. Affected contracts: all. Validation: review evidence names the verified commit and digest, and `./trade dev design-check add-controlled-mutation-testing --strict` returns zero findings. Rollback: do not implement if stale or failing. Completion evidence: current-date verified commit/digest and zero findings. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:review]

## 2. Controller and Configuration

- [ ] 2.1 Add the exact mutation dependency group and closed configuration parser. Objective: make Cosmic Ray 8.4.6 and coverage.py 7.10.7 API/operator/traversal compatibility, the exact seven-source definition/test/clock/entropy/dependency-manifest matrix, lowering-only mode/source/AST/import-closure/private-tree/report/retention budgets, safe output root, and mutant-ID exceptions machine-checkable. Inputs: approved spec and actual paths. Outputs: `pyproject.toml`, lockfile, `config/mutation-testing.toml`, `config/mutation-baseline.json`, `config/mutation-exceptions.toml`, parser, and tests. Affected contracts: mutation config v1. Validation: focused tests reject unknown keys, broader overrides, missing APIs/operators/definitions/tests, unsupported `fixed` clock/entropy, undeclared temporal/entropy/dynamic imports, any unreviewed row-field change, broad paths, unsafe output roots/symlinks, wildcards, ambiguous/stale exceptions, unsupported tool versions, and unsafe limits; exact operator-local `get_ast`/`ast_nodes`/`MutationVisitor` compatibility and valid config parse; Ruff, BasedPyright, TOML, and JSON checks pass. Rollback: remove optional group/config/parser. Completion evidence: focused pytest and syntax-check results. [validates:mutation.first-order-selection] [validates:mutation.gradual-policy] [validation:test]

- [ ] 2.2 Implement deterministic Git/source/test/mutant planning. Objective: seal and revalidate base/merge-base/HEAD/index/staged/worktree/diff identity, select only canonical mutation-token positions on changed lines without broad fallback, report unlisted/untracked production paths as deferred, keep core/full on the exact definition matrix, and enumerate first-order allowlisted positions under file/byte/AST/dependency/visit/candidate/deadline budgets. Inputs: config, explicit base, exact Cosmic Ray traversal, tracked regular source. Outputs: `GitScopeSnapshot`, deferred inventory, unique nested definition/test map, execution/definition occurrence identities, deterministic plan, generated-versus-scanned counts, and zero-work behavior. Affected contracts: changed/core/full planning and first-order invariant. Validation: temporary Git repository tests cover add/modify/content-rename/rename-only/delete, index/worktree drift before copy/publication, deferred untracked/unmapped paths, no eligible work, canonical changed-token admission with multiline-span refusal and no enclosing-definition fallback, nested definition/lambda ownership, source/graph/visit ceilings, deadline checkpoints, `generated_mutants = selected`, unknown unscanned remainder, deterministic truncation, definition exclusions, and multi-mutation rejection. Rollback: remove planners. Completion evidence: focused planner pytest and plan snapshots. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validation:test]

- [ ] 2.3 Implement the pre-`uv` sole-spawner supervisor and bootstrap contract. Objective: establish one `CLOCK_BOOTTIME`/boot-ID deadline domain, run ID, bounded phase-journal receipt, subreaper, sole OS spawn ownership, authenticated bounded spawn/signal/wait/fallback protocol, opaque worker handles, fork-stop cgroup admission, signal handling, controller-loss fallback, and cleanup evidence before any project child. Inputs: approved mode constants, output-root contract, absolute Python 3.7+ interpreter, and shell command-entry uptime anchor. Outputs: `bootstrap_contract.py`, Python 3.7-compatible `supervisor.py`, protocol client/server schemas, receipt transition protocol, cgroup/process-group owner, sole fallback writer, and focused tests. Affected contracts: invocation receipt, deadlines, process ownership, controller-loss recovery, and cleanup truth. Validation: syntax tests run under Python 3.7; static/runtime tests prove controller modules contain no spawn primitive and cover interpreter resolution/version rejection, no pre-supervisor `uv`, protocol replay/unknown command/argv/environment/FD-role rejection, opaque-handle authorization, hung `uv`, bootstrap/controller crash, controller SIGKILL and unique-cgroup proven/unknown OOM, wrapper SIGTERM, child fork-before-move prevention, pidfd/start-token identity, delegated cgroup membership/resource read-back, unsupported cgroup plan-only refusal, guard-pipe endpoint ownership, D-state/cleanup-unconfirmed evidence, controller fallback request, and no false killed/clean result. Rollback: remove supervisor/bootstrap contract and root dispatch hook. Completion evidence: focused process tests show one OS spawner/receipt/fallback writer and no ordinary supervisor-managed descendant after completion. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.4 Implement private source trees, mutation and coverage adapters. Objective: build a bounded reviewed import closure, prove private-tree provenance, apply exactly one position-verified Cosmic Ray mutation, and collect target-only line coverage independent of ambient coverage configuration. Inputs: deterministic plan, matrix/dependency manifest, tool compatibility, and supervisor deadline. Outputs: private-tree builder, engine adapter, minimal hashed coverage rcfile, provenance plugin, source restore/purge logic, and tests. Affected contracts: first-order proof, source immutability, coverage/no-coverage classification, clock/entropy cache eligibility, and private-tree capacity. Validation: tests cover operator-local occurrence/span handshake, source drift, original hashes, closure manifest and 10x unrelated modules, dynamic-import refusal, same-size fixed-mtime bytecode isolation, exact mapped tests, multiple imported modules with one filtered target, cleared `COVERAGE_*`, malformed/missing/duplicate/oversized coverage, mapping/line no coverage, baseline-unavailable, private-tree file/byte/copy/free-space limits, and unchanged checkout hashes. Rollback: remove tree/engine/coverage modules. Completion evidence: focused tests mutate only temporary copies and produce deterministic plans/coverage facts. [validates:mutation.first-order-selection] [validates:mutation.narrow-scope] [validates:mutation.isolated-execution] [validation:test]

- [ ] 2.5 Implement fail-closed Linux worker isolation. Objective: deny and independently observe network, process creation, and non-private data access without pathname races or forgeable evidence. Inputs: private tree, reviewed allowlists, worker identity, policy digest, and supervisor-owned cgroup/start gate. Outputs: Landlock launcher, seccomp user-notification broker, `SCM_RIGHTS` listener handshake, controller-side `openat2` plus `SECCOMP_IOCTL_NOTIF_ADDFD`, child write-only guard stream, controller-private syscall audit, and tests. Affected contracts: kernel isolation, descriptor ownership, guard/audit integrity, and plan-only fallback. Validation: tests cover listener/tracee identity and FD closure, ACK/liveness failure, brokered allowlisted opens, symlink/pathname races, real SQLite/pandas-pyarrow/os.open/Path.open/mmap/DuckDB denial, caught Python/native violations overriding exit 0/1, credential/proxy scrubbing, reviewed native thread clone, denied process clone/fork/exec/network, complete/empty/truncated/duplicate/wrong-token independent streams, unsupported Landlock/seccomp fail-closed, and non-Linux plan-only. Rollback: remove isolation modules and leave execution disabled, not weakened. Completion evidence: focused isolation tests expose no real-data/network/process path and cannot have one evidence channel forge the other. [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.6 Implement bounded scheduling and terminal classification. Objective: admit work by CPU plus enforced per-worker cgroup memory controls, enforce per-mutant/global deadlines and output bounds, and assign one truthful terminal/phase/cleanup tuple under every race. Inputs: plan, baseline timings/`memory.peak`, supervisor opaque-handle API, isolated launcher, and closed model. Outputs: logical scheduler, finite queue, supervisor-handle client, sticky-integrity typed state machine, process-tree cancellation requests, immutable outcomes, and cache eligibility facts. Affected contracts: workers, timeout, cancellation, OOM, terminal algebra, and result cache identity. Validation: tests cover effective CPU calculation, controller/safety/renderer/worker memory equation and `memory.max/high/swap/pids` read-back, no-delegated-cgroup plan-only refusal, native threads one, closed baseline failure reasons and baseline timeout exclusion from mutant timeout, changed 60-second cap, execution-deadline admission, bounded output, every control-race order plus late integrity after natural exit, notification drain and both stream completion orders, complete terminal x mutation-applied x test-started matrix, exact pytest exits, unique-cgroup OOM proof/ambiguity, cleanup-unconfirmed infrastructure override, no full-suite fallback, and every closed terminal. Rollback: remove scheduler/executor modules. Completion evidence: focused race/count tests leave no ordinary child and never classify timeout/infrastructure/cancellation/cleanup failure as killed or cacheable. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.7 Implement generation-atomic report storage and recovery. Objective: publish one bounded root-hash-bound report generation under an independent report sequence, preserve sole-supervisor fallback truth, and provide safe deterministic inspection/repair. Inputs: immutable execution records, receipt journal, sealed Git scope, safe output root, and render limits. Outputs: report models/renderers/store, leases, redaction, report reservation/tombstone, exact `current.json`, fallback request, quarantine, retention-unit/evidence-tombstone handling, inspect/repair APIs, and tests. Affected contracts: `trade.mutation.report.v1`, invocation/fallback/manifest/inspect/repair/evidence-tombstone schemas, status/count algebra, and exit codes. Validation: golden tests cover every terminal/phase/cleanup tuple and mixed run, scanned/generated equations, manifest non-self hash, report reserve before pointer, exact run/sequence/root/size `current.json` recovery versus tombstone, interruption at every fsync/rename/pointer boundary, supervisor fallback requests/controller loss, first-run/corrupt/symlink/missing-generation recovery, evidence-expired inspect state, dry-run/apply exits, Git drift before publication, safe-root/lease races, redaction, synchronous retention or tombstone, truncation and renderer limits. Rollback: remove report store/render/recovery modules while preserving ignored evidence for manual audit. Completion evidence: every run status exposes one receipt-bound generation or supervisor-owned typed fallback and never stale current/unrooted evidence. [validates:mutation.truthful-report] [validation:test]

- [ ] 2.8 Implement disposable committed result cache. Objective: accelerate exact repeated outcomes without treating cache as evidence or weakening truth. Inputs: published generation, complete cache identity, staged eligible fresh outcomes, and output lock. Outputs: content-addressed entries, run commit markers, deterministic local eviction, bounded remote archive import/export, and tests. Affected contracts: cache identity/eligibility/retention. Validation: tests cover uncommitted/partial/degraded/cleanup-unconfirmed/stale/corrupt/clock-entropy-ineligible misses, same-key races, exact marker binding, 20000-entry/512-MiB eviction, at most 20 remote candidates/two downloads/512 MiB/deadlines, numeric run-attempt ordering, seven-day archive retention metadata, and correctness with no cache. Rollback: remove cache projection; reports remain authoritative. Completion evidence: cache hit/miss/eviction/restore reason snapshots. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 2.9 Implement trend ledger, aggregate, and reconciliation. Objective: make core/full trend continuity auditable under an independent sequence and bounded remote carriers. Inputs: published core/full report root/report sequence, score/key-set digests, GitHub run/attempt metadata, and output lock. Outputs: trend reservation/source/tombstone chain, high-water/checkpoint, `trend.jsonl`, bounded rolling aggregate validator, `reconcile` service, and tests. Affected contracts: trend sequence/epoch/projection schemas. Validation: tests cover independent report/trend sequences, changed/plan runs consuming no trend sequence, high-water/hash-chain/checkpoint recovery, numeric `(workflow_run_id,run_attempt)` ordering, duplicate same-high-water carrier, corrupt-highest `predecessor_invalid` without old fallback, unavailable epoch, 20-candidate/20-MiB metadata/two-download/64-MiB/180-second restore limits, 64-MiB aggregate, projection gaps, and dry-run/apply reconcile without fabricated predecessor/score. Rollback: remove trend projection/aggregate while reports remain readable. Completion evidence: deterministic chain and epoch golden fixtures. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.10 Implement baseline, exception, and capacity governance. Objective: evaluate score comparison only from verified detail evidence and admit scheduled modes only on a fresh qualified runner. Inputs: reviewed baseline/exceptions/capacity files, immutable bundles, comparison identities, runner probes, and qualification measurements. Outputs: detail-recomputing baseline evaluator, exception validator, serial/capacity qualification proposal/validator, and tests. Affected contracts: `trade.mutation.baseline.v1`, exception registry, `trade.mutation.capacity.v1`, and gradual policy. Validation: tests cover bundle/report/run/commit/tool/environment/key-set binding and recomputation, stored aggregate mismatch refusal, exact comparison/denominator/exception/no-coverage sets, RFC3339 future/rollback/five-minute-skew and 30/180-day interval rules, runner capacity identity versus measurement throttling, at least 30 completed mutants, worker/serial throughput/p95 runtime/20% time-memory-disk/headroom equations, identity/freshness refusal, and proposal never editing reviewed files. Rollback: leave baseline unestablished and scheduled execution disabled. Completion evidence: baseline and qualification golden decisions. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.11 Implement immutable CI bundle construction and independent validation. Objective: package exact receipt-bound evidence in the execution workspace and validate downloaded bytes without path trust. Inputs: receipt, referenced generation/fallback, workflow/repository/run/attempt identity, and closed failure codes. Outputs: `trade.mutation.bundle.v1`, local builder/validator, artifact metadata outputs, fresh-directory validator, and tests. Affected contracts: bundle/validation schemas and evidence integrity. Validation: tests cover exact receipt/generation-or-fallback binding, manifest/member/count/cleanup/root digests, extra/path-escaping/missing/corrupt members, run-attempt mismatch, exported artifact name/digest, no global-current/newest selection, and validation from a copied/downloaded directory with no original path. Rollback: disable workflow evidence route; local reports remain. Completion evidence: cross-directory golden bundle validation. [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:test]

- [ ] 2.12 Implement canonical CLI and root/facade contracts. Objective: expose one supervisor-first command grammar with deterministic argv/stdout/stderr/receipt/exit behavior. Inputs: controller services, supervisor protocol, root `trade` routing, and optional frozen dependency group. Outputs: Python CLI, pre-`uv` `trade dev mutation` dispatch, `scripts/mutation-test` facade, help and text/JSON renderers. Affected contracts: `changed|core|full|inspect|repair|qualify|reconcile`, lowering-only options, plan-only, verbosity, and exits. Validation: tests cover root/facade argv and exit parity for every subcommand, help, quiet/verbose, format, receipt export, plan-only, inspect/repair evidence-expired, qualify, reconcile dry-run/apply, missing dependency/preflight diagnostics, no pre-supervisor `uv`, frozen uv forwarding, Bash syntax and ShellCheck. Rollback: remove additive command/facade. Completion evidence: CLI golden snapshots and parity tests. [validates:mutation.bounded-execution] [validates:mutation.truthful-report] [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:test]

## 3. CI and Documentation

- [ ] 3.1 Add report-only GitHub Actions routing with strict cross-job evidence validation. Objective: run changed only for PR eligible definitions, core at `17 18 * * 1-6` UTC/manual, and full at `17 18 * * 0` UTC/manual on the reviewed ephemeral mutation runner without making mutation outcomes or full a PR gate. Inputs: canonical command, invocation receipt/bundle artifact metadata, reviewed capacity file, and rolling aggregate contract. Outputs: `.github/workflows/mutation-testing.yml`. Affected contracts: CI events, runner preflight, exact concurrency semantics, outer timeouts, result cache versus trend storage, capacity admission, bundle retention, and summary behavior. Validation: static workflow tests assert `runs-on: [self-hosted, linux, x64, ephemeral, trade-mutation-v1]`, no secrets for fork PR, image/namespace/mount/delegated-cgroup/memory/Landlock/seccomp preflight, explicit base SHA, literal crons, 15/35/100 minute limits, real `${{ github.repository }}`/PR expressions, PR cancellation, shared long group with `cancel-in-progress: false`, native one-running/latest-pending semantics, exact manual input, report-only execution exit capture, original exit in receipt/summary, execution-job `if: always()` local bundle validation/upload, job outputs for exact artifact name/digest, separate job download and fresh-directory byte validation, 14-day bundle retention, bounded cache/90-day aggregate restore by run-attempt and invalid epoch, capacity identity/equation/freshness refusal, hard-runner-loss best effort, and no PR core/full; YAML parses if available. Rollback: remove workflow. Completion evidence: workflow pytest and config parse pass. [validates:mutation.ci-routing] [validation:test]

- [ ] 3.2 Add concise mutation-testing operations documentation. Objective: document rationale, exact included/deferred scopes, modes, commands, lowering-only budgets, run/mutant statuses, survivors, exact exceptions, matched-mutant baseline policy, CI triggers, artifact/trend recovery, and anti-gaming. Inputs: final CLI/config/report contracts. Outputs: `docs/mutation-testing.md` and README developer link. Affected contracts: developer guidance. Validation: documentation review covers unresolved base, unsupported tool/API, baseline/provenance failure, no coverage, stale/corrupt cache, source/report limits, cancellation/orphan cleanup, missing artifact, equivalent exceptions, and no meaningless assertions, with zero implementation inconsistencies. Rollback: remove docs/link. Completion evidence: source-cited review finding set and text hygiene pass. [validates:mutation.gradual-policy] [validates:mutation.ci-routing] [validation:review]

## 4. Validation and Delivery

- [ ] 4.1 Run focused controller/config/CLI/workflow tests and the original stable core unit suite. Objective: prove mutation infrastructure and its selected tests are stable before a real mutation. Inputs: completed implementation. Outputs: passing focused evidence. Affected contracts: all. Validation: focused pytest, Ruff, BasedPyright, compileall, config/TOML/JSON syntax, bash syntax, and workflow static tests pass. Rollback: fix owning unit before commit. Completion evidence: command/result table. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.2 Run real bounded mutation smoke and capacity modes. Objective: demonstrate real high-value mutants without touching production source and produce reviewable scheduled-capacity proposals. Inputs: clean worktree, eligible changed fixture/branch diff, core/full config, cold cache, serial and capacity profiles on the exact `trade-mutation-v1` runner. Outputs: changed report/bundle, bounded core smoke, and cold-cache core/full qualification evidence. Affected contracts: changed/core/full execution, resource admission, capacity schema, immutable bundle, and report format. Validation: changed `generated_mutants`/executed stay below 150 and 600 seconds; bounded core smoke preserves upper limits; cold core/full record runner identity, worker count, serial throughput, planning/copy/baseline/per-mutant p50/p95/render/fsync/cleanup and cgroup memory/disk headroom; pass equations are recomputed; reviewed capacity remains disabled unless qualification passes; reports/bundles confirm counts, survivor details, cleanup state, root hashes, and unchanged source hashes. Rollback: discard ignored reports/cache and keep scheduled execution disabled. Completion evidence: validated bundle summaries, qualification proposal, capacity table, and unchanged source hashes. [validates:mutation.bounded-execution] [validates:mutation.first-order-selection] [validates:mutation.truthful-report] [validation:test]

- [ ] 4.3 Run repository gates and record the known full-suite baseline. Objective: ensure no production behavior change and distinguish pre-existing debt. Inputs: final diff. Outputs: quality/test evidence. Affected contracts: existing repository. Validation: `./trade dev check --show-plan`, `./trade dev check`, focused and stable core pytest, `python -m compileall trade_py tests`, `git diff --check`, and full pytest run; only the pre-existing `test_verify_reports_current_then_stale` failure may remain unless upstream fixes it, and any new failure blocks delivery. Rollback: fix or revert mutation-owned files. Completion evidence: before/after failure comparison. [validates:mutation.narrow-scope] [validates:mutation.ci-routing] [validation:test]

- [ ] 4.4 Run the six-role implementation review on one frozen commit, resolve every P0 and implementation-blocking P1, refresh strict approval if governed artifacts changed, then commit/push/open a PR. Objective: deliver one isolated reversible feature branch. Inputs: frozen diff and validation evidence. Outputs: final consensus, strict pass, intentional commits, remote branch, and PR. Affected contracts: developer mutation testing only. Validation: review cites actual code and evidence, reaches consensus with zero P0 and no implementation-blocking P1, confirms a clean worktree and required commit trailers, and records the project-local GitHub CLI PR result. Rollback: close PR/revert commits; no data rollback. Completion evidence: commit SHAs, review disposition, gate output, and PR URL. [validates:mutation.bounded-execution] [validates:mutation.isolated-execution] [validates:mutation.truthful-report] [validates:mutation.ci-routing] [validation:review]
