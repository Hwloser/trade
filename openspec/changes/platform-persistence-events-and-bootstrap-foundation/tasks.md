## 1. Governed Design Approval

- [ ] 1.1 Run the diagnostic design gate and resolve every deterministic finding against the audited Platform persistence, EventBus, backup, Web runtime, CLI composition and migration sources. Objective: freeze an implementation-ready foundation without changing runtime behavior. Inputs: the parent architecture, exact approved Kernel contracts, current source audit, four capability specs and design policy. Outputs: internally consistent proposal, design, specs, tasks and design-quality evidence. Affected contracts: DatabaseRuntime, local transaction, command/delivery, restore, Bootstrap and CapacityEnvelope. Validation: `./trade dev design-check platform-persistence-events-and-bootstrap-foundation` exits zero with no blocker or warning. Rollback: revise only this change's OpenSpec artifacts. Completion evidence: the report selects public-contract, persistent-write, schema-migration and runtime-concurrency profiles and lists no unresolved finding. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:test]

- [ ] 1.2 Run the required six-role design review in a clean review worktree, reconcile contradictory findings once, resolve every P0 and implementation-blocking P1, and record digest-bound evidence. Objective: challenge persistence safety, concurrency, ownership, temporal/data integrity, operational evidence and future Capture/news/stream integration before implementation. Inputs: one frozen commit and its artifact/policy digests. Outputs: `design-review.toml` with reliability, performance, architecture, data-quality, observability and news/future-integration verdicts plus finding dispositions. Affected contracts: every capability in this change. Validation: each role cites current files, identifies P0/P1/P2 counts and reviews the exact commit/digests; accepted residual findings name a child and owner. Rollback: amend design artifacts, invalidate the prior digest and repeat the panel. Completion evidence: six approvals, zero unresolved P0 and no unresolved implementation-blocking P1. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:review]

- [ ] 1.3 Run strict design approval before any production or test implementation. Objective: bind all later child changes to the reviewed artifact generation and exact Kernel prerequisite. Inputs: current `design-review.toml`, design policy and reachable reviewed commit. Outputs: strict approval report. Affected contracts: all Platform foundation contracts and activation prerequisites. Validation: `./trade dev design-check platform-persistence-events-and-bootstrap-foundation --strict` exits zero and reports matching current artifact/policy digests. Rollback: do not implement; repair and re-review any changed governed artifact. Completion evidence: strict pass at the current date with six-role consensus and zero P0. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:review]

## 2. Independent Foundation Children

- [ ] 2.1 Create and strictly approve a `platform-persistence-runtime` child, then implement framework-free DatabaseRuntime contracts, owner-bound read-only/compatible-writer/migration-leader modes, local owner transactions, capability/writer fences, checkpointed migration coordination and the one allowlisted `LegacySchemaBootstrapAdapter`. Objective: separate observation, writes and migrations without creating another global database facade. Inputs: exact merged Kernel contracts, architecture baseline/guard, current v2-v22 migration behavior and temporary legacy fixtures. Outputs: additive Platform persistence package, owner-specific repository ports, schema capability/migration/lease metadata and public contract tests. Affected contracts: DatabaseRuntime request/handle, LocalTransactionContext/Result, MigrationRegistration/Plan/View and legacy compatibility receipt. Validation: temporary-root tests prove read-only creates nothing, stale fences roll back all participants, one owner state/audit/inbox/outbox transaction is atomic, cross-Context writes fail, leader races elect one fence, checkpoints resume by digest, unaware legacy writers block incompatible cutover and the bridge is reachable only from Bootstrap migration mode; architecture checks reject raw connection and unapproved legacy imports. Rollback: disable the target capability, select the legacy constructor, preserve additive metadata and prior schema generation, and revert the isolated child PR without deleting records. Completion evidence: focused unit/integration/contract/architecture tests, migration rollback rehearsal, compile/check gates, strict child approval and no real-data access. [validates:platform.persistence-runtime] [validation:test]

- [ ] 2.2 Create and strictly approve a `platform-command-and-delivery-store` child, then implement durable command admission, outbox/inbox, fenced lease/ack, explicit ordering, bounded retry, dead letter and audited redelivery beside the unchanged EventBus. Objective: provide at-least-once transport with exactly-once effective owner transition and no unbounded worker retry. Inputs: Task 2.1 transaction runtime, exact Kernel command/receipt/error contracts and current EventBus recovery fixtures. Outputs: Platform ingress/events contracts, owner transaction protocol adapters, additive technical repositories and bounded operator queries. Affected contracts: OperationReceipt/ErrorEnvelope admission, OutboxEnvelopeRecord, DeliveryLease/Receipt, InboxReceipt, OrderingContract/Ref, DeadLetterView and RedeliverMessage. Validation: two-connection and subprocess crash fixtures cover equivalent/conflicting ingress, refusal outcome priority, commit-before-dispatch, duplicate/mismatched inbox, crash before/after consumer commit and ack, stale lease/fence, N+1-before-N, epoch regression, finite gap exhaustion, retry exhaustion, terminal-persistence outage, hot-key fairness, concurrent redelivery CAS and safe bounded status; tests assert no retry or child process/thread survives its deadline. Rollback: stop target admission/dispatch, retain immutable receipts/outbox/DLQ for reconciliation, keep legacy EventBus selected and revert the child without reclassifying legacy rows as formal facts. Completion evidence: focused unit/integration/contract/golden/capacity tests, process-tree cleanup evidence, EventBus compatibility suite, strict child approval and zero duplicate effective transitions. [validates:platform.message-delivery] [validation:test]

- [ ] 2.3 Create and strictly approve a `platform-backup-certification-and-restore` child, then implement consistent backup certification, bounded manifest/archive parsing, no-follow staged extraction, per-member/archive digest verification, writer fencing, activation-journal CAS, rebind health and prior-generation rollback only on temporary roots. Objective: make restore evidence truthful before any real-data activation. Inputs: Task 2.1 runtime/migration fences, current backup command snapshots and synthetic SQLite/artifact generations. Outputs: BackupManifest/Certification, RestoreOperation/View, activation journal, certified artifact adapter and bounded operator query. Affected contracts: backup certification, remote publication status, restore state machine, generation activation and rollback receipts. Validation: malicious traversal/link/device/duplicate/collision/bomb, missing/extra member, digest/schema mismatch and partial remote fixtures fail before activation; crash injection at every transition proves one writable generation; stale writers fail; target health failure CAS-restores the prior generation; legacy CLI/function/API snapshots remain selected and explicitly uncertified. Rollback: disable the capability, preserve staged/certification/operation evidence, CAS back to the verified prior generation and retain legacy commands; never overwrite the real data root. Completion evidence: temporary-root unit/integration/golden/crash/rollback/capacity tests, independent manifest verifier, strict child approval and no unrestricted `extractall`. [validates:platform.backup-restore] [validation:test]

- [ ] 2.4 Create and strictly approve a `platform-bootstrap-profiles` child, then implement the sole target composition root, typed `cli`, `http`, `worker`, `scheduler` and `migration` profiles, explicit lifecycle/unwind, technical capability registry and deterministic CapacityEnvelope harness while leaving current entrypoints on selected compatibility paths. Objective: converge concrete construction without turning Bootstrap into a service locator or claiming shutdown guarantees prematurely. Inputs: Tasks 2.1-2.3 public APIs, architecture guard, current CLI/Web composition inventory and exact shutdown-hardening prerequisite. Outputs: typed profile handles, compatibility selection, lifecycle/status contracts, capacity schemas and architecture tests. Affected contracts: BootstrapProfile/Capabilities, lifecycle states, compatibility delegation and CapacityEnvelope. Validation: architecture tests prove only Bootstrap imports concrete adapters and Platform has no business vocabulary; profile tests cover read-only CLI, partial-start reverse unwind, competing owner fences, degraded optional capabilities and bounded status; 1x/10x delivery/migration/restore fixtures record finite workers/queues/deadlines, resource peaks and pass/defer/overload without zero substitution. Rollback: keep every non-migrated entrypoint on its existing constructor, disable target profiles and retain prior compatibility selection. Completion evidence: unit/contract/architecture/capacity tests, unchanged CLI/HTTP/OpenAPI/SSE snapshots, strict child approval and no formal current-runtime shutdown receipt. [validates:platform.bootstrap-capacity] [validation:test]

## 3. Runtime Adoption and Compatibility Gates

- [ ] 3.1 Strictly approve and implement `runtime-owner-shutdown-and-recovery-hardening-v1` before routing EventBus, WebResourceContainer, RuntimeCommandRunner, FastAPI lifespan or daemon ownership through target Bootstrap. Objective: eliminate the audited close hang and make deadline/cancellation receipts truthful before lifecycle adoption. Inputs: current unbounded terminal-persistence loop, concurrent-stop behavior, executor-tail wait and signal-to-reap audit. Outputs: bounded persistence retry, one monotonic shutdown deadline, explicit residual owners, idempotent concurrent stop, partial-start cleanup, process-tree termination and crash takeover. Affected contracts: Kernel lifecycle/control/shutdown receipts and Bootstrap adoption prerequisite. Validation: fake-clock and real subprocess/Uvicorn fixtures prove TERM/KILL escalation, executor and child-process cleanup, no background continuation, bounded repeated stop and signal-to-reap completion; failures retain owner/fence evidence rather than reporting stopped. Rollback: keep legacy lifecycle selected and do not activate target runtime profiles. Completion evidence: independent strict child approval, focused runtime tests and six-role implementation review with zero P0. [validates:platform.bootstrap-capacity] [validates:platform.message-delivery] [validation:test]

- [ ] 3.2 Create one compatibility child per selected CLI, HTTP, scheduler or EventBus message family; delegate exactly one entrypoint/profile at a time with dual observation, snapshots, a kill switch and a minimum 30-day retirement window. Objective: preserve public behavior and prevent simultaneous independent legacy/target owners. Inputs: Tasks 2.1-3.1, consumer inventory and current contract snapshots. Outputs: thin compatibility shim, selected route/command/message-family mapping, parity evidence and retirement record. Affected contracts: existing CLI args/exit/data, HTTP methods/status/payload/error/SSE, scheduler command and EventBus observation surfaces. Validation: legacy/new snapshots and owner-fence tests pass; absent actor, ordering, payload proof or certification remains unknown/legacy rather than fabricated; no Interface reads Platform tables or starts migrations/work. Rollback: switch the named selection to legacy, stop new target admission, reconcile retained target records and leave other entrypoints untouched. Completion evidence: one reviewable PR per surface, strict child approval, compatibility window evidence and successful rollback rehearsal. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:test]

## 4. Implementation Validation and Delivery

- [ ] 4.1 For each implementation child, run the repository quality plan/check, focused Python tests, compile validation, architecture/contract/golden/capacity suites, migration/restore rollback rehearsal and `git diff --check`, using only temporary roots. Objective: prevent an individually green slice from violating shared dependency, durability, compatibility or process-cleanup invariants. Inputs: frozen child implementation and its test fixtures. Outputs: command/result summary with compatibility, capacity, data-safety and residual-risk evidence. Affected contracts: the child contracts named in Tasks 2.1-3.2. Validation: `./trade dev check --show-plan`, `./trade dev check`, focused `uv run pytest`, `uv run python -m compileall src trade_py trade_web tests` where paths exist, and `git diff --check` all pass; any C++/frontend path touched also runs its owned build/test. Rollback: do not commit the failing unit; correct only its owner slice or disable it. Completion evidence: exact commands, elapsed results, process cleanup and no-real-data assertion are recorded in the commit/PR. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:test]

- [ ] 4.2 Run the six-role implementation review against every child diff before merge and resolve all P0 findings. Objective: verify that code still implements the approved boundaries and failure products. Inputs: frozen implementation commit, validation evidence and current strict design digest. Outputs: reliability, performance, architecture, data-quality, observability and future-integration findings with disposition. Affected contracts: all implemented child contracts. Validation: reviewers cite actual code/tests, confirm no design drift and identify owned P1/P2 follow-ups; any contract/state/ownership drift invalidates strict approval and returns to design review. Rollback: amend the child implementation and repeat focused validation/review. Completion evidence: zero unresolved P0 and no hidden shutdown/data/compatibility claim. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:review]

- [ ] 4.3 Inspect worktree safety, commit only each validated implementation unit, push after three to five commits, open its PR and squash-merge only after all gates pass. Objective: preserve isolated, auditable and reversible delivery. Inputs: passing strict design, implementation validation and review evidence. Outputs: intentional commits, remote branch, PR and clean master squash history. Affected contracts: only the child named by the PR. Validation: `git status -sb` excludes local data/cache/unrelated work; commit records scope/tests/compatibility/data risk and the required TRAE CLI co-author trailer; project-local GitHub CLI push/PR flow succeeds. Rollback: disable/revert the isolated child or select its preserved compatibility generation; never reset or delete unrelated state. Completion evidence: commit hashes, PR URL, squash commit and retained rollback source are recorded. [validates:platform.persistence-runtime] [validates:platform.message-delivery] [validates:platform.backup-restore] [validates:platform.bootstrap-capacity] [validation:review]
